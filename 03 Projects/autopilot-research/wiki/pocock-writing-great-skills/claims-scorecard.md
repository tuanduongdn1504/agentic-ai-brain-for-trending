# Claims scorecard

14 substantive claims, reconciled across Workflow `wf_d267ed85-320` (5 dives + 14 refute-first verifiers + completeness critic, all Haiku 4.5) **+ ~12 Opus main-loop `gh api`/WebFetch/WebSearch anchors + the full transcript**. Verdict rubric per `CLAUDE.md`. Where a Haiku verifier judged a claim against the *current published skill* rather than *what the talk says*, the main loop overrode it using the transcript + first-party docs — 5 such overrides, all logged in [[pocock-writing-great-skills/caveats-and-corrections]].

| # | Claim (what the talk asserts) | Verdict | Note |
|---|---|---|---|
| CL1 | Skills are user-invoked or model-invoked; model-invoked exposes a description in-context (context load) + lets the agent fire it; user-invoked sets `disable-model-invocation: true` (zero context load, manual only); you can always invoke manually | **CONFIRMED** | **Verbatim vs Anthropic's own Claude Code docs** (frontmatter table: `disable-model-invocation: true` → "Description not in context, full skill loads when you invoke"). [[pocock-writing-great-skills/disable-model-invocation-mechanism]] |
| CL2 | Model-invoked skills also cost **unpredictability** (model may not fire the skill even when perfect) → forces you to **eval** your skills; user-invoked removes that class of problem; neither is strictly better | **CONFIRMED** | Talk says this explicitly [~6:36–7:14]. *(Override: a verifier rated MISLEADING by judging vs the published skill, which reframes cost as context-vs-cognitive load; the talk's words are accurate.)* |
| CL3 | `mattpocock/skills` is **primarily user-invoked** (Matt prefers control/minimal agent context); `obra/superpowers` is **primarily model-invoked** ("gives the agent superpowers") | **CONFIRMED** | Definitive `gh api` count: **23/40 user-invoked (~58%)**; superpowers README confirms auto-triggering. *(Override: verifier miscounted 22 total → "50/50"; recount to 40 confirms majority.)* Two-layer nuance in [[pocock-writing-great-skills/trigger-invocation-and-superpowers]] |
| CL4 | A skill = **steps + reference**; keep `SKILL.md` minimal (maintainability + tokens); move branch-specific reference behind a **context pointer** into an external file | **CONFIRMED** | Stated verbatim in `writing-great-skills` SKILL.md; demonstrated by `domain-modeling` (ADR-FORMAT.md + CONTEXT-FORMAT.md) |
| CL5 | Example skill **`2PRD`**: 3 steps (find context / confirm test seams w/ user / write PRD) + 2 references (test-seam, PRD template), single branch | **CORRECT-BUT-INCOMPLETE** | Accurate at recording; the skill is now **`to-spec`** (no `2prd` trace remains; description says "you may know this document as a PRD") and has evolved to **publish to the issue tracker**. Naming history unconfirmed. [[pocock-writing-great-skills/talk-vs-published-skill]] |
| CL6 | Example skill **`domain-modeling`**: updates a `context.md` glossary / creates ADRs / or neither → multiple branches → templates live outside `SKILL.md` behind context pointers | **CONFIRMED** | Exact match: `CONTEXT.md` + `docs/adr/` + `ADR-FORMAT.md`/`CONTEXT-FORMAT.md`, "create files lazily." *(Override: verifier rated MISLEADING over the "or neither" phrasing — but "neither" = branch-dependent template loading, consistent with lazy creation.)* |
| CL7 | **Leading words**: dense-meaning terms the agent echoes in reasoning traces, changing behaviour; verify by watching the traces; example "vertical slice" | **CONFIRMED** | Technique defined in GLOSSARY; "vertical slice" is real dev terminology; trace-echo is a sound heuristic. *(Override: verifier rated MISLEADING because the repo's house examples are "tight"/"red" not "vertical slice" — but the talk's example is illustrative + valid.)* |
| CL8 | **Legwork**: agents rush a step when the goal is visible; split into separate skills (one step at a time) to force legwork; plan-mode's clarifying-questions rushes → fixed by `grill-with-docs` before `to-spec` | **CONFIRMED** | `grill-with-docs` = "relentless interview"; `to-spec` = "Do NOT interview the user" — the two enforce the ordering. Plan-mode rush is a plausible practitioner observation |
| CL9 | Pruning: keep skills small; **single source of truth**; don't duplicate reference material | **CONFIRMED** | GLOSSARY defines both; repo practices it (grill-me ~1 line; domain-modeling delegates formats to external files) |
| CL10 | Pruning: **sediment** = stale accreted material from shared editing; fix via structure (move to branch / kill) | **CONFIRMED** | Talk states this; **caveat**: published skill refines sediment (prune) vs **sprawl** (structure) as distinct. [[pocock-writing-great-skills/talk-vs-published-skill]] |
| CL11 | Pruning: **no-ops** = lines that don't change behaviour (common when an agent writes the skill); find via **deletion test**; this is how Matt keeps skills small | **CONFIRMED** | Talk states all three; **notes**: "deletion test" is talk-only terminology (concept encoded, name not); published skill frames info-hierarchy as the *primary* minimalism lever |
| CL12 | `mattpocock/skills` is "**one of the most popular** engineering skill sets out there" | **CONFIRMED** | **170,613★** (created 2026-02-03, MIT) — top-tier; the hedge "one of" is accurate. Note: `obra/superpowers` is larger (254,795★). Vault Pattern #52 |
| CL13 | This is the talk he'd have given at **AI Engineer World's Fair 2026** (SF) but pre-recorded — couldn't attend ("family matters") | **CONFIRMED** | World's Fair 2026 = June 29–July 2, Moscone West, SF; video uploaded opening day 2026-06-29. Not on the public speaker schedule (consistent w/ a remote contribution) |
| CL14 | He encoded the checklist into a **`writing-great-skills`** skill you can download and run over your own / community skills to improve them | **CONFIRMED** | Skill exists (`disable-model-invocation: true`, "all reference"); user-invoked skills ARE "run" by manual invocation. *(Override: verifier rated MISLEADING reading "run" as agent-auto-execution.)* Skill has evolved past the talk |

## Tally

**13 CONFIRMED · 1 CORRECT-BUT-INCOMPLETE · 0 MISLEADING · 0 FALSE · 0 FABRICATED.**

## Read

The **cleanest content-rich talk in the corpus**. A domain authority (author of the repo, the vault's Pattern #52) describing a **documented mechanism** — so nearly every claim was checkable against Anthropic's own docs + the live repo, and all held. The one non-CONFIRMED item is *evolution since recording*, not error. It sits at the honest pole with [[github-copilot-cli-agents/_index]] and [[codesistency-mobile-app-course/_index]], and is the least-hedged of the three because it's self-referential.

**The verification's real work was on the workflow, not the talk:** 5 of 14 Haiku verdicts (CL2, CL3, CL6, CL7, CL14) plus a dive's token-mechanic finding were **over-strict against the current skill/docs and overturned in the main loop** — the same Haiku skill/docs-lag failure mode seen in [[local-ai-coding-agents/_index]], [[google-ai-studio-github-import/_index]] and [[codesistency-mobile-app-course/_index]]. Full override log: [[pocock-writing-great-skills/caveats-and-corrections]].
