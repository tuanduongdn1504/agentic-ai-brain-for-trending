# Seth Phaeno — The New Age of Modern Terminal Multiplexer Herdr


> Source: 27B50lXinWM.en-orig.vtt
> Converted: vtt-to-md.py
> Format: `[MM:SS] line` — verbatim auto-sub, deduplicated, word-timing tags stripped


**[00:02]** A new modern terminal multiplexer that takes a lot of inspiration from tmux, but adds a few things that has really impressed me. This is called Herder. On macOS, you can use brew install Herder. Curl is also available for Mac as well as for Linux. For Windows, you can use PowerShell. For NixOS, it is also supported here. For this video, I'm on the latest version 0.7. Once installed, you can simply start it up by typing Herder. On startup, you can see that we have the

**[00:31]** tabs up here on the top. Then we have our sidebar to the left, which is going to be used for managing our projects and workspaces. Herder uses prefix for key maps. The default prefix is control B, which is exactly how I use my tmux as well, but it can be remapped later in the config. To detach from Herder, we can use prefix Q, which is control B and Q. That is going to bring us back to our terminal while keeping things running in the background. To reattach, we can simply run Herder.

**[00:58]** If we want to stop the Herder server completely, we can use Herder server stop. So, anything running in the background is going to be shut down. This includes any agent processes as well. However, Herder separates session states from process states. So, your layout, tabs, and panes are saved as a snapshot. If your tmux server dies, everything dies unless you use a plugin to restore and bring them back. The out-of-the-box support for key maps and prefix is amazing. As an everyday tmux

**[01:25]** user, this feels right at home. You don't really need to set up anything at all unless you want to do some remapping. From the documentation, this is the path to where Herder would read the configuration from. Once you create your config file, you can then use this command to automatically generate the default configurations and go from there. Also, when you hit prefix, at the bottom here, you'll see hints for key actions that are available. Like question mark shows all the key bindings, including

**[01:51]** the ones that are remapped in your configuration. From here, let's jump into the settings with prefix S. That brings us to the themes, which are built in. These are not plugins. You can preview the theme by just cycling through them and hit apply on the one you want. The second tab is for sound alerts. The third tab controls your notification pop-ups, either from Herder, terminal, or the system. We'll get to pane labels and integrations later. And the last one is the experimental features. Finally, I also

**[02:20]** remap my settings to prefix semicolon. Moving on to tabs and panes, for the sidebar over here, if I hit prefix b, that hides it. Prefix b again brings it back. So, this can be used to toggle the sidebar. For tabs, just like tmux, you can create a new tab with prefix c. We can also rename the tab with prefix shift t, but I changed that key map to prefix comma. That's going to allow me to rename this tab. So, let's name this

**[02:46]** config. In my Herder configuration, in the config.toml file, as you can see here, I remapped the rename tab to prefix comma instead of prefix shift t. We can also move between tabs using prefix n and prefix p. Jumping to tabs directly is also possible here. Like prefix one, prefix two, prefix three. This is very similar to tmux.

**[03:14]** Finally, we can simply close tab with prefix x. Now, let's take a look at the panes. We can do a vertical split with prefix v. That's a default key binding. Then we can also do a horizontal split with prefix dash or prefix minus. I also remapped my vertical split here to a new key map. Navigating panes are amazing as well because it does have vim-style key bindings built into it. So, as you can see, prefix j, prefix h, prefix l. But

**[03:42]** guess what? Those can be remapped to control h, j, k, and l exactly like vim tmux navigator. It's great because we shouldn't really need a plugin for things like this. But this might not work for you though because Herder will take precedence over those binds. So, keep that in mind. And if you make any changes to your configuration, you can always hit prefix capital R to reload your config. Panes can also be resized. Prefix R will put you in resize mode, and again, the Vim keys work. As you can see from the resize bar at the bottom

**[04:09]** here, you can just adjust the width or the height easily. Panes can also be maximized using prefix Z. That puts the current pane into a full screen. Hitting it again will toggle it back to the previous state. Did I mention the copy and select mode as well? That actually has a Vim style key bindings built into it. Hitting prefix open bracket puts you into a select and copy mode, allowing you to walk through your history, so you can grab whatever output you need. I also remapped resize to prefix capital R and

**[04:39]** change reload to prefix R instead. And zoom remapped to prefix M. Without a doubt, Herder comes with such a great keyboard support. But I think some of you will actually appreciate the mouse support that it comes with as well. With Tmux, mouse support is not enabled by default. You would have to enable it in your config. Even then, it's pretty basic. So first off, you can see here that we can basically just switch tabs by clicking them. Right clicking the current pane will also give you the option to split right or split down. Selecting zoom will also maximize the

**[05:07]** current pane. So the right click itself can do quite a lot. For example, let's say we close a pane, it focuses on the right pane. If we go to the unfocused pane, right click, we can swap that with the focus pane. I know we're just swapping panes here and there, but it's pretty cool to see. Which can also be done from the keyboard using prefix shift and a direction of the Vim keys that you want to go to. Drag to resize is also available, and it works really well as an alternative for non-keyboard users. The sidebar also works the way

**[05:35]** you would expect it to. And the menu tab on the sidebar here also allows you to access the settings, the key binds, reload your config, the patch notes, or detach from your server. As much as I don't use my mouse, I like how it doesn't force you to just use key binds for the basic functions. Moving on to workspaces, in Herder, instead of traditional sessions like in tmux, it uses something called workspaces. A workspace is basically a container for your tabs and panes, but it's built around projects. They are not as

**[06:02]** isolated as tmux sessions, but they are much better suited for organizing different projects and agents side by side. For example, I can have one workspace for my main project, another workspace for my side project, one for notes, just like in tmux, and maybe another one just for testing agents. Each workspace keeps its own tabs, panes, and agent states visible in the sidebar. Herder also has its own remote and session management, too, which we will talk about later. Let's move on to

**[06:30]** key maps for workspaces. To create a new workspace, use prefix capital N, which I remapped to prefix capital W. So, as you can see, that's going to go ahead and create new workspaces. To close them, you can hit prefix capital D and confirm it. You can rename workspaces using prefix capital W, which I remapped to prefix dollar sign. So, I'm going to rename this current workspace to something like main and all caps workspace. There's also a list and go to workspaces

**[06:59]** using prefix G. Again, I remapped that to prefix S. Hitting the key bind will open up this pane where you can navigate through your workspaces, tabs, and panes. Or, you can also hit forward slash to search for the names of those spaces and jump there. This is very similar to list sessions in tmux. There's also something called nav mode, or the key bind is actually called workspace picker. So, if I hit prefix W, once in this nav mode, we can directly jump to a workspace by hitting the

**[07:28]** number of that workspace. So, prefix W 3 4 5 and so on. Or, I can go into nav mode and use the up and down arrow keys to automatically focus the workspace sidebar and select where to go, which I also remapped to control K and J. As long as you're in nav mode, using vim arrow keys moves your focus around the panes or hit tab to cycle through the panes. This is just focusing the panes and doesn't actually put you inside the

**[07:55]** pane itself. You might also notice the bar down here that it has other options available in nav mode as well. For example, our go to workspaces is also showing up here. This is where Herder really stands out as well, agent awareness. When running agents inside Herder, it automatically detects them in your workspace, which you can see from the symbol. Expanding the sidebar will allow you to see more of the agents, like which workspace they're in and what agents are running. Like if an agent is working, running,

**[08:22]** and idle, or blocked requiring your permissions or done when it's complete. This becomes really handy when you're running multiple agents at the same time. Even in the go to and list workspaces, you can see the different agent states running, which means you're able to search and jump there. You'll also notice something called states, where you can filter out the states of those agents. So yeah, if you have more than 10 windows running agents everywhere, then I guess this is going to help you a lot. Another big advantage is that agent keeps running even after I

**[08:51]** detach from Herder. I can come back later and everything is still there the way it was. Like I said before, detaching doesn't actually stop any processes. Agent key maps are not mapped by default, so you'd have to go into the config and map them yourself. Some useful config options include things like resume agents on restore. This is false by default. This automatically resumes supported agents enter their native conversation sessions. The supported agents are listed in the integration tabs of the setting. Here

**[09:18]** you see three agents show up as available but not installed. So I'm going to go ahead and install this. And also, show agent labels on pane borders. This shows the detected agent name in the pane borders when you haven't manually named them. This is the same as pane label tabs we saw in the settings. For example, I know Hermes and Codex is supported as Herder integrations, but Grok isn't. So, let's say my conversation stops here and I decide to stop the server.

**[09:46]** Upon reattaching, we should see that we still have the supported agents restored back to their current session and conversation. So, instead of three agents running, now we only have two. The next thing I usually want is a fast way to build the actual interface that those agents are helping me create. That's where Hosting Your Horizons comes in. It's a builder that is able to create full web apps just by describing them. First, I need to access it. I went to the Horizon page and the starter plan is what we're going to use. It's the middle ground for most users, giving

**[10:14]** access to Horizon builder itself that includes a free domain, five mailboxes for website, and any hosting services for the first year. That is if you decide to go with the annual plan. It also comes with a 30-day money-back guarantee. AI is already built into it. So, you have things like chatbots, image generators, or smart assistants to your app without needing extra accounts like OpenAI or Gemini. And that's without additional billing. You also get access to their AI assistant Cody to help guide

**[10:40]** you during the build and it doesn't even use up your credits. If you're interested, the link in the description will take you straight to the page. Use my code shown on the screen here at checkout to get an extra 10% off on top of the current ongoing discount. I think that's a pretty good deal. Once I have access to it, I can go to Horizons on the left side and start giving it some ideas on what I want to build. Gave it a prompt to build a simple web app for some personal movie tracking stuff. It will then take you to a new page and

**[11:06]** start generating the whole thing. And this is what I ended up with, which is actually pretty impressive. But, it still needed a lot of improvements, so I reviewed it, made a few changes cuz at the end of the day, we're the ones doing the final checks. After a few more prompts and debugging, it ended up like this. The basic functionality of it works. It got the state changes correctly and everything looks good. Now, I'm going to export the whole thing and actually deploy this on VPS. From here, I'll navigate to the code section and at the bottom, I'm going to export the code, which will give us the zip

**[11:34]** file. For VPS, I'm going to use Ubuntu 24.04 LTS, then set up my SSH key and root password. Then I basically just SSH into my VPS, which I already set up, and SCP the whole project into my VPS, NPM run build the project again with the help of both PM2 and Nginx. All I have to do now is run my IP in the browser, and this should just work. And there it is, that whole thing built with Horizon up and running on VPS. You can take this even further and add your free domain to

**[12:01]** this as well. Big thanks to Hostinger again for sponsoring this video. Now, back to it. When we talk about sessions in Herder, what we're in right now is already a session containing multiple workspaces. So, let me detach from this session and show you what I mean. I'm going to create a new session by saying Herder session attach, followed by the name of the session. Now, we're in a completely new session separated from the other one. We can obviously create multiple sessions, but we can't really create a session from within the current session.

**[12:29]** Sessions can be listed using Herder session list. It will always use default as its main session. Let's say I detach from this session, and I run Herder, that attaches to the default. But if I want to go back to another session, I'll have to detach and run the session command again. Sessions can also be stopped using Herder session stop. If we run session list again, we should see it saying stopped. You can delete sessions as well, but

**[12:55]** note that you have to stop the session before it can be deleted. Sessions cannot be renamed. The only way to do that is to create a new session all together. There's also no direct key binding to switch between sessions. Herder leans more on workspaces. It makes sense to see sessions kept light. You just attach to a new one when you actually need a fully separate environment, and not just using sessions as workspaces. Herder also shines when working on remote servers. Let's say I'm

**[13:22]** just in my default shell, not attached to anything. Normally, you would just SSH into VPS, and then run Herder inside that SSH session. It works, but you're limited to key bindings and clipboard of the remote machine. A much better way is to use Herder's remote mode from your local machine. We can do that by running Herder, passing in the remote flag, followed by the server. This acts as a thin client, for example, connecting over SSH to the Herder server running on VPS. This

**[13:49]** streams the full interface back to your local terminal. Everything looks exactly the same, but now you're using your local key bindings with better clipboard support. We can connect directly to a named session on the remote machine using the same command we ran before and passing in the session flag with the new session name at the end. This is powerful when it comes to working with things on the remote server. You get the full Herder experience while staying in your familiar local terminal environment. But of course, you need to have Herder installed on your server in

**[14:17]** order to do this. I also wanted to touch on a bit of custom commands that we can create. For example, I can hit prefix capital E to open a terminal file explorer. If you were using Yazi, it would work here as well. Or I can also run my script directly from within Herder by hitting a key map. Or I can just be in a Git repo and just use Herder to open up LazyGit. So, these custom key commands are for sure useful. Back in the config, we just have to look for a key.commands, and that's actually

**[14:43]** where I set it up. It's just setting a custom key map to run a custom command. But for commands, things like CD, LS, and echo won't work. It doesn't seem to run the command through the shell. It has to be an executable program in order to work. So, those are just some of the examples of what you can do with it, but it gets better because, believe it or not, there is already a plugin called HerderPlus that literally improves upon these custom key commands and adds even more feature to it as well. This plugin

**[15:11]** introduces two things: quick actions and projects. It puts all actions into one fuzzy list and projects into another. Let me show you how it works first, so you can decide if you need this. Starting with quick actions, the first thing we need to add are the key commands for this plugin. The key you set here is going to trigger the action fuzzy list. You can find this whole block in the documentation. Let's look at some examples. So, hit prefix Y to open that up. Right away, we can see that we are now in a quick actions list. This is where we can fuzzy search and

**[15:39]** select the actions. Like this script that is going to go ahead and update NVIM nightly. So, everything we set up with the custom commands can now be done here as well. And actually, it's kind of better because you don't actually need to waste any key bindings. You just hit one key bind and you get to run multiple actions. We can also nest selections into another selection. This can be handy if you have some configurations that you open quite often. You can put them all in this fuzzy list and just go to them.

**[16:05]** For projects, we're also doing the same thing. For this, I set the keys to prefix capital O. Hitting prefix capital O will now open up the projects list. This will also allow us to jump to a different directory, but it also creates a new workspace for that directory as well. If you're coming from tmux, this might actually feel very familiar. Also, when we navigate through these projects, you'll also notice something at the bottom here. It's actually showing the different workspaces that has different tabs in it. So, let's jump into this

**[16:33]** workspace and notice something here. We have four tabs set up, each with different names, and then agent is also being detected here. If I go to the third tab, grok is already up and running, ready to go. While the fourth one has lazygit that can't open because it's not a git repository. So, basically, you can set up all workspaces with the tabs you want, with the panes you want, the directory you want to have open for that workspaces, or the agents that you want to have running right away. This is definitely a

**[17:01]** step up from workspaces with everything that it has to offer, and now this. If this is not goated, I don't know what it is. Now, to install and actually set this plugin up, make sure that you're on the latest version 7.0. Installing a plugin is as easy as running a command. You can then run her to plugin list to check if the plugin is successfully installed. The path to the plug in directory should be located in the Herder root directory. You can use this command to check for the path. From here, you want to create two

**[17:27]** directories, which is quick actions and projects. That is if you don't already have one. Then from the root of your Herder config, go to the plugins directory. That should already be there once you install the plugin. Then go into config, and you should see the plugin right here. And that is where the projects and quick actions is going to be at. So, for all the quick action stuff, you can see that each tunnel file represents each action. That is the same for projects as well. For example, in the update nightly one, we basically

**[17:55]** just have a name, a description, and the command that is actually coming from the scripts. So, that's how you would add your scripts into this. Let's actually go take a look at listing main configs. So, we have the name, the description, the command, but the thing that stands out is the type. So, that's how you would do the nested selection. And for the command here, it's the same thing. It has to be an executable program. So, we're going to run Nvim home/ . And that value is the path value we have in these options. So, those were the quick actions. Now, inside projects, it's also

**[18:24]** the same case here. Each tunnel file for each workspace, like the one in zshrc, we can see that we have the name, the description, the working directory, and then two tabs set up in here that runs Nvim command and jump into the file directly. Here is another one from projects.toml. It has about three tabs inside, but the third tab has panes set up exactly how I want it. The first pane run codex, and the second one just split to a shell. So, yeah, you can see the multiple configurations and workspaces that you

**[18:52]** can create with this. Also, if you use a lot of git worktrees, they actually have integrations with git worktree as well, which is just crazy to me. Honestly, I do think that this plugin really makes everything just serves as come together and work better. I can totally just replace this with my tmux now and feel just fine. But, I would really like to be able to customize more of my tabs. It's just kind of hard locked at just looking like this, but that's okay though. I'm curious to know what you guys think about this. I'll leave the whole config down below. If you guys

**[19:20]** enjoyed the video, don't forget to like and subscribe for more, and as always, I will see you guys in the next one. Peace.
