# t3 — Cloud Codes: OmniRoute + OpenCode is INSANE (Why I Dropped Claude Code) [AQm1ig0GrP4]


> Source: AQm1ig0GrP4.en-orig.vtt
> Converted: vtt-to-md.py
> Format: `[MM:SS] line` — verbatim auto-sub, deduplicated, word-timing tags stripped


**[00:02]** I dropped Claude Code last month, and not because it got worse. I dropped it because two open source tools now give me the same thing for almost nothing. The coding agent is called Open Code. The gateway that feeds it models is Omni route. Open Code lives in your terminal, drives your whole code base, and talks to any model you point it at. Omni route is a free router, one endpoint on your machine, over 200 providers sitting behind it. 50 of those providers ship a free tier. 11 of them stay free forever.

**[00:30]** Wire the two together, and you get a setup that behaves a lot like Claude Code. Open source, model agnostic, and it does not freeze when a 5-hour window runs dry. Let me be fair up front. Claude Code is one of the best coding tools anyone has shipped. And Opus is still what I reach for on the hardest problems. So, this is not a takedown. This is about the tool you actually open every single day, and why the one I open finally changed. So, here is the entire build, start to finish, plus the honest

**[00:57]** places where it still falls short of Claude Code. Start with the part people skip past. Claude Code is proprietary. You can read the docs, but not the source, and you cannot swap the brain inside it. It runs Anthropic models, and only Anthropic models. That is a lovely experience, right up until the afternoon you want a different model, and simply cannot have one. Then there is the meter. The pro plan is $20 a month. Max runs 100 to 200. Your usage refills on a rolling 5-hour

**[01:25]** window, with a weekly cap stacked on top of it. Hit the ceiling in the middle of a task, and you either wait it out, or flip to pay as you go, and watch the tokens tick past your budget. Anthropic even doubled those limits back in May, which tells you how often people were slamming into them. Add it all up, and here is your position. A subscription tied to one lab, on a clock you do not control. For occasional work, that is completely fine. For someone in the terminal all day, the ceiling shows up

**[01:52]** sooner than you would like. What I actually wanted was small. The exact same tight loop of read, edit, run, and fix, but with my own choice of model, my own keys, and no 5-hour timer tapping me on the shoulder halfway through an afternoon of real work. It turns out that precise wish is what these two open tools were built to grant. So, let me walk through each half on its own and then snap the two together into one workflow. Open Code is the agent half of the setup. It is an open-source coding

**[02:20]** agent that runs in your terminal, built by the SST team, the group now called Anomaly. It reads your repository, writes a plan, edits files, runs commands, and then checks its own work against whatever the commands print back. The adoption is hard to argue with. It has passed 160,000 stars on GitHub, which makes it the most starred open coding agent in the world, with roughly 7 and 1/2 million developers touching it every single month. That is not a niche experiment.

**[02:48]** That is a movement. Here is the line that matters most to me. It is model agnostic. 75-plus providers work straight out of the box, Anthropic, OpenAI, Google, Grok, OpenRouter, or a local model served through Ollama. You decide which brain sits behind the loop, and you can change your mind per project. And it does far more than shuttle text to a model. Open Code wires in your language server, so after every edit, it hands the real compiler errors straight back to the model.

**[03:16]** The agent gets to see exactly what your editor sees, which is the difference between guessing and actually knowing the code compiles. It also stays put. Your sessions are written to a local SQLite file that stays on the machine, and every change is a Git-based snapshot you can undo. The whole thing is MIT-licensed, so you can read every line and fork it the moment you disagree with a decision. It even flexes to how you work. There is the full terminal interface for interactive sessions, a single-shot command you can drop

**[03:43]** straight into a CI pipeline, and a headless server that exposes the whole agent as an API for your own scripts. Same tool, three completely different shapes. So, the agent side is settled, open, local, and happy to run any model you like. But that freedom comes with a brand new chore. Suddenly, you are the one juggling a dozen different providers, a drawer full of API keys, and a stack of base URLs that all look slightly different. Cleaning up that exact chore is what OmniRout does. It is

**[04:11]** a free gateway you run yourself, one OpenAI compatible endpoint sitting on localhost, and behind it, every provider you own, stitched into a single tidy list of models the agent can pick from. The headline numbers are the whole pitch. 230-plus providers reachable through one address. More than 50 of them carry a free tier. 11 stay permanently free, no credit card at all. Open code only ever sees that one localhost endpoint, and OmniRout decides which provider actually answers.

**[04:39]** The feature that earns the name is fallback. If a provider goes down, gets rate limited, or just drags, OmniRout reroutes the very same request to a healthy one on its own, mid-request, without bothering you. It's tagline is, word for word, never stop coding. And that turns out to be the entire point of the thing. Then comes the compression, which is smarter than it sounds. Coding agents fire off enormous, repetitive tool payloads on every turn. OmniRout runs two passes it calls RTK

**[05:08]** and Caveman, and on that tool-heavy traffic, it strips out 78 to 95% of the tokens before they ever hit the model. And it is careful about exactly the part that scares you. Your code, your URLs, and your structured JSON come through byte for byte identical. It only squeezes the repetitive prose and the tool chatter, and it leaves the actual source you are shipping completely untouched. On privacy, it stays home. OmniRout runs entirely on your own hardware, your provider keys stay on the

**[05:36]** box, and it does not phone out to anyone. You get all of the routing and none of the usual price, which is handing some middleman a full copy of your traffic. It is not a thin little proxy, either. It ships around 94 built-in tools over the model context protocol, plus a semantic cache that remembers answers it has already computed. So, a repeated question can skip the model entirely and come back instantly for free. Underneath all of it are 18 routing strategies to pick from. Cheapest first, fastest first, weighted

**[06:05]** splits, hard cost caps, and a few more. You set the policy one time, then forget it, while the gateway makes the actual call on every single request. Now, watch the two click into one thing. Omni route has a button that generates an open code config for you. It spits out a ready file with every model it can reach already listed inside. You drop that file in place, point open code at the local host endpoint, and the agent is wired to the whole pool. With that, the loop closes. You type a task into open

**[06:33]** code. It plans the change, edits your files, runs the tests, reads whatever the compiler complained about, and fixes it. Then loops again until things pass. It is the exact same agentic rhythm that Claude code made the default. The whole difference lives underneath. The model doing the work is your pick, and the tokens are flowing through 50 free tiers with automatic fallbacks standing in front of them. That free pool alone documents around 1 and 1/2 billion tokens a month, which is far more headroom than a single developer

**[07:01]** can burn. So, the money math simply flips. The workflow that used to mean a subscription plus overage now leans on free capacity first and only reaches for a paid model when you genuinely want the heavy hitter for one hard problem. Most afternoons, honestly, I never reach for it at all. And that 5-hour window for me, it is simply gone. When one provider taps out, the next one picks the session up without a single pause. I work straight through the day, and nothing pops up to tell me to come back in a few

**[07:29]** hours once the meter cools off. Here is where the compression really pays off. On a long, tool-heavy session, cutting 78 to 95% of the tokens means the same work that used to burn clean through a paid budget now barely dents a free one. The savings and the fallback compound on each other. That was the afternoon I moved my daily driver over. The same loop, my own models, and no meter running in the corner of the screen. Claude Code is still installed on this laptop, sitting right there. I just find

**[07:57]** myself opening it far less often than I used to. Now, the honest half, because none of this is magic. Free tiers come with real strings attached. Slower models, tighter rate limits, and quality that swings depending on which provider you happen to land on. Fallback softens all of that. It does not make it disappear. You are also the integrator now. Claude Code simply works the second it installs, no wiring at all. This setup is two separate tools, a config file, and a handful of provider

**[08:24]** keys you have to manage yourself. More control, but genuinely more assembly required on your end. And on the truly hard problems, a top closed model like Opus still pulls clearly ahead of the free pack. If a single task is worth the best brain that money can rent, then just rent it for that task. That is simply not what most of my day actually looks like. So, the verdict is not that Claude Code lost. Keep it for the heavy, model-sensitive work where it genuinely shines. Reach for Open Code and Omni

**[08:51]** route for everything else, which, if your day looks anything like mine, ends up being most of it. The real shift under all of it is that the agent and the model finally came apart. The loop is open, the brain is now a choice you make, and the bill turned into something optional. Once you have felt what that is like, it is genuinely hard to go back. Open agent, any model, almost no bill. That
