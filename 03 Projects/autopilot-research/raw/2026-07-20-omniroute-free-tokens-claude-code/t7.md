# t7 — NetworkCoder: Claude Desktop Without Anthropic (AI Gateway Basics) [4DlNb2weD_s]


> Source: 4DlNb2weD_s.en.vtt
> Converted: vtt-to-md.py
> Format: `[MM:SS] line` — verbatim auto-sub, deduplicated, word-timing tags stripped


**[00:02]** This is Slot Desktop.  The Full Ep.  Co-work tab, running background agents.  Code tab with visual dips and gate integration. Schedule tasks firing automatically. Live artifacts refresh every time you open them.  And if you look at the bottom right corner, it says Anthropic Opus 4.8. Everything looks completely normal, right? Now Let Me Flip Over to Something Called by Frost.  This is an A gateway running on my machine.  And I want you to look at one thing. The cost column is zero.  Every single request

**[00:31]** that clout desktop just made route it through by frost hit a free model came back total cost across everything $ and this is not some temporary hack.  This is not a free trial that expires in seven days.  This is an open source gateway that you can set up right now on your own machine and it works.  So in this video I'm going to walk you through the complete setup step by step.  We're going to cover two approaches.  The first is the cloud method.  We take CLOT desktop, route it through Bifrost, and

**[00:58]** connect it to free information on Open Router. Models like Nvideo Nemtron at 120 billion parameters.  Free tier, no API costs, nothing.  The second is the local method. Same Cloud Desktop.  But instead of cloud information, we run everything on your own machine using LM Studio.  Download a Model, Serve It Locally.  Your data never leaves your computer.  Completely private, completely offline, and obviously free.  No anthropogenic account needed, no bills.  You get the full clout desktop experience.  Co-work, code

**[01:25]** tabs, scheduled agents, live artifacts for $ a month.  And I'm going to show you exactly how.  Let's get Okay, so before we get into the setup, I need to make sure you actually understand what we're working with here.  So cloud has two main products for power users, right?  You have cloud code C LI.  That's the terminal version.  You install it, you type cloud in your terminal and you are off. Works on Windows, Mac, Linux Full

**[01:53]** agentic coding.  Edits your files directly, runs git commits code, the whole thing.  Then you have clout desktop.  And this is a completely different beast, right?  This is a native GUI app.  And when you open it , you have two tabs.  Co-work which runs background agents while you are doing other things.  Parallel tasks, multiple sessions at once, it can access your files, your calendar, your apps.  And then you have code, which is a full visual coding environment with diff views, gate work trees.  Shows you

**[02:21]** exactly what's changing in your files visually.  So, Cloud Desktop is genuinely powerful.  But here is the problem.  And this is the reason you are watching this video.  Cloud Desktop by Default Only Talk to Anthropic.  That's it.  Hit Your Rate Limit Stuck.  Subscription expires. Stuck.  Want to use a different model.  Tough luck. Now Cloud Code CLI Was Always a Bit Different Here.  And by the way, I've covered how to connect the CLI to local models and cloud alternatives in previous

**[02:48]** videos.  Links in the description if you want that but desktop that beautiful cowork tab that code tab with the visual dips locked completely locked to anthropic only until they added something anthropic quietly built in a feature called third party inference it's hidden behind developer mode inside the app most people have no idea it exists now here is the thing this feature wasn't built for what we are going to do today right anthropic does this for

**[03:15]** enterprises big companies running cloud on amazon bedrock google vertex microsoft foundry their own private cloud infrastructure.  That's the intended use case.  But here is what we figure out. This feature under the hood just points Cloud Desktop at a different API endpoint.   That's it.  It doesn't care what's on the other end as long as it speaks the right protocol.  So what if we put something in the middle.  A gateway that sits between the cloud desktop and the actual model.  Cloud Desktop Thinks It's Talking to an

**[03:43]** Enterprise Information Provider.  But we route it silently to a completely free model via an open router.  And that's exactly the hack we built.  It's not straight forward.  There are moving parts. There are things that break.  And I'm going to walk you through every single one of them.  But When It Works, You Open Klout Desktop, Co-Work Tab, Code Tab, Full Experience. So before we jump into the setup, let me just

**[04:11]** quickly explain what Bifrost actually is.  Because this is really the key piece that makes everything work.  So BiFirst is an AI gateway.  Now if you've heard that term before, just think of it like a middle man, right?  Instead of your app talking directly to a provider like Open AI or Anthropic, you basically put Byfrost in between.  So, every request goes through Bifrost first.

**[04:39]** And then Bifrost decides where to send it. Now why would you want a middle man? Because it just gives you control.  You can route requests to different providers. You can set up rules.  You can monitor everything.  You can basically switch models without changing a single line of code in your app.  And in our case, we're going to use it to intercept CLUT desktop requests and just redirect them to free models.

**[05:08]** Now there are other gateways out there.  Right? LiteLLM is probably the most well known one but BiFrost is different it's built in Go not Python and that actually matters because it's really fast we're talking like 50 times faster than LiteLLM according to their benchmarks now here is the best part two BiFrost is completely free and open source it has a Linux 2.0 license you can check the source code on

**[05:37]** GitHub you can download you can just run it locally.  So everything we are doing today costs nothing. Now they do have an enterprise tier for big companies that need clustering, Aptiv load balancing, compliance, governance, all that stuff.  But for what we're doing, the free open source version is honestly more than enough. And by the way, Byfros supports over 23 providers out of the box.  Open AI,

**[06:07]** Anthropic, Mistral, Grok, Olama I mean the list just goes on. So just to summarize, ByFirst is our gateway.  It sits between the desktop and the actual model.  It intercepts, it routes, it logs everything, and or it's free. All right.  So now let me show you exactly how this works.  I have basically drawn this out.  So it's crystal clear.  On the left

**[06:37]** side this is what clout desktop thinks is happening right you have the app cowork tab code tab the full experience and when you type something it just sends a request for clud opus 4.8 it thinks it's going to anthropic that's basically the normal flow clud desktop talks to API anthropic and you pay for it but we're crossing that out we're just not going there so

**[07:06]** instead look at the middle this is basically the hack right we put bifrost gateway in between it's just running on your local machine and inside bifrost we set up a routing rule very simple if the model name contains the word clud intercept it doesn't send it to anthropic so where does it go ok look at the right side this is what actually happens byfrost

**[07:34]** just silently reroutes that request to open router.  Now Open Router is a free tier API that gives you access to like dozens of models and we're pointing it at Nvidia Nemotron 120b a 20 billion parameter model which is basically a free model with a rate limit and the response comes back through the same chain right Nemotron response Open Router passes it back

**[08:02]** byfrost hands it to clout desktop and clout desktop just displays it like nothing happened it still says Anthropic Opus 4.8 in the bottom right corner it has absolutely no idea. So ya the flow is pretty simple just four steps.  Cloud sends request first Bifrost intercepts this and then routing rule fires.  Finally, the free model responds to our prep and interestingly, the

**[08:33]** cloud has no idea about this.  So that's the flow.  Great.  Now We Know How It Works and Alright so let's set this up from scratch.  The first thing we need is a cloud desktop.  So head over to the download page shown on screen.  You will see the download for Windows right here.  Let's run the installer. And there it is, Cloud for Windows.  The

**[09:00]** Fastest Way to Talk a Cloud.  Now don't click Get Started Yet, and we are not signing in as well.  Ok now let's jump into the Byfros Gibe page and if you scroll down to Quick Start you will see two options Docker and NPx and we are going with NPx because it's simpler on Windows no Docker needed.  So just open up a terminal and run the command shown on the screen.   That's it.  Just one command.  Now watch The Terminal.  You will see that Bifrost Eski

**[09:28]** art, version details, The Fastest LLM Gateway, et cetera.  And if you scroll down through the logs you can see it initializing config store logs MCP Manager are all initialized and or successfully started and serving UI on local host port 880 so let's open our browser and go to local host 8080 and this is the byfros dashboard clean empty zero requests

**[09:57]** zero cost basically a fresh slate now if you look at the left side bar we've got observability with dashboard alllm logs MCP logs you've got models with model catalog model providers routing rules we're going to use all of these through this video alright now let's add our first provider so go to model providers tab you will see this empty screen saying add a provider to start routing requests so click add provider and look at this

**[10:26]** list and there's pick Azure AWS bedrock et cetera I mean over 23 providers supported out  Out of the box.  We're going with Open Router today.  So just click it.  And now it says configured keys.  No key is found.  So we need an API.  So open a new tab.  Go to open router.ai.  Sign up for free if you don't have an account. Once you are in the Go to Your API Key section, click Create Key.  Give it a name.  Leave

**[10:55]** Credit Limit Blank for Unlimited.  No Expiration And Just Hit Create And Copy That Key. Now before we go back to Bifrost let me show you something important. So in open router click on models at the top and in the search bar just type colon free and look at this.  Right all these free models available.  Nvidia Nimoton 3 Super sitting right at the top.  120 billion parameters free with rate limit.  We also got

**[11:26]** Open AI GPT OSS 120b GLM 4.5 Air and you know dozens of other free models.  So click on Nemotron you can see the full details here It is an open hybrid mixture of experts model through put 15 tokens per second with 1 million token context window also written All props and outputs are locked to improve the providers model.  So don't upload anything personal and confidential.

**[11:54]** OK now go back to by frost and paste your open router API key give it a name leave allowed models as all models and save and now you can see the key is configured green check mark so or we are connected to open router now here is the important part back to clout desktop remember we are not signing in so look at the menu bar at the top go to help then trouble shooting and here you will find enabled developer mode this is the key

**[12:24]** step.  So, go ahead and enable it and it takes a few seconds to refresh.  Alright now you will find a new Developer option inside the menu bar.  Click Developer then Configure Third Party Interface and a new window opens up.  And this is basically where everything connects.  So, let's configure it one by one.  Connection type is already set to Gateway.  And if you click that dropdown, you can see all the options. Gateway, Anthropic API, Bed Rock

**[12:52]** et cetera.  Now these are the enterprise options anthropoic build this feature for.  You know companies are running cloud on their own cloud infrastructure but we are using gateway to point at our own Bifrost instance.  OK Credential Kind.  Click the dropdown select Static API Key.  Now Gateway Base URL.  This is critical.  So type it exactly as shown on screen.  That's basically Byfros running locally on port 880 with the anthropomorphic end point.  This

**[13:21]** specific path is what Cloud Desktop expects.  Gateway API's Just Type Anything Here. A dummy value is fine. Bifrost handles the actual authentication with Open Router on its end.  Gateway Auth Scheme. Just leave it as is.  Now scroll down. You will see a section called Models.  Model discovery is turned on by default.  So Click Test Model Discovery and Just Watch This. Model Discovery Found 18 models.

**[13:50]** Anthropic Claude 3 hikes Claude 3.5 all the way up to Opus 4.8 and Opus 4.8 fast.  The full Anthropic model line up.  All Discovered Through Our Bifros Gateway.  Beautiful right. Ok.  Now click Apply Changes.  The CLOT And here is what happens.  You will see an error banner gateway returns an error.  This is

**[14:18]** completely expected.  Just look at the details.  It ses probed model anthropoc clod 3 hiec.  Response body Failed to getConfig for provider not found.  So what's happening here is Cloud Desktop sends a test request to verify the connection works.  It sends it using cloud 3 hike as the model.  ByFirst receives it but we haven't created a routing rule yet.  So BiFirst doesn't know where to send this request and it fails.

**[14:46]** This is exactly what we need to fix next.  The routing rule is basically what connects everything together.  So let's go to Bifrost.  Click on the Models tab.  Then routing rules.  Empty No rules configured.  Click New Rule.  Rule name. Type Opus to Free Nemotron.  Add a description if you want to enable the rule Make sure it 's on scope Set to global priority zero That's the highest

**[15:15]** priority This rule fires before anything else Now this next part is important to understand This is the actual routing logic So scroll down to the Rule Builder section and click Add Rule First condition Set it up exactly like this This field is Model Operator is Contains Value is cloud So any request where the model name contains the word

**[15:44]** cloud gets copied Then click the And button and add another rule Second condition Field is Model Operator is Equals And for the value select Anthropic cloud Oppers 4.8 from the dropdown Don't type it manually Select it from the dropdown So there are no mistakes.  Now look at the CEL expression preview at the bottom.  It shows model dot contain clod and model equals anthropoic clod opus 4.8.  So basically any request that mentions cloud anywhere in the model name

**[16:22]** Now scroll down to Routing Targets. This is where we tell Byfrost where to actually send these intercepted requests.  Click Add Target Provider Select Open Router.  The One We Just Configured model we're going with is the Nvidia Nemotron.  You can go with your choice here.  I found this 120 billion parameter model available on the free tier. Now make sure you include colon free at the end of the model name.  That's important. Without it you will hit the paid version.  Weight

**[16:50]** set to one.  API's.  Select the key you configured earlier.  And that's the full rule configuration. Let's save it.  Ok.  Now go back to Clout Desktop.  Click Check Again on that error banner and this time no error.  Clean Co-Work tab on the left.  Code tab next to it.  Gateway showing at the bottom left corner and Anthropic Opus 4.8 in the bottom right.  And if you click that model drop down you can see all the cloud models discovered.  Hyko Sonet

**[17:19]** Every Opus Version Fast Variants.  All listed.  And all of them get intercepted by our routing rules.  Doesn't matter which one you pick from this list?  They all route to the Let's quickly verify and by first click on LLM logs 100% success rate Six requests came in as part of the model discovery and connection test You can see the latency, the token usage, and the total cost at zero Setup is complete Now let me

**[17:48]** show you what this can actually do OK First let's check if routing is working Open the CLOUT desktop code tab and ask "Hello" What model are you and look at that I am CLOUT code powered by Anthropics Opus 4.8 model It fully believes it, but it's not that Nemotron Running through Open Router Routing confirmed Now let me show you the features working Cowork tab One prep I'm actively job hunting Create a live tracker for three roles

**[18:16]** Schedule a weekly follow up Every Monday at 9:00 am It runs four commands autonomously Created a scheduled task firing Every Monday Created a live artifact SN Interactive dashboard Build Persistent files on disk All from one prep, scheduled tasks, live artifacts, autonomous agents, all running on the Nemoton free tier.  Now I did run multiple use cases offline to properly test the features, the quality and the response times.  And the results were honestly pretty good.  But there is one thing we need to

**[18:44]** understand here.  What we're really getting is the Klout Desktops front end.  The Co-Work tab, Code tab, Schedule Tasks, Live Artifacts.  That's Anthropic's Feature Set, And It Works Regardless Of What Model Seats Behind It.  The actual intelligence two, the reasoning, the code quality that always depends on the brain you plug in.  Right now that's Nemotron.  Solid Model 120 Billion Parameters But It's Not Cloud Opus.  So basically, the cockpit is the same.  All the buttons

**[19:13]** work.  The Engine Is Just Different.  Just keep that in mind as we continue.  Great.  Now let Ok.  So let's flip to the ByFirst console dashboard. First overview tab. Total requests 84. Token usage $34000. Tokens and cost $. Just one model handled everything.

**[19:43]** Nemotron through Open Router. Click Provider usage.  You Can See the Traffic Pattern That Spikes Right There Is Our Job Hunting Demo.  387,000 tokens processed all through the free tier.  Now LLM Logs and This Is Where It Gets Really Interesting. So filter by success and you can see every request listed.  The Message Column Shows Exactly What Was St.  Hello, what model are you?  I am actively job hunting.

**[20:13]** Create a live tracker.  I mean everything is visible and treble.  So, click into that first request.  You can see the full details.  Rule that fired opus to free nemotron model nemotron free tier 43000 tokens in $ cost and get this 71 tools were exposed to the model the full clout code tool set bash execution scheduling file editing search all of it.  So that's basically why

**[20:43]** the Cowork demo could create files and schedule tasks.  Right?  Now click the Routing tab.  This is the one.  You can see exactly how the rule is evaluated.   The request came in for Cloud Opus 4.8 from provider Anthropic.  Bifrost Evaluated One Rule Opus to Free Nemotron.  Condition matched.  Selected target open router Nemotron free tier and just milli seconds.  That's the entire

**[21:12]** interception happening in real time.  Now the Messages tab shows something worth noting. Cloud Desktop is a 229 line system with nearly 7000 tokens. Basically, telling the model you are using Cloud Code Anthropics official CLI. That's the identity package baked into every single request and Nemtron just receives that and works with it.  So, Byfros gives you full observability: every

**[21:39]** request, every routing decision, every token counted.  Whether you are running free models and paid ones, you see exactly what is happening.  And that's really the Alright now let me show you the second approach running everything locally.  So Everything We Have Done So Far.  Open Router, Bifrost, Routing Rules.  That's the cloud approach.  But what if you want like no dependency on

**[22:09]** anything.  No rate limits, no daily caps, just your machine.  So let me show you how to run this fully local.  For This We Need LM Studio.  Now LM Studio is basically a free desktop app that lets you download and run open source AI models directly on your computer.  Nothing Leaves Your Machine.  So open up a terminal.  I'm in VS Code here.  And just run the win get install command shown on the screen.  You might see it already installed.  That's fine.  As long as

**[22:37]** you can verify it with the LMS version. Good CLI is working now start the local server server running on port 1 2 3 4 so now we have a local inference server ready but we don't have a model loaded yet so let me show you what's available if you just run LMS you get this interactive model browser I mean 100s of models quen llamas gemai mistral all open source all

**[23:05]** downloadable now I want a Google model specifically so I just filter it with LMS get Google and now I can see all the Google models.  So I'm going with JMA 4E4B 4 billion parameter model small efficient good for agentic tasks.  Only about six gigs pretty manageable on most machines.  Mines already downloaded.  But if you are doing this for the first time it will take a few minutes depending on your internet.  Now here is the important part

**[23:33]** two.  CLOT Desktop Will Only Discover Models That Have the Word Opus and Sonnet and Haiku in the Name.  It's basically looking for anthropic model names.  So we need to alias it and watch this.  Just run the load command with the context length and identifier set to Cloud Opus 4.8 exactly as shown on screen.  It Loaded Successfully 6.33 Gigs. And if you run LMS PS you can see our model running.  Identifier Cloud Opus 4.8

**[24:04]** Context 8000 But it's actually just Gema running on your local machine Alright quick sanity check hit the API directly with a curl command press Y to confirm on Windows and there it is JSOA response showing Cloud Opus 4.8 is available.  So let's test it with a quick chat before we connect anything.  It thinks through the answer.  You can see the thinking tokens and it responds that's gema running locally right okay now

**[24:32]** let's connect clout desktop developer menu configure third party inference same window we used before for byfirst but this time we just point it at lm studio connection type gateway credential kind static api key gateway base url local host port 1 2 3 4 api key just type lm studio it doesn't need real authentication but the field requires something. Now scroll down to

**[25:07]** and Model Discovery Found One Model.  Cloud Opus 4.8 That's basically our local Gema pretending to be Opus and or Cloud Desktop found it.  Click Apply, Save Changes and Restart.  Cloud Desktop relaunches.  Welcome to Claude.  No account needed.  Just continue without signing in. Bottom right shows Anthropic Cloud Opus 4.8 Now let's run a quick test Just type something in and flip over to LM Studio Open the Developer tab and look at the logs

**[25:36]** Requests coming in live from Opus 4.8 Model processing Token streaming and your local machine doing all the work.  Now give me something important here. Look at the logs carefully.  Token length 36372 but we set our context to 8000.  So what's happening is CLOUT desktop is sending its system prep lines nearly 7000 tokens just for the system prep plus tool definitions plus your message it all adds up to over 36000 tokens

**[26:06]** and our context window is only 8000 so or the model will truncate and struggle so we just unload it and reload with a bigger context window set context length to 40 and here is the honest part my system is CPU only so 40 40,000 contexts on a 4 billion parameter model on the CPU is going to eat a significant chunk of memory.  It works but it's noticeably slow and that's the real trade off with local models, right?  Complete Privacy

**[26:33]** No Rate Limits.  But You Are Limited By Your Hardware.  If you have a GPU, even a modest one, the experience changes significantly.  Fast inference, big context windows, no system crawling. So now you have both approaches setup.  Cloud via Open Router and Bifrost Local via LM Studio Same CLOT Desktop Same Co -Work Tab Same Code Tab You can literally switch between them in 30 seconds.  Develop Menu Configure Third Party Inference Just

**[27:04]** change the gateway URL.  Port 1234 for local port 8080 with the anthropoic path for Bifrost.  That's it.  And if you want the best of both, just route everything through Bifrost.  Add LM Studio as a custom provider along side open router.  Set up routing rules so different tasks go to different models.  By Frost basically handles the routing and gives you observability on both.  So or that's it. Complete guide covering both the approaches. Cloud and local both giving you the cloud

**[27:31]** desktop feature set.  If You Found This Useful Hit That Like Button.  It Really Helps The Video Reach More People Looking For This Kind Of Setup.  And if you're new here, I make videos on AI coding, local models, open source tools.  Subscribe and turn on notifications so you don't miss the next one.  Links to BiFirst Open Router LM Studio All in the description Drop a comment if you have questions See you
