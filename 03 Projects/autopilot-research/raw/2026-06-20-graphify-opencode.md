<!-- compiled: 2026-06-20 -->
# RAW — OpenCode + Graphify: Stop Wasting Tokens (AI Stack Engineer)

> **Path:** 5 yt-dlp-only (operator-submitted single video)
> **Video:** https://www.youtube.com/watch?v=-L_faOE-H5g
> **Title:** OpenCode + Graphify: Stop Wasting Tokens in Opencode, Every Developer Use this
> **Channel:** AI Stack Engineer | **Uploaded:** 2026-04-21 | **Duration:** 10:11 | **Views (at fetch):** 46,677
> **Captured:** 2026-06-20 via `yt-dlp --write-auto-sub --sub-lang en` → VTT → dedup (ffmpeg absent; VTT parsed directly)
> **Transcript words:** 1,753 | **Note:** the '71.5x' headline number is NOT spoken in this video — it comes from derived blogs; verify separately.

---

## Cleaned transcript (verbatim, deduped)

Let's say you're working on a project in Open Code. You have just opened a new session and you want to understand the code base before making any changes. So, you ask a simple question, what does this payment module do? Give me a quick summary of each file. Pretty basic question, and now watch what happens. Before it answers anything, it opens the readme to figure out what kind of project this even is. Then it reads a config file. Then it starts going through your source files one by one, building a mental picture from scratch. Reading file after file, and every single one of those reads burns tokens. And the answer you eventually get is only as good as whatever Open Code managed to pick up along the way. Now, here's the part that actually hurts. If you close that session and come back tomorrow, Open Code starts from zero all over again. No memory of what it read, no idea how your files connect, no clue which modules are the important ones. Every new session, same thing. Same tokens burned, same files reread, same grepping around in the dark. So, there's a tool called Graphify that fixes this. It reads your project once, builds a knowledge graph of how everything connects, and then Open Code just reads that graph at the start of every session instead of scanning your files over and over. And the interesting thing is, this tool actually came out of Andrej Karpathy mentioned publicly. He's one of the co-founders of OpenAI, and these days a lot of developers follow him for his takes on AI and tooling. And the problem he described is exactly what Graphify solves. A pile of mixed content you can never fully reconstruct when you need it. The repo pretty much addresses that use case head on. Let me show you how it works and how to set it up in Open Code. The idea behind it is pretty simple. Think of a regular Open Code session as a contractor who walks in on day one with no context. They have to skim everything before they can answer even a basic architecture question. Graphify is more like handing them the internal wiki before they start. They already know which files are the core of the system, which modules talk to which, and where the important decisions live. You build that wiki once, and every future session gets it automatically. Before I continue, let me introduce Code Sniff, a security-first pull request code review platform. You open a pull request, and Code Sniff scans it through nine deep analysis steps to give you a complete review before you merge. Actually writing code these days is easier than ever thanks to AI. But with that speed comes a new kind of risk, security threats, hidden bugs, and code you don't fully understand shipping straight to production. I connect my GitHub account, grant Code Sniff access to my repo, and with just one click I generate a full analysis of my project. I instantly get a clear summary of my code base, my tech stack, and the complexity hotspots I need to watch. Then the dependency graph gives me a visual map of how everything connects, so I can spot risky areas in seconds. When a pull request is created, Code Sniff automatically analyzes it and hands me a full review, an overview, the risk level, merge advice, and clear warnings about any breaking changes. I can dive into a complete walk-through, see the highlights, the concerns, and detailed testing notes. Code Sniff runs every PR against industry-standard testing pipelines, helping you catch issues before they reach production. So, if you're serious about shipping safer code, and you want to stop merging code you don't fully understand, give it a try. Link in the description below. And this doesn't only work for code. You can point it at a folder of research papers, PDFs, meeting notes, screenshots, even video files. It handles all of that in the same graph. But let's stick to a code project for the demo. Under the hood, Graphify runs in three passes, and it's worth understanding them because it tells you where your money actually goes. The first pass parses your code using tree-sitter. It supports around 23 languages including Python, JavaScript, TypeScript, Go, Rust, Java, C, C++, Ruby, Swift, Kotlin, and a bunch of others. This pass is fully local. It reads every class, every function, every import, and every call, and it costs you nothing in API tokens because no model is called. It's just static analysis. The second pass handles audio and video. If you have meeting recordings, tutorial videos, or YouTube links in your corpus, Graphify transcribes them locally using Faster Whisper. Again, no API tokens, no network calls for the audio. Everything stays on your machine. The third pass is the one that actually costs money. For markdown files, PDFs, images, readmes, and docs, it fires off parallel sub-agents through your model provider to pull out concepts and relationships. This is the only pass that hits the API, and it only runs once per corpus. After that, the result is cached. When you change files later and run an update, it only reprocesses what actually changed, so you're not paying for the same file twice. All right, let's get into the install part. Before you do anything, check your Python version. Graphify needs Python 3.10 or newer. If you're on an older version, update it first because the install will either fail or you'll hit weird errors later. Once Python is sorted, open your project folder in a terminal. The install is two commands. First one is pip install graphify. And just so you don't trip on this, the package name ends in double y. That's graphify y, not graphify. Also, if you're on WSL or Linux, you might want to use a virtual environment because newer Python setups block global pip installs. After the package installs, run graphify install. By default, this sets up the skill for Claude Code. Since we want Open Code specifically, we need to run graphify install {dash} {dash} platform Open Code. That copies the skill file into the right place for Open Code to pick it up. Now, the next step is the one that actually makes Graphify always on. You run graphify open code install. This does two things under the hood for Open Code. It writes a section into agents.md in your project root, which tells the assistant to check the knowledge graph before doing anything else. And it installs a plugin file at {dot} opencodeslashplugins/graphify.js, which hooks into Open Code's tool execute before plugin system. What that plugin does is fire right before any bash tool call and inject a reminder into the output saying, "Hey, the knowledge graph exists. Read the graph report first before grepping around." It registers itself in open code.json, so you don't need to wire anything manually. This is the piece that makes it actually useful day-to-day. Without the plugin, you'd have to remember to tell Open Code about the graph every single session. With it installed, every new conversation just has the graph context available without you typing anything. Now, you're ready to actually build the graph. Open Open Code in your project and type {slash} graphify followed by a dot. The dot just means current directory. You can also point it at a specific folder like {slash} graphify.slash.src if you only want to graph a subfolder. If your project has a ton of files, you can also add a {dot} graphifyignore file at the root. Same syntax as {dot} gitignore, just put node_modules, vendor, dist, whatever you want skipped, and Graphify won't include those in the graph. run takes a while. How long depends on your corpus size. For a medium project with a few hundred files, expect something like 5 to 10 minutes. It'll show you progress as it goes. When it's done, you get a folder called graphify-out with four things inside. A graph.html file you can open in your browser, a graphreport.md which is the summary, a graph.json which is the full machine-readable graph, and a cache folder. If you open graph.html, you'll see something like a star map, a bunch of colored dots with lines between them. Each dot is a concept, a function, a class, a document, an image, whatever was in your corpus. The bigger dots are what Graphify calls god nodes. Those are the pieces that everything else connects to. If 40 files in your code base touch a specific module, that module becomes a big node. On the side panel, you can click any node and it shows you which files that node lives in. You can also filter by community, so if you only want to see the authentication stuff, just uncheck the others. Keeping the graph in sync matters, too, because if you build it once and never update it, it goes stale fast. Two options. Run graphify update manually whenever you feel like it, and it'll only reprocess files that have actually changed. Or run graphify hook install, which wires up Git hooks. After that, the graph rebuilds automatically on every commit and every branch switch. I'd recommend the Git hook approach because you'll forget otherwise. One thing I want to call out honestly, if your project is small, like under 10 files, don't bother. The graph value is structural for small stuff, not token savings, and Open Code can already handle that scale without help. Where Graphify earns its keep is on projects with lots of mixed content. A code base with a docs folder full of markdown, a research vault, a folder of PDFs and meeting recordings. That's where the compounding starts to show up. You can also query the graph directly from the terminal without even opening Open Code. Commands like graphify query followed by a question in quotes will traverse the graph and give you an answer with source file references. Graphify path lets you find the shortest connection between two concepts. Graphify explain gives you a plain language summary of any node. These are useful when you just want a quick answer without spinning up a full session. All right, so that's it from the video and I hope you enjoyed it. If you did, please like this video and subscribe to the channel and I'll see you in the next video.