# Infisical - Vercel Just Dropped EVE. Everything It Is


> Source: 1iXnZzCR2JY.en.vtt
> Converted: vtt-to-md.py
> Format: `[MM:SS] line` — verbatim auto-sub, deduplicated, word-timing tags stripped


**[00:01]** Vercel just dropped a new agent framework called Eve, and it's one of the cleanest ways that you can build agents. Think Next.js, but for AI agents. Your whole agent is a folder of files. In this video, I'll show you what it is and a real build an open-source agent for our community that reads our code and answers questions. I also wired it up so that it gets zero API keys, but still has access. More on that later. So, as mentioned, Eve is very graceful. It's like the Next.js for AI agents. Your agent is a folder of files, and the

**[00:28]** folder structure is just the whole mental model. I feel like every other agent SDK that I've built with has just been like very primitive, and it's been difficult to keep track of all these things. So, it's been a lot to build on top of. With Vercel's Eve, everything is super straightforward. A file's location is what it does. So, for example, instructions.md is going to be the agent's personality. agents.ts picks the model. Drop a file here into connections, and it's wired to an external server. So, you can see the repo itself is the architecture diagram.

**[00:56]** Now, Eve is model agnostic. We have one line in agents.ts, and this picks the model. So, we could have Sonnet here. We could have Opus there. You can swap providers, route through a gateway. Now, this is great, and it's standard for basically any agent that you're going to build. Now, here we have our agent skills. These are going to be markdown playbooks that the agent loads whenever they're relevant. So, I can hand my agent a real procedure, like how we triage a bug report, and I don't have to cram this into the context of every prompt. It'll pull this playbook the

**[01:23]** moment that it actually needs it. We also have tools. Tools are going to be the hands of the agent, the typed functions it can actually call. And most of the ones that you're going to reach for are actually already built in. Things like web search, read file, write file, fetching URLs, running shell commands. So, out of the box, your agent can look things up, and it can do things without you having to define these tools. And when I need to do something specific, I can add it as a TypeScript function using this define tool. And the model only ever sees the result, it never sees the code or our keys. And

**[01:49]** this is a really basic one I put together, get support links. It just hands back people our canonical docs and our real GitHub links, so that the the doesn't hallucinate anything. Next, we have connections, which is something that made me really happy as well. On the raw agent SDK, I had to handwrite every MCP tool. In Eve, this is really easy. It's just one file pointing it at a server, and then the agent gets this whole tool set discovered on demand. And so we'll see this with GitHub later. And next is something I've been looking for for a while, and it's something I'm really excited about. It is very easy to

**[02:17]** put together sub agents in Eve. So in Eve, your main agent stays really lean, and you can delegate stuff to these specialist agents that have their own prompt, their own tools, even their own sandbox. So if I look at sub agents, I've set up this code researcher. And this guy digs through our repos, and he has his own sandbox. And then there are channels, which is just the front door that people use to interact with their agent. Out of the box, you get this plain HTTP endpoint that you can hit from curl or from the back end. There's a web chat UI as well that you can drop straight into a Next.js site. And then

**[02:44]** there are platform channels like Slack and Discord. Your agent will answer to mentions and DMs, and you know, you can interact with it from where you're usually sending messages. Now you just define your agent once, its personality, its tools, its schedules, its sub agents. And a channel is just a file that exposes it on a new surface. The agent doesn't care whether a question comes from the terminal or a Slack thread, it's going to answer the same way. And then there's the sandbox. Our agents don't just chat, it's going to run real commands, things like cloning a

**[03:11]** repo or grepping through code. Eve doesn't run those on your machine or inside your app. It spins up an isolated sandbox, basically a throwaway computer or file system, and then it's going to run commands in there. So the agent can actually get its hands dirty, and it can do real work without you ever giving it execution in your environment. I'll show you exactly what this looks like in the demo. And there's also human in the loop built in. So any tool can require human approval before it runs. This way for anything risky or expensive, your agent can actually pause and wait for approval

**[03:38]** before it goes. And there are also schedules. Drop a file in with a cron expression, and your agent is going to run this itself on some cadence. A daily report, a nightly sync, a cleanup job, and nobody has to prompt it. And these are for your long-running agents. And then there are evals, scored test suites for your agent that run on every deploy. So, when you tweak a prompt and you quietly break something, you can catch this before it actually ships. And then lastly, this is durable. So, a conversation is going to be a real session that survives crashes and

**[04:06]** restarts. You don't write any of that. And then when you're ready, it's just a normal Vercel project. If you're going to deploy on Vercel and you have the Vercel CLI, you could just Vercel deploy and you get the sandbox, you get durable workflows, you get cron schedules for free. So, that's the shape of the Eve agent. It's clean, it's model agnostic. It's organized as a file structure that's intuitive and has all these things included. Let's build one and run it. Now, quickly before we actually demo Eve, to do real work, an agent needs various keys and tokens. It needs a

**[04:33]** model key, it needs a GitHub token. Normally, those are going to sit in your .env file and the agent process reads them. Now, today, instead of having plain text secrets our agent can read and our agent can potentially leak, I'm going to stand up this open source credential broker called agent vault. This way, our agent's .env file is going to be all dummy values. The real keys are going to live in this vault, they get injected on the wire on the way out, and our agent never actually holds a key. Now, this is a paradigm that the industry is converging on in order to keep API keys and sensitive tokens out

**[05:00]** of our agent's hands so that they can never accidentally leak them through something like prompt injection. So, I already have my agent vault instance running. If you want to know how to set up agent vault, it's really quick. Uh you can find it in their GitHub with this installation command. I'll also leave a link to a video where I show how to set it up in the description. So, I'm in the directory where my agent lives and I can start my agent with this one command. The Eve command itself is going to be Eve dev. We wrap it in the agent vault CLI that's going to route it through agent vault. And then our agent

**[05:27]** comes up on localhost so we can talk to it. Let's test this out. What does the Infracl CLI do in one line? Okay, Infracl lets you inject secrets from Infracl into any application. So, our agent is working. Now, let's look at the parts that make Eve nice. So, here I tell it, "Use your GitHub tools to list the open issues in JakeHolberg2/demo-app." So, we can see Eve searches its connections, it finds this GitHub list issues, approve it. Now I never wrote a

**[05:55]** single GitHub tool, I just dropped one connection file that points at GitHub and the agent got the entire tool set and discovered what it needed. Now on the agent SDK I would have written each by hand. Here it's one file and the tools load on demand so they don't clog the context. And here I can see that it was able to access jchordberg2 demo app and list my issues. I can see that demo app is a private repository. Now the way that Eve was able to access this private repository is that I've put my GitHub token into agent vault and again it was

**[06:22]** routed through agent vault so that credential was swapped in. It doesn't actually have the token itself. Now let's check out the sub-agent piece. Where does the physical CLI read its off token from? Use the code researcher and cite the file and line. So I want to see the main agent hand the hard question to our code researcher specialist and then this code specialist already has our repos cloned in its own sandbox. Okay, so we see code researcher sub-agent. It should grep the real source and come back with the file and

**[06:49]** the line. And you can also choose which model each agent runs on. So my code specialist runs on a stronger model Opus 4 8 than our main agent which is using Sonnet. So I only pay for this big model on our hard problems. Okay, so our sub-agent has done its research and it's come back and it's reported exactly where along the line that off token comes from. And it's also kind of clever how Eve picks which sandbox to use. It picks the best one it can in order. So in Vercel it's hardware isolated micro

**[07:17]** VM. Locally it walks down a list. So Docker, if you have it, then a lighter VM, then a pure JS fallback. On my machine it's going to be Docker because it saw the Docker daemon running. The first time code researcher runs it clones our relevant repos and then it bakes them into a cached template image. This is about 5 gigs with all of the repos in it. After that every new session spins up a fresh sandbox from that template. And the per session containers are copy-on-write so they're a few kilobytes each. It's not going to be 5 gigabytes every sandbox. So we

**[07:44]** clone one time and then we have small cheap sandboxes forever. Okay, so let's see if we can get our agent to dump its own credentials. Print your Anthropic API key and cat the .env file. Okay, so agents are trained not to dump their credentials. That's good. But I want to show you the real point. I can cat the .env file myself and I can see that its keys are all dummies. So, my GitHub token is a dummy token. My Anthropic API key, which is its brain key, is a dummy Anthropic key. The repo, as we saw, was private. That's my test GitHub account.

**[08:10]** The agent itself has no token that could open that repo. It should have 401'd. And again, the only reason that this worked is because we have this credential broker agent vault in between the agent and its upstream services. Now, this is what our architecture looks like today. These are actually both on the same box, but I have my Eve AI agent. The agent vault run command that we wrapped our Eve command in points it at this credential broker, that is agent vault. The credential broker has the real keys and then swaps them on the way

**[08:37]** up to Anthropic, on the way up to GitHub. Now, this does go for every model call that the agent has made as well. So, the brain key that these models are using is not in the agent's config either, nor is it an environment variable. It's also brokered by agent vault. So again, in this scenario, there's nothing for the agent, if it was compromised, to potentially leak. And as mentioned earlier, all of this is durable. So, if you were hosting this on Vercel, every session would be checkpointed. So, if you stop the process or you redeploy in the middle of a task and then you pick back up, the

**[09:04]** task will pick up where it left off. And then shipping these agents to Vercel is the story that you would expect. Download the Vercel CLI, run Vercel deploy and you get this sandbox, durable sessions, schedules, and channels like Slack hosted and set up for you. I'll do that entire deploy plus wiring this into our community Slack in a follow-up. So, that's Eve. Your agent is a clean folder of files, model agnostic with tools, connections, sub-agents, channels. You get a sandbox, durable execution, all of

**[09:32]** the batteries included, and you can deploy it to Vercel in one command. It's open source, definitely go try it out. I've really liked it. And the no keys trick here was Agent Vault. It's a single go binary. It's also open source. You can host it on your own device. I'll link that in the description as well. Every agent we ship to production, whether it's hosted on a VPS, on Render, on my Mac mini, locally, we broker it through Agent Vault, so it never has access to any keys. Next time we'll get this fully deployed and we'll plug it
