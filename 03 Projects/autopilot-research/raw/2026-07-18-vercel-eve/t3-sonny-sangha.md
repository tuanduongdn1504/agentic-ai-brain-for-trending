# Sonny Sangha - Vercel Eve Changes How We Build AI Agents (Beginner Tutorial)


> Source: 5zLdVdOUxgE.en.vtt
> Converted: vtt-to-md.py
> Format: `[MM:SS] line` — verbatim auto-sub, deduplicated, word-timing tags stripped


**[00:01]** So, Vel just did something and quite honestly, it's the new standard of AI agent building and it's definitely how I'm going to be building agents from now on. Let's check it out. What's going on, guys? It's your boy Sunny. If you're new here, I teach everything AI. If that sounds good, hit the like and subscribe and let's jump into it. Okay, so if you wanted to build a real AI agent before, it used to mean coding lots of infrastructure before it did anything actually useful. And quite honestly, I don't think people realize how much you actually have to hand build yourself. Think about it. streaming tokens and to

**[00:28]** talk calls back to the user in real time. Routing between different models if one was to go down. If your agent is a bit more complicated and you want multi-step, you need to make sure that stays alive for minutes or if not hours. Or if I want to talk to it from somewhere else like Slack, web, how does all that work? And the biggest one, my agent needs to run from somewhere safe and isolated so it can't do any damage. And that's not even mentioning human in the loop moments. That's just scratching the surface, but the point is it's a mountain of plumbing you got to deal with. And that's the whole point of this video. You see, Velo noticed that loads

**[00:55]** of devs were struggling with this exact problem. So, they turned every single one of those problems into a product you can just plug in and they call it the agent stack. Now, the agent stack pretty much comprises of six different areas. So, when we're talking about connecting to a model, streaming model calls, we're going to be talking about the AI SDK. And if we want to route to several LLMs and also handle if one was to fail and switch to another provider, well, that's where the AI gateway comes in play. And then when it comes to actually running our complex workflows with our agents,

**[01:23]** we can use REL sandbox to run untrusted code safely in an isolated environment. And we can use workflow to keep long runs alive which survive crashes, prevent double charging, all that stuff. And then what about if our agent wants to plug into MCP servers or outside APIs like linear or GitHub and that's where pretty much connections handles everything for us. And finally, like I mentioned, you want to ship that agent into loads of useful places, whether it be Slack, Teams, Discord, maybe your own web app. That is where the chat SDK comes in clutch. And remember, it's not

**[01:49]** just about building a core agent. It's about scaling to real traffic without paying for things like, you know, code starts, idle time. How do we handle scalability if loads of people want to use the agent at the same time? And that's where you can avoid all the headache of setting up your own servers and instead use VLE's fluid compute, handling everything from code starts to scaling, concurrency, overheads, a scalable pricing model, and ensuring that you don't overspend on CPUs that you're not using. And for quite some time, if you've seen any of my live streams, I've been using a lot of these

**[02:15]** products in my builds. And I've even used them in a couple of production apps, which means that I'm focused on building a successful app as opposed to any of the agent infrastructure behind it. But it was only recently that I got really, really excited because Vel just dropped Vel Eve, which is their brand new agent framework. And the easiest way that I can describe this is that the same way that Nex.js came in and leveled up React, we pretty much now have Eve to level up Next.js and handle everything agent related for us. And real quick before we dive any further, full transparency, this video is sponsored by

**[02:43]** Vel, which quite honestly is a massive, massive privilege. And thank you to everyone who made that possible. So thank you to all you watching right now. You all know that I've been turning to VEL for ages on this channel. Whether it be Nex.js, whether it be deployments, whether it be the AI SDK, the chat SDK, I've used them a ton in previous videos. You can go check those out. But on my last live stream, we even played with Velv. And if you go and watch that right now, the live chat was blown away by how simple it was to sell. Today we're going to go even deeper. So the plan for

**[03:09]** today, we're going to take a look at Vel Eve and see how it addresses each part of the agent stack. I'll show you the exact problem it kills. We're going to demo it live inside of a real agent. And I'll show you how you can use Vel to build your entire agent stack or individually piece together different elements from the agent stack as and when you need them. So let's take a look into probably what will be the easiest agent you will ever build in your life. You can safely hold me to that. So landing over at versel.com/eve, you can see we've got two options for human or for agents. Simply if you go the agent

**[03:37]** route you can install the skills and get it to set everything up for you which I thought was really cool. But the main thing is is that the way you set up your agent when you use Versel Eve it's all through your directory. Okay. So I want to basically I think the visual here really kind of ties everything together. But here we can see you start with your instructions MD which is literally a markdown file explaining what your agent does. So if we take a look at my agent I built today post a data analyst agent for this demo. We can see inside of my

**[04:03]** agent folder, I've got an instructions MD which pretty much just explains who postes and what they should be doing. Okay, simple enough. And then next up, we go ahead and initialize our agent. And this is where we leverage the facel AI gateway. So over here, you can see pretty much we just include the agent.ts file and we literally just include this very small snippet and Eve does the rest for us. And you can even customize things further. So if I go to my agent.ts here, we can go ahead and define what model we want to use. again

**[04:31]** supports all of the popular SDKs. In this case, you can see they have tons to choose from. Okay, so the VEL AI gateway is amazing for this. You can select your reasoning model and then there's tons of different options. For example, when you want your threshold for compaction, limits you might want to put in place, sub aent depth when it your main agent calls across to sub aents and of course the description for the agent itself. Okay, so just with those two right now, we already have an agent and it knows what to do with an instructions MD file.

**[04:57]** And again, it's worth mentioning that everything on the right here is optional. So you can literally just stack these on only as you need them. Okay, so next up, what if our agent wants some skills? Well, in this case, skills are just markdown files. So in this case, you have a skills folder. And then you simply put the MD files inside of our skills folder, just like we've ever experienced skills before. So in this case, let's take a look at our skills over here. I have one, which is metric definitions, which is how my data analyst agent can go and basically

**[05:24]** calculate things and help us out. And then we have Vel connect which is actually a skill I installed into this agent over here. And you can see that this is simply a skill MD file which has the usual folder structure that we've expected if you've ever installed a skill before in the past. And again just by having it inside of our agent folder inside of the skills folder everything gets registered and picked up from our agent. Okay. But we want to get more powerful. We want to add tools so our agent can actually do things which are more interesting for us. So in this case

**[05:52]** they've got an example for getting weather. Okay. Now I'm going to show you a bit bit more of a cooler example here. Now in our agent post I've got two tools query metrics and run analysis. If we go into this one we can see simply a define tool call. We have a description the input schema which uses zod for validation. And then we have the execute block where all of the magic happens. So if I was simply to compress this down you can see everything just simply happens inside of our execute block. And then we just return some structured data. And then we have the sandbox. So

**[06:19]** each agent actually runs in a completely isolated instance with its own files and its own tools. This means every agent can go ahead and create files, do calculations, do really complex things all again in its own isolated environment which would not affect any other agent that gets called. And then we have more things such as connecting our channels such as Slack integration stack as well such as MCPs or any other connection as well as sub aents as well as the ability to run it on a schedule. But enough talk, let me show you the actual demo itself. So here I built a

**[06:46]** complete demo of posts, right? You can go ahead and grab the entire code down below in the description. I've included a free GitHub repo for all of this. So you can play with it yourself. And this entire demo is powered by Vel Eve where we're using the agent stack under the hood. Okay. So let's go ahead and give it an example question. I can either type in the question here or I've got these predefined ones here. So how did signups do this week compared to last week for example? Click that and then we can see our agent will spin off. Now the first thing I want you to pay attention to is on the right hand side we can see

**[07:13]** the run timeline. So we have thinking happening here. We have skills being called and look at this. We can see it's loading skills in and I can see exactly what it's doing. We have the query metrics and all of this is happening in real time as we speak. Okay. And it's absolutely amazing because not only have we got reasoning at play here, we've got tool calls, but the main thing is is that I can visually see what is going on. So that way I can be sure that my agent is actually doing only what I want it to do. Now I really want to showcase

**[07:40]** this, right? So in this case, our main agent actually delegated to a sub agent investigator, right? So this is an investigator sub agent. And if we were to look at the code here, you can see all it is is another folder, right? Where and this is basically the name of our agent and everything else keeps the exact same structure as the parent agent. So we have its own skills, its own tools, its own agent.ts and its own instructions file. So you can see this

**[08:08]** is simply just a agent.ts describing what our sub agent is. And you can do this with as many sub aents as you want and then your parent agent can just delegate as it needs to. Okay. So in this case we can see let's go ahead and see what the calculations it did was. So it actually returned to us and we can see it gave us a nice breakdown of everything that happened. Okay. Now if we look at the output here you can see it actually gave me an entire breakdown answering my question perfectly. And if we look at the run timeline we can see when the agent initially began the turns that were caused the workflow. So in

**[08:36]** this case you have steps that are actually executing and this is how it remains durable. And then we can see a sandbox analysis happen. So if we go over to where the sandbox analysis actually happened, we can see look if we click on the run analysis, this is one part I really really find quite cool. So Python ran in the EVE sandbox. Now if we look at what's actually happening here, this is an individual isolated environment where this agent has actually created its own files. Okay, so

**[09:05]** this ran a Python script and actually created its own post analysis JSON, postchart SVG and report.md, right? All right. And then it was able to return those files for us. And this is all happening in an isolated sandbox. So you saw the setup earlier. Fairly minimal things that we had to do. And then every single time a step was completed. We can see the actual estimated cost because we have access to the VEL AI gateway. And then we can see how everything came to an end. So in this case, we can see the final response when it was ready, when

**[09:33]** the workflow came to its conclusion, the turn ended, and then the session parked. And as you can see, post is waiting durably for the next turn. So in this case, if I was to ask another question, it's warm. It will be able to answer that question straight away, nice and quickly. And as you can see, the whole flow starts again. Now, you're probably wondering, well, Sunny, to get this whole thing set up, I have to go ahead and do all of that and learn a new folder structure and everything in between. And the answer is absolutely not. All you need to do is type this one command in MPXeve at latest init post,

**[10:02]** right? Or whatever name you want to give your main agent. And this will set the entire directory up for you. You can even go ahead and add in the additional argument to pass in channel web next.js to add in an entire web chat application from a template. And one of my most favorite things is you can see we can actually talk directly inside of the terminal. So this agent is not just on the web, but if I was to go ahead and actually run this agent, you can see I have a little script here for actually running Eve with a Eve dev command. So

**[10:31]** I'm just going to go ahead and run this command like so. And now I can actually get a full chat going with the exact same agent. So in this case, if I wanted to say something like compare last week's numbers against this week's numbers and give me the most important metrics. Go ahead and hit enter right now. And we can see Vel is actually running inside of the terminal right now. We can see when tools are being called. We can see when commands are being run. And look at this guys. We can actually see

**[10:57]** everything in between including the entire reasoning process and how it's going to get to that final answer. And what's really cool about this is that human in the loop elements such as if we want it to ask us questions, we can have that all inbuilt inside of here, including approval gates. So whenever you're using any kind of other coding harness out there, you're probably used to it saying if you want to allow something to happen or not. All of that is built into facel and it's super super easy to use. We can even see here that when it loads a sub agent, you can

**[11:24]** actually see a nice difference in the terminal. Right? So in this case, look at the UI. It now launched an investigator agent and then this is all a sub agent. So it's literally orchestrating the entire flow right now and everything is running through Vel's entire agent stack. And then we can see our final response comes through super neatly, right? As we got the headline, we got the signup. So we can see exactly what is going on. And all of this happened by simply having a Eve.TS channel. And as we can see here, by

**[11:51]** simply adding in a local dev, we're able to run Evedev and we're able to go ahead and run it in our terminal. Now, if you're shop, you would have seen that in the channels, we also have Slack. And getting Slack up and running is so so easy as well. If you want to know how to do it, so simply go to the documentation, go down to channels, hit Slack, and what you'll see is they have a bunch of CLI commands that can go ahead and help you out with this. Now, I've already actually set this entire thing up. So, in this case, you can see we've already installed the credentials inside of the demo, and then we have our Slack connector inside of our

**[12:19]** environment variable, or we have the connector over here. Now, this is all able, you can actually see where all of your connections live. If we simply go over to our dashboard and inside the dashboard you can see over on the left hand side we can see VEL connect. Click into it and this is where the connection actually happened. So if I head over to my Slack workspace now I've got a Vel Eve channel here and I go ahead and type in for example Vel Eve bot. So in this case I've just created a test bot and I can go ahead and say update me on the churn rates. How are we looking from

**[12:46]** let's say this week to the last couple of weeks and then hit enter. We can now see our agent is going to kick in and it's going to literally be thinking on the spot here. So, as you can see, all of those individual tool calls, all of those commands that it's running now visually are shown in here. And I thought this was a really nice addition with the connector to Slack. And all of this is using the chat SDK from the sales agent stack. Or you could just use the chat SDK in your existing app and go ahead and add in that capability if needed. And of course, as you can see,

**[13:13]** it works across a ton of different platforms. So, you're covered in tons of different areas. And as we can see, heading back over to Slack, we can actually see the agent actually returned the information. Perfect stuff. Look at that. And then to resume the chat, all I would go ahead and do is continue on the conversation, tag it in. I can go ahead and ask follow-up questions. For example, how many activated members do we now have, and you can see that that chat will go ahead and resume inside of the thread. So, obviously, you can customize all of these like, you know,

**[13:40]** variables. So, you can have it that we have to go ahead and mention it, for example. So that way if in case I was to type something else. Let's go ahead into another thread here and I type in hello. It won't just go ahead and respond to everyone because you might be having a chat with your team for example, it will only go ahead and reply if I have mentions. So you can see this is really powerful guys and all of it was possible through literally one snippet of code as small as this. Okay, so let's go ahead and break down this entire build so you can see just how easy it is to set yours

**[14:06]** up yourself. Okay, so as I mentioned it's similar to Nex.js. Just like we have an app brower inside next and we have pages inside the different folders which resemble routes. In this case, each individual folder resembles the different element that we're trying to interact with. For example, if we're trying to add new skills, we simply add things inside of the skill folder. Okay? So, for example, all of our metric definitions go inside of here. Then, for example, if we want to add any new tools, it simply goes in the tools folder. The sandbox configuration can go

**[14:34]** inside of sandbox. Any channels that we want to interact with inside of the channels. And then for example, our sub agent is basically going to be a nested folder structure where every single folder beneath it resembles an entire agent where they have the same layout that the parent has. Okay? So they have an instructions MD and agents.ts and so forth. And this can basically go on and on, right? So we can have basically as many sub agents as we want here. Now in this case, I showed you how we can run this from the terminal, from Slack, as

**[15:01]** well as from our web interface. Now, I showed you how to communicate with the agent from the web, from the terminal, and from Slack. But I didn't actually show you the code for the web. So if we dive into it, when you're using React, we have this handy use Eve agent hook. Okay, so in this case, all we need to now do to handle all of the streaming and everything in between is just call the used Eve agent hook, which gives us back the following helper. Okay, and then all we need to do is do agent.end and we can actually send the message

**[15:31]** alongside any context that may be needed as well. Okay, so that's step one, right? that handles the input where we can send a message to our model. Now, what if I want to go ahead and actually interrupt the agent through any moment in time? I can actually just call agent stop and that will go ahead and basically halt its execution. And then, of course, I can tap into the agent status at any point in time. And if we can see here, we've got the agent status, which could either be error, ready, streaming, or submitted. So that way, I can go ahead and basically make the UI respond however I want. And then,

**[15:59]** of course, the most important part, actually, the agent's output. So if there's an error, we can just simply tap into agent. And then go ahead and showcase something. However, the messages themselves come through agent.data domes messages. So this is how we're seeing everything stream inside. And this is all going to happen just by simply using the use eve agent hook. And then I've linked all the code down below. So you can actually dive into this further. But if we were to look into this a bit more, if you've ever done any work with the VEL AI SDK, it's very familiar to you because we

**[16:27]** have the messages and then we're simply going ahead and passing the messages down. And then inside of agent message what I can actually see is we have message.parts and then what you typically are used to is having a part type. So this could either be a step text if it was text that returned reasoning if there was any thinking that occurred authorization dynamic tooling and this is basically where we would showcase individual tools that were called and how they should look and how you choose to render out those parts

**[16:54]** completely up to you. The main thing that you need to understand is from simply running the init command once you get the agent folder with the bare minimum. So you'll have an agent TS a simple instructions file and then you can go ahead and start creating your agent from scratch adding in your individual tools your skills the sandbox the channels it's going to interact in. And then comes the question well okay so so far this is all working off of my own data but how for example could I connect to let's say linear or notion if I've

**[17:21]** got data stored elsewhere. So if we head back over to the versel.com Eve page, we can actually see the connections tab, right? And what I really like about their homepage right now is you can actually see each of the individual agent stack elements that it actually leverages. So in this case, the sandbox will leverage the Vel sandbox. We can see that we have channels leveraging the cell connect and the chat SDK. And then the connections is actually where we can go ahead and define our MCP connections. So in this case, if we want to connect to linear, we'd simply just go ahead and

**[17:48]** do the following. We'd include the linear MCP address, the description of what it is, and then we have a connect block here. And one thing I really, really like about this is if you actually jump into the documentation yourself. So, if we head down over to get started, you can actually go ahead and dive into all of this. So, if you click on connections, for example, MCP connections, we can actually go ahead and define a connection. And then you can actually use either VLE connect for the oorthth, which will basically go ahead and pull it up in front of you

**[18:15]** inside a browser or flow. So you can go ahead and authenticate for example your linear account and then those credentials would get stored inside of your VEL dashboard. So in this case it will go ahead and get stored here. Or if you want to use something like tokens you can use tokens in this way or if you want to go ahead and send it through the headers or if you have a token you can use those as well and you can even use no or if you're brave enough or it's a local MCP for example. And it doesn't just stop there. It works with MCP connections yes but it also works with

**[18:43]** any open AI compatible connection as well. Now, what if I wanted posts to run, let's say, every day at 5:00 p.m. and I want to get a summary sent to me somewhere. How could we do that? So, they thought of that, too. And if we go into our schedules here, we can see I've set up one for a Monday summary. Okay. So, inside this code block, I've actually included a ton of comments to help you out. But in this case, every Monday at 9 UTC time, we're going to get the following trigger fired off, and it's just written as a crown job. Okay,

**[19:10]** here we've got the markdown. run the post-weekly metrics report for the current week or versus the previous week. And in this case, we can go ahead and set these here. In this case, I've just hardcoded these in, but you can feel free to add any logic here that works for you. And then we can go ahead and ask it to do whatever it needs to do. And then once we've deployed our agent, every single Monday at 9:00, I'm going to get the following update automatically sent out to me. And all of this can work either on Vel. So we can either deploy it through Vel's deploy in which case when we deploy it will set up

**[19:38]** a chron job for us or you can self-host it yourself on a self-deployed host. So you're not locked into VLE when you're using VEL eve. And that's something really important I want to mention here. You can actually check this out for yourself inside the doc. So you can go over to the deployment tab and then deploy on Vel. Now deploying with Vel is super easy. You simply run Vel deploy and as long as it's detected inside of your photo structure it will deploy it alongside your Nex.js app. And if you wanted to go ahead and deploy it with alphaso, you can simply go ahead and do that by running Eve build and then

**[20:05]** deploying it to your host. In this case, it actually compiles the entire EVE agent into a standard Nitro output. And that way you can run it wherever you want inside of your own Docker container or inside of your self-deployed host. But one of my favorite things is if you're going to use it inside of your NexJS project. Setting that up was way easier than I thought. Let's go ahead and take a look at our next project here. So I have an app and I have a page.tsx with the agent chat inside of it. Now, if you're probably wondering, okay, typically we look at the layout.tsx to go ahead and wrap our app

**[20:33]** with some kind of provider that will allow us to use Eve, right? Well, it's actually way simpler than that. In here, what we actually do is we go to our next config. All we need to do is actually wrap the next config with Eve. Literally, it is that simple. Just by doing this now, we're able to go ahead and deploy our EVE agent alongside wherever our NextJS app is deployed. And we can see here it supports a couple of different framework integrations. So we've got for next.js use with Eve and

**[21:01]** then you get access to the use Eve agent for nux you have the appropriate or for any other react app you use the following over here. Now something I wanted to showcase remember when our agent was running there were steps and workflow turn calls. Okay that was actually using the workflows part of the agent stack. So here you can see we have a bunch of different workflows that have been executed right. So in this case if we were to look at this one over here we can actually see all of the different turns. Okay. Now in this case our actual VLE EVE execution we don't actually have

**[21:30]** to include any specific kind of markers in our code to actually get this turnstep behavior. It actually knows but just by using Vel Eve every function inside of it will naturally be turned into a step. So we can see the different execution points. So that way we can replay from certain points see the events any streams or whatever it's doing within. But where this can get really really handy is if you want to get granular. And to check out a really nice example of this, just go to visel.com/workflows. And we can see that to set this up, you

**[21:57]** can pretty much see, look, if you go ahead and actually label each thing, you've got a really nice breakdown. So, for example, here we've got fetch order for 230 milliseconds, validate, enrich price, you can actually see every single step that happens in the workflow. Remember, this doesn't have to just be for agents. This can be for any kind of execution that you want to remain durable. And actually adding it into our app is super easy. So to go ahead and do it all you would need to do is simply add the use workflow directive at the top of wherever you want the workflow to

**[22:25]** actually start and just by doing this you can see we actually get full telemetry right so in this case we can see when everything gets carried out how long it took observability we can look at every single thing that executes here. So whenever we have a function all we need to do is simply add in the use workflow when we want things to start up on a workflow and then basically everything that happens inside of it is part of that workflow. Okay. Now in this case one of the functions pomemetrics we we've defined it over here we can simply pop in the use step right if we want to

**[22:54]** now what this will do is it will define that as a step inside of that workflow and that way we can go ahead and make sure it gets included into our entire workflow and why this is really cool is because this allows you to have durable workflows which means they can run for absolutely ages and they're not going to time out and also when they're not being used you're not going to be paying for that idle time. So you can clearly see here you're only paying for the time of the actual execution time. And this is

**[23:21]** the difference between hosting a server which sits idle for ages and you have to pay that bill versus using something like Vel workflows where literally it's only getting build as it's being used. And under the hood this is all powered by Vau's fluid compute. And one of the main reasons why I like fluid compute so much is because honestly it's just handles everything for me. Now, sure you can self-host, you can do everything you want. And that's why I like Vel the and agent stack because you can self-host a lot of the options here. However, in a

**[23:48]** lot of cases, you just want to focus on the product you're building, not the scalability behind it and all this stuff because far too often I find that many people are focusing way too much on that and then they build a product that sucks, which in my opinion is a huge time waste. So, I instead opt for fluid compute where they handle everything and it just ensures that my API is always up. It can handle extreme workloads as well as if there's a ton of traffic all at one point. I can be sure that everything remains functional for all of the customers. And like I mentioned before, the idle time, right? So no

**[24:15]** longer having to worry about servers that just sit there. For example, I've seen this over and over again when people set up their servers. If you've ever overpaid for a situation like that, I would highly recommend you check out something like play compute. And this brings me back to Velve, the framework itself, because this is where it's truly quite honestly a breath of fresh air. I feel like this was needed in the space for a very long time. Yes, we had all the other components which I've been using for quite a long time to be fair. However, they've remained yet to be one

**[24:43]** single framework which tied everything together in just a clean way. Now, I can promise you this is what I'm going to be using in my builds from now on because it's just so simple to use and it gives me everything I need out of the box. And like I told you guys, the only thing which is actually mandatory here is the instructions MD file. If you don't even include an agent.ts TS file, it will default to your VEL AI gateway default model. And just to show you what that looks like, if we go into our actual AI gateway here, you can see the AI gateway

**[25:11]** is very, very simple, guys. You can simply create an API key, see all of the usage very simply. In this case, I've got the demo key here. If we click into this key, I can see all of the usage, the breakdown, the different models that could have been used. In this case, I've only used clait 5, but remember I can have tons of different sub aents using different models here and it would just simply work. You can even bring your own key if you want to as well. And this is where everything comes together really nicely, right? We got the workflows powering all the underlying parts of our

**[25:39]** agents infrastructure and then the channels to go ahead and hit the agent where we need to. Now, the final thing that I actually wanted to touch on really quickly as well is the sandbox, right? So, I mentioned before the sandbox execution, right? But in this case, if we go to tools and run analysis, I want to show you actually where it gets really cool. Okay, so in this case, we have our tool call that gets executed. So let me just go ahead and make this a bit smaller so we can see that we have a description, input, and execute. Right? And then we have a specific structured output here. But in

**[26:06]** this case where we have the create sandbox paths, right? So I've actually refactored this to make it a bit easier to see. But all we have here is paths. Okay? So these paths are basically just going to be routed to you can see here this is going to be the actual isolated workspace instance for every agent and we can actually see this happening in real time. So we can actually see dot workflow data and you can actually see all of the individual steps the events the streams everything gets actually

**[26:33]** stored here. So you can actually see locally what is happening. So if you're not deploying on bell effectively everything is getting stored locally on the file system. And this is also how we're able to test it locally when we use it. And it gets even cooler because everything that is included inside of this workspace folder is actually going to be for every single agent. So here you can see sandbox workspace. And in this case, I can actually store raw Python scripts. Yes, I said it. Raw Python. This is now Python inside of my project. And this will absolutely work

**[27:02]** when our agent requires it. Okay, so in this case, post analysis.py. So I've got a TypeScript file here which is actually going to go ahead and pull and use that script when needed. And we can actually see where that script is ran from by simply going ahead and doing a command f on post analysis. And I can actually see that we have the output path and the seeded python script path. So in this case the python command that I actually want my agent to run. And then if we dive deeper into the code, all I do is run sandbox commands against that

**[27:30]** original sandbox. So in this case sandbox run make directory sandbox.right text file sandbox.run command and this is where the python command will run. sandbox readext file and so forth. And you literally have a full working directory for every single agent. And if you want to know how all of that works at a deeper level, you can simply go into Vel sandbox itself to look at the core product on its own. Or better yet, what I like to do if you're going to use Eve anyway, go to the get started

**[27:56]** documentation and then head over to the sandbox tab because in this case, it will show you all of the tools which are readily available as well as examples of how to go ahead and use the sandbox just like so. So in this case that you can see get the sandbox, write a text file. In this case, it created a Python script from this script that we passed through and then it's able to run that entire Python command in the isolated sandbox and then return the result. And trust me, doing that on your own is not as

**[28:23]** clean as you'd think it would be if you were to do it without Vel. And then as for where those sandboxes live, if you go ahead and set it up with the VLE, so in this case, when you actually have your sandbox config, so in this case, we go to the define sandbox, which is our sandbox.ts TS file you can go ahead and in this case define what the back end is. So here we can actually define what the vCPUs are. So you can go ahead and increase that if you need more power but you can go ahead and use VEL if you want to stay on the VLE ecosystem docker a micro sandbox just bash or default

**[28:51]** backing if needed. And again the best part about all of this is you don't need to understand every single thing that's going on here. In my honest opinion, the best way to get started with this is simply go to the homepage visel.com/eve install this skill and just simply have a conversation with your agent. In fact, in the read docs, if you actually go to the get started section, they have a really awesome prompt that you can go to over here setting up with a coding agent and simply go ahead and just copy this

**[29:18]** prompt and this will actually set up Eve inside of your existing projects. So, you can get started with it right now today. And there we have it guys, for sales agent stack. You've seen me use the components in individual builds previously, especially the VEL AI SDK, and you can do exactly that if you only need one part of that agent stack or you've already got a setup where you could benefit from one of those parts of the agent stack. Or if you're yet to build an agent and you've thinking about which framework to go ahead and use or potentially how to go ahead and build it, my 100% recommendation because this

**[29:46]** is what I'd be doing from now on is use for sale Eve. And as you can see here, by simply going ahead and using the folder structure that is allocated when we set Eve up, you can go ahead and get all of the benefits of the entire agent stack just in its most simple execution form. Okay, so in this case, very very minimal amounts of code to go ahead and get the full power of the sandbox, the chat SDK, for cell connect. So you can connect literally to as many MCPs as you want. Put it on as many different

**[30:12]** channels as you want. Have everything routed through the AI gateway for simplicity and then be sure that nothing's going to go down and that you have full visibility because it's all running through a cell workflow. And that's not even to mention tool calling, sub agents, how complex you can really get with this. For example, by adding in tons of different human in loop elements to make your agent conversation a lot more useful to an end user. And that's the real takeaway from this video. I'd highly advise that you just pick the level of integration that fits you. If you need just one product, every single

**[30:39]** one works standalone with whatever you're building today. Or if you want to compose your own agent architecture, they're all just simply SDK. You can wire them in however you like. And if you want the entire assembly done for you, that's Eve. And I just want to wrap up by showing you one last demo. If we were to generate, for example, a weekly report. Okay, so in this case, this is absolutely deployed. All I did was simply do Vel deploy and this went to production. And now we can see it's executing. Let's take a look at the back end. Right. So in this case, if I go

**[31:05]** over to our dashboard and I want to go down to our sandboxes, remember I said this will kick up a sandbox. So in this case, I'm going to go into the deployment and you can see that this sandbox is now just started running, the resources it's using, and we can actually click into this and see every single thing it's doing. It ran with node version 22. And then we can go ahead and see all of the different bash commands it did. We can click into the activity. We can see different snapshots if we need to. So when it comes back to us with an answer, this is actually

**[31:32]** going to remain open. So in this case, it's not going to be just shutting off by itself. This will actually remain open for the agent. So it's warm. So that way it doesn't need to start up another environment. So if I was to go ahead and for example send another message, let's go back to our chat and let's go ahead and just put another message in the mix. We can now see that this will simply go ahead and continue on another workflow. Right? And as I mentioned, because this is running live firstly, you can see the different steps that are now kicking off. But we can

**[32:00]** also see those workflows as well. So, if we go down here, I can actually see there's an active workflow. And you can see that this one completed 19 seconds ago. And then it spun up another workflow. And we can see here it's actually going ahead and showing us the entire workflow. So, I'm going to go ahead and pop this on the side. Now, if I go ahead and ask another question, we can see another workflow spins up. And just like that, guys, if we go back over to our workflows, we can see yet another workflow kicks off. Okay. So, in this

**[32:26]** case, we can see that it's still waiting for a turn to come back. So any moment now we can see that this is actually running. So this is actually happening in real time. So that as this is running we can see Eve sub agent investigator in this case while this is running doing everything that the sub aent is is supposed to do. We can see it will automatically configure that as a step. And you can see look the step just finished. Amazing stuff. And then the response comes back. Right now I can see

**[32:54]** that the most longest part of that entire execution was this right here. And this is actually going to be where the sub agent kicked off. We can actually click into it, see all of the different events. And then of course we can dive into each of the individual events. So if I want to jump into the step completed, for example, here I can actually decrypt the results. Everything is encrypted. And then you can go ahead and actually see the output. So for example, if I was to really dive through here, the session itself, the agents history, and so forth. We can actually see some of the information that was

**[33:20]** getting passed back and forth from that call. So you can go ahead and get real granular with this if you need to. And there we have it, guys. In my opinion, the fastest way to go from idea to a production agent. And don't forget that the entire post code base is free and linked down below in the description. And of course, when you go ahead and sign up to Vel, it will mean the world to me if you go ahead and use the link in the description, it will tell them that you came from this video. And for that, we're massively thankful. So, let me know down below in the comments. Will you be using Vel Eve or parts of the

**[33:47]** agent stack? And what agent will you build with it? And also, this one took a ton of work to get done. So, all we ask is that if you like this video, simply go ahead and see that button down there, smash that like button, and make sure if you haven't already, go and subscribe because tons of you are watching this video right now who aren't subscribed to the channel. And trust me, we've got tons of more AI and agent content coming on the way. As always guys, it's your boy Sunny. I'll see you in the next one. Peace. &gt;&gt; [music]

**[34:20]** &gt;&gt; I know how to love you. &gt;&gt; [music]
