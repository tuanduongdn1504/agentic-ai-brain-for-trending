# (C) loop-engineering — Verdict (v189)

**Ship:** 2026-07-02 · branch `wiki/v189-loop-engineering` off the v188 tip (b1e315b; v188 itself unmerged — branching off main would orphan the v188 state) · pinned commit **`f18df04`**.

## Phase 0.9 verdict — GOAL-ALIGNED INCLUDE 3/4 [(a) FAIL · (b) STRONG · (c) STRONG · (d) STRONG]

- **(a) FAIL (clean).** Cobus Greyling = disclosed individual — Chief Evangelist at **Kore AI** (conversational-AI vendor), South Africa, prolific AI writer (Medium/Substack/X) — **not Anthropic** ((a)-7 is Anthropic-only; profile WebFetch + web research: no Anthropic affiliation). Serial "X-engineering" portfolio (loop-engineering + goal-engineering + fleet-engineering, 129 repos) → **#19 19a first Cobus-Greyling author**. Touches the disclosed-builder (a)-axis held operator-reviewable at N=3 per v182 — reinforces, does NOT reopen. No heritage/locale rescue (v159→v188 discipline).
- **(b) STRONG — keys the tier (§31).** A methodology + pattern collection + audit toolkit for **scheduled, unattended coding-agent loops** — the *control layer around* Claude Code and its peers, built explicitly on **Boris Cherny's Claude Code workflow** (`/loop`/`/goal`/`/schedule` cited as first-class primitives) with Claude Code first-class throughout (.claude starters/skills/agents + 8 Claude examples + matrix column). Dead-center Goal #1, and it lands on **five live operator threads at once**: multi-agent-orchestration (maker/checker, worktrees, collision locks) · claude-api-cost-optimization (loop-budget 80%-throttle/100%-kill, loop-cost, per-pattern token models) · CC-memory-systems (STATE.md durable spine) · CC-observability (JSON run-log + readiness scoring) · Pocock/how-we-claude-code (AFK queues + agent-native verification). Uniquely: **the vault already RUNS loops** (autopilot-research, scheduled tasks, `/loop`) → immediately, install-free pilotable. STRONG-not-STRONGEST = third-party + Grok-first attention (default `--tool grok`, the author's essay maps Grok) + Claude one-of-6/7 harnesses + the concept originates upstream (Osmani/Cherny/Steinberger).
- **(c) STRONG.** 7 registry-validated patterns + 16 starters + 4 root skills + 10 templates + **6 CLI tools** (loop-audit 17-signal weighted L0–L3 scorer w/ hard L3 gates + 12 tests · goal-audit · loop-init · loop-cost w/ per-pattern token cost model · loop-sync drift detector · a read-only MCP lookup server) + ~46 cross-harness examples + 9 failure modes + 10 anti-patterns + a real safety model (denylists, no-auto-merge default, MCP least-privilege, kill switch) + **commit-verified dogfooding** (15-entry JSON run-log via its own GitHub Actions; live STATE.md at L3/100; score-gated auto-PRs) + npm OIDC-provenance releases + zero-supply-chain-risk source. **Caveats held honestly:** ~22 days old; the scorer measures *scaffolding presence*, not loop quality; "week-one" stories are labeled anonymized narratives (adoption evidence = self-dogfood); README-vs-starters gaps (Cursor named, no Cursor starter; ci-sweeper missing LOOP.md); cost numbers are estimates.
- **(d) STRONG.** gh-aw v48 (platform-layer domain neighbor) · agent-skills v184 (Osmani's session-internal half — the #57 link) · Paseo v150/ai-maestro v163 §C (interactive orchestration platforms) · ponytail v168 (behavior-ruleset sibling) · observability sub-archetype (run-log/score conventions) · agentmemory v66/memory family (STATE.md) · spec-kit v17/cc-sdd v61 (checklist-driven) · SkillOpt v178 (optimize skills ↔ audit loops) · #81 manifest-drift family (loop-sync) · #83 honest-disclosure family (failure stories).

**Streak:** GA:49 → **GA:50** (⚠️ under the v176/v186 OFF-GOAL reading → GA:48·OG:13). **§35 CLEAR** (window {v187 GA, v188 GA, **v189 GA**} = 0 OG). **35 consecutive goal-aligned ships v153→v189** (GA reading).

## Pattern outcome — 1 NEW §C standalone at N=1 (CORPUS-FIRST for the loop-operations-methodology surface)

**"Loop-Engineering Codification Kit — named-methodology pattern collection + starters + readiness-scoring/audit CLIs for scheduled UNATTENDED coding-agent loops (maker/checker split · durable state-file conventions LOOP.md/STATE.md/loop-budget/loop-run-log · graduated L0→L3 autonomy ladder · token-budget governance w/ throttle+kill-switch), cross-harness."**

- **The defining CONJUNCTION:** (codifies a *named, multi-author* 2026 practice with cited origins) × (7 operational patterns whose OBJECT is the unattended loop *around* agent sessions, not the session content) × (a quantified **audit/scoring** artifact class — "get a score" L0–L3 with hard L3 gates) × (durable state-file conventions as the loop's memory spine) × (cross-harness starters, Claude Code first-class).
- **Why a real boundary, not draw-the-circle:** every prior corpus artifact class differs on the object or the layer — **gh-aw v48** = scheduled agentic workflows as a *platform/compiler* (GitHub product, T4); **agent-skills v184 / ai-berkshire v187** = session-*internal* knowledge/discipline collections; **Paseo/ai-maestro §C#23** = interactive orchestration *platforms*; the observability sub-archetype *watches* agents, doesn't codify how to run them unattended. A methodology+pattern+scorer KIT whose object is the scheduled loop is a new §C surface (the agency-agents v185 primitive-boundary / OpenMontage v188 output-type-boundary style of argument).
- **Scope honestly bounded:** corpus-first for the **methodology/kit artifact class only** — the scheduled-agent-loop *domain* has corpus precedent at the platform layer (**gh-aw v48, credited**), and it is **NOT a world-first** (it explicitly implements Osmani/Cherny/Steinberger's articulation; external platform peers gh-aw/Continuous-AI exist; Ralph precedes as folk practice). Mint at N=1 per the Kilo-Code v177 / agency-agents v185 precedent: a recurring, world-visible class (Steinberger 6.5M-view post + Osmani essays + Anthropic's own `/loop`/`/goal` primitives) unrepresented in the corpus, captured via its most prominent OSS codification (4.7k★ page-stated).
- **⚠️ Operator/audit-reviewable NO-MINT alternative recorded:** "loops are just another vertical → file as a T1 methodology collection with cross-refs only (the ai-berkshire v187 treatment), or park as a DEFERRED watch axis." The mint stands on the object/layer boundary (session-external control system + scorer artifact class ≠ skill collection), flagged as the single reviewable judgment. Either way **counts UNCHANGED 46/11**.

## SECONDARY (NOT minted)

- **#57 corpus-recursive influence-citation** — cites corpus subject **Addy Osmani (v184)**'s "Loop Engineering" essay as *"the most complete written synthesis"* + the originating X thread = a genuine load-bearing methodology-citation (the PilotDeck v175 shape, arguably stronger: origin, not thanks-list). The corpus now holds both halves of Osmani's 2026 arc (skills v184 / loops v189). NO promotion; exact N-tally = audit bookkeeping. (Cherny/Cat Wu/Steinberger = non-corpus lineage nodes → Pattern #19 Anthropic-team-aggregator echo, no claim.)
- **#19 19a** first Cobus-Greyling author + serial X-engineering portfolio; disclosed-builder (a)-axis data-point (N stays operator-reviewable).
- **#84 84c cross-harness** — byte-identical SKILL.md copies (Grok/Claude/Codex) + hand-translated Opencode + per-tool starters + a CI registry↔CLI sync gate; **NO N-bump** per v86; NOT the ponytail v168 generator mechanism.
- **#81 Manifest-Drift family** — `loop-sync` (a *shipped drift-detection CLI*: STATE↔LOOP↔skills) + `check-loop-init-sync.mjs` + `validate-patterns.yml` registry consistency = instance-strengthening data-point (claude-seo v64 / agency-agents v185 family); exact N = audit.
- **#83 honest-disclosure** — *"Failures are first-class content"* (CONTRIBUTING) + why-we-killed-ci-sweeper (8M tokens/48h kill-switch) + verifier-false-APPROVE story + narratives explicitly labeled as such.
- **LV-C3 Self-Referential-Methodology-Demonstration** — aggressive commit-verified dogfooding (LOOP.md + live STATE.md + 15-entry run-log + *"the loop cannot mark its own change green unless the real gates passed"*) → files into the LV-C3 cluster (clustering-first §28; tally = audit).
- **LV #20 Token-Economy QUALIFIED-ADJACENT** — per-pattern token cost models + loop-cost estimator + 80%/100% budget governance + per-run tokens_estimate = cost-*governance* and *estimates*, not measured benchmark impact → **N stays 4** (v168/v188 precedent).
- **#12 LLM-routing artifacts** — AGENTS.md + SKILL.md dirs (.claude/.codex/.grok) + **three NEW artifact-TYPES in the family: LOOP.md, STATE.md, loop-constraints.md** (+ loop-budget/run-log); recorded, NO N-bump.
- **#18 B1-MCP adjacency** — `tools/mcp-server` = a small read-only lookup MCP server (repo v1, npm pending); an adjacency note, NOT a B1-MCP instance claim.
- **#66 supply-chain** — install BENIGN (zero postinstall / curl|bash / network calls; OIDC provenance; MIT). The real risk = the *domain*: unattended loops (token runaway, auto-merge, prompt-injection via triaged content) — mitigations are the repo's own L1-first/denylist/budget discipline, which the pilot fence adopts.

## NON-claims

NOT a new top-level pattern (max #85) · **NOT #52** (4.7k★/596 forks page-stated §37.4 → velocity unestablishable; the *creation date* 2026-06-09 IS clone-verified, so ~215★/day is page-implied — suggestive only) · NOT a #57 *promotion* (citation recorded) · NOT the Domain-Vertical-Skill-Collection (its "domain" is the loop layer itself, not a knowledge vertical) · NOT an N=2 of gh-aw v48 (different artifact class — platform vs methodology-kit; gh-aw credited as domain neighbor) · NOT #18 B1-MCP · NOT world-first loop tooling (Osmani/Cherny/Steinberger articulation + gh-aw/Continuous-AI platforms + Ralph precede) · NOT verified external adoption (stories labeled; adopters = self).

## Tier + counts

**Tier T1 Skill/Methodology Collection (Loop-Operations flavor)** — the karpathy v63 / mattpocock v57 / CodexKit v121 / agent-skills v184 / ai-berkshire v187 family. **Counts UNCHANGED 46/11.** §C live standalones **32 → 33** (+1, N=1). Tracked PROVISIONAL surface ≈39 → **≈40**. §35 CLEAR. ⚠️ Registry-sync note: the v188 OpenMontage §C row was recorded in the shim + 03c but missing from `_patterns/06` — mechanically synced in this ship (flagged for the next audit's tally check).

## Verification trail (per `feedback_wiki_verify_independently_check_collisions`)

Verdict produced **INLINE + hand-verified**. A **13-agent read-only workflow** (`wf_fbe77308-374`, ~1.96M subagent-tokens; 10 succeeded, 3 returned null on output-formatting and were re-done by hand) did source-reading + upstream research ONLY. **ALL corpus-fact/collision/identity claims verified BY HAND:** grep over `_state/` + `_patterns/` + `03 Projects/` + CLAUDE.md → **NO prior loop-engineering/cobusgreyling subject (collision-clean)**; the term "Loop Engineering" appears in-corpus only as Osmani's essay inside the v184 docs (→ the #57 link + the both-halves note); full §C standalone list read → **no loop/automation-methodology standalone (corpus-first for the surface)**; gh-aw v48 entry read (platform-layer neighbor confirmed); author identity via direct WebFetch of the GitHub profile (Kore AI / South Africa / serial portfolio); README + `resources/sources.md` + LOOP.md/STATE.md **hand-read in full** (the Osmani/Cherny quotes above are from my own read, not agent summaries — one agent's "Osmani = Head of Claude Code" title-swap error was caught and discarded); first-commit date + tags **clone-verified**; loop-audit rubric quoted from source. One workflow-agent confabulation-class error caught (the title swap); zero corpus claims were delegated.

**inflation_check = discipline HELD:** 1 mint ≤2 cap · N=1 scoped to the artifact-class surface with domain priority credited (gh-aw v48) and world priority credited (Osmani/Cherny/Steinberger) · NO-MINT alternative recorded operator-reviewable · max #85 · counts 46/11 unchanged · no double-count (#84/#12/#20/#81 all no-bump) · README-vs-starters gaps + narrative-vs-real-story distinction caught and documented.
