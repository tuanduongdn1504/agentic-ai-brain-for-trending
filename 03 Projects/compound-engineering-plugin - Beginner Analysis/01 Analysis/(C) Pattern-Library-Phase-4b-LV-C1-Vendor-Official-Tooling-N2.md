# (C) Pattern Library — Phase 4b — compound-engineering-plugin (v126)

**Routine:** v2.5. **Ship:** 2026-06-01. **Net Pattern Library state change: NONE** (46 confirmed / ~25 active / 8 Library-vocab CONFIRMED unchanged). **ZERO new Library-vocab standalones** (§28 disciplined) — the contribution is **two N=2 strengthenings** (LV-C1 + LV-C3) + administrative counts. **No new top-level Pattern; no promotions at ship.**

> **§28 compliance:** the two strengthenings below are *actually written into* `_patterns/06-library-vocab-registry.md` this session (rule 5 — "filing is an act, not a claim"). No loose narrative minting.

---

## PRIMARY — LV-C1 "Vendor-Official Agent-Tooling for Own Product/Offering" → N=2

**The observation.** v114 gsap-skills registered the CORPUS-FIRST item *"Product-Vendor Official Agent-Skills for Own Product"* (GreenSock shipping official skills teaching agents to use GSAP, its own animation library). v126 compound-engineering is a 2nd instance with a meaningful sub-distinction:

| | v114 gsap-skills | v126 compound-engineering |
|---|---|---|
| Vendor | GreenSock (software-library vendor) | Every (methodology / editorial-media vendor) |
| "Own product" | a JS animation **library** | a named **methodology** (Compound Engineering) + the every.to editorial brand |
| The official tooling | skills teaching agents to *use the library* | a plugin that *is the executable form of the published methodology* |
| Go-to-market tie | DevRel for the library | drives readers to every.to guides/essays |

**Both = a vendor shipping official agent-tooling for its own offering**, as a DevRel/go-to-market channel → **LV-C1 N=2, promotion-eligible at N=3.**

**Honest sub-distinction (load-bearing, flagged for the next audit):** v114 is a *software-library* vendor; v126 is a *methodology/editorial* vendor whose "product" is the methodology itself. These may be one theme or two sub-types — if a 3rd instance arrives, decide whether to promote a unified "vendor-official-tooling-for-own-offering" item or split a `library-vendor` vs `methodology/editorial-vendor` sub-typology. **Not decided at ship** (an audit call). Filed to registry **LV-C1**.

**Why this is PRIMARY (and why it is *not* a new standalone):** for a subject this significant (18,810★, the most goal-aligned subject in the recent corpus), the honest Pattern-Library headline is a **strengthening, not a mint** — exactly the v119/v124 calibration. The subject's real value to the corpus is being the **first strong GOAL-ALIGNED pilot under v2.5**, not a novel pattern. Saying so plainly (rather than minting a standalone to manufacture novelty) is the §28-disciplined outcome.

---

## SECONDARY — administrative / strengthening (no new mint)

- **agentskills.io 57k Standards-Conformance chain → 13th implementer.** 44 `SKILL.md` in-repo (compound-engineering plugin's `ce-*` skills + a 2nd `coding-tutor` plugin + test fixtures). Follows v124's 12th. **CONFIRMED N=3 unchanged** — a count, not a state change.
- **Pattern #84 cross-vendor ecosystem-tolerance + 84c provider-agnostic-by-design + "CLI-generates-native-formats-from-canonical-source" (v76 sub-mechanism) N+1.** Per-harness `.claude-plugin/`/`.codex-plugin/`/`.cursor-plugin/` `plugin.json` manifests + the `bunx @every-env/compound-plugin install … --to [target]` converter targeting 8+ harnesses (Claude/Cursor/Codex/Copilot/Factory/Qwen/OpenCode/Pi/Gemini/Kiro). One of the strongest multi-harness instances in the corpus.
- **Claude Code plugin marketplace → v95 claude-plugins-official sibling.** Three `marketplace.json` (`.claude-plugin/` + `.agents/plugins/` + `.cursor-plugin/`) hosting 2 plugins (compound-engineering + coding-tutor).
- **Pattern #80 Tool-Level Adversarial Review** — `/ce-code-review` is multi-agent; in-repo `2026-03-26-001-feat-adversarial-review-agents-plan.md`. The corpus's own adversarial-verify methodology, productized into a shipped tool.
- **LV-C3 "Self-Referential-Methodology-Demonstration" → N=2** (v87 + v126). The repo dogfoods its own loop: `docs/brainstorms/*` + `docs/plans/*` are the actual brainstorm/plan artifacts for the plugin's *own* features (≈28 brainstorms + ≈40 plans). Filed to registry **LV-C3**.
- **Pattern #52 SUSTAINED-MODERATE-HIGH N+1** (~80★/d × 235d). **Library-vocab #12** routing artifacts (AGENTS.md + CLAUDE.md + .claude-plugin/ + .agents/ + .cursor-plugin/). **agentmemory v66 / Pattern #85** connectivity (in-repo `auto-memory-integration` brainstorm + plan). **Pattern #82** quantitative-marketing (80/20 ratio; "37 skills + 51 agents").

---

## Honest non-claims (load-bearing)

1. **(a) FAILS** — Every is a US media/software org, not a cultural-peer and not (a)-7. Not laundered.
2. **ZERO new Library-vocab standalones; NO new top-level Pattern; no promotions at ship.** Two N=2 strengthenings (LV-C1, LV-C3) + administrative counts. The §28-disciplined result for a high-value-but-not-pattern-novel subject.
3. **NOT a 3rd instance of Pattern #57 "corpus-recursive-at-methodology-influence"** (currently N=2, v94 consume + v118 generate — both the *Karpathy-LLM-wiki* pattern). "Compound engineering" shares the *compounding* theme with the vault's premise but is a **different methodology** (compounding *engineering work* via planning/review/codification, not a knowledge-wiki). Noted as **conceptual adjacency only** — explicitly not counted toward v94/v118's pattern. Over-claiming this would be exactly the kind of manufactured-promotion the v125 audit warned against.
4. **README "37 skills / 51 agents" vs repo "44 SKILL.md / 46 agent .md":** the README counts the compound-engineering plugin (stable/beta subset); the tree counts all SKILL.md across both plugins + test fixtures. Both reported honestly; no single inflated number asserted.
5. **Filing is done, not claimed** — registry LV-C1 + LV-C3 edited this session.

**Storm Bear's blunt take.** The Pattern-Library haul is deliberately small and honest: two N=2 strengthenings and a 57k count, no mint, no promotion — because the subject's real significance isn't a new pattern, it's that it's the **first genuinely GOAL-ALIGNED pilot under v2.5** and the most directly-usable software-dev subject in the recent corpus. v2.5 exists precisely to make that distinction visible: after a run of off-goal captures (voice-AI, faceless-video, a Codex business-ops kit), this one PASSes (b) at STRONGEST and routes to GOAL-ALIGNED. The one temptation I refused: calling "compound engineering" a 3rd instance of the Karpathy-LLM-wiki corpus-recursive pattern — it rhymes (compounding!) but it's a different methodology, and inflating it would undo the v125 audit's whole point. The real next move isn't another wiki — it's installing this one and running a loop.
