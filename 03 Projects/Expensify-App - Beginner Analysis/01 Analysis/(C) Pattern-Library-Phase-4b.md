# (C) Expensify/App — Pattern Library Phase 4b (v152)

**Outcome: NO Pattern Library state change. ZERO new Library-vocab minted. Registry 06 NOT moved. NO sub-archetype. NO new top-level Pattern.**

Off-goal captures are **corpus-knowledge, not pattern-fuel** (the v128 / v129 / v133 / v146 / v148 discipline). Authoritative state is **UNCHANGED**: **46 confirmed patterns / 9 Library-vocab CONFIRMED** (post-v151-audit head; #21 promoted at the v151 audit). Everything below is recorded as an **observation only** — not minted, not filed to registry 06.

## Observations (NOT minted)

1. **Claude-Code-as-internal-dev-exhaust thread → N=4 — and v152 is the RICHEST instance.**
   - Members: v122 dograh (CLAUDE.md=@AGENTS.md + hierarchical AGENTS.md + drift-guard/PR-audit skills) + v128 Echo Loop (`.claude/`+AGENTS.md+CLAUDE.md) + v142 OrcaSlicer-bambulab (`.claude/`+CLAUDE.md+AGENTS.md) + **v152 Expensify**.
   - v152 is by far the richest: a **317-line `CLAUDE.md`** operating manual + `AGENTS.md` redirect + **`.mcp.json`** + **four named skills** (`/agent-device` device-driving interactive testing, `/playwright-app-testing`, `/react-native-best-practices`, `/Sentry`), at a **263k-commit production org**.
   - Still an **observation, not a mint** — by the v122 discipline this thread documents "real teams adopt Claude-Code tooling internally," which is engineering exhaust, not the subject's product. A future audit *could* consider whether N=4 (with v152 as the strong anchor) warrants a recorded observation-track for "production-scale internal Claude-Code operating-manual adoption" — flagged, **not** acted on here (off-goal subject → no pattern-fuel).

2. **Pattern #18 B1 MCP** — `.mcp.json` present = another B1 instance (the B1 line is already dense: v66/v70/v119/v132/v134/v140/v141/v144/v149 + now v152). N+1 note only; the MCP-server tier-sub-archetype review remains OVERDUE (v149 finding) — **not** advanced by an off-goal subject.

3. **Pattern #84** — multi-skill internal tooling (4 skills across one repo) = a thin N+1 instance note; not minted.

4. **§35 / off-goal-rate signal (the dominant intake finding).** v152 is the **3rd `[ceiling-override]`** (v146, v148, v152) and the **10th lifetime / 7th-forward operator override**. Off-goal-capture rate under v2.5/v2.6 ≈ **11-of-26 (≈42%)**. The override door remains the de-facto intake for (a)-FAIL off-goal subjects — exactly the v125/v130/v134-135/v151-audit common-cause: the lever is **upstream subject-selection + piloting**, not accounting. (Recorded; the next audit — already RE-MANDATED by 7-in-20 — owns the decision.)

5. **NOT a Pattern #52 claim.** ≈4.9k★ against **263,873 commits** → commits ≫ stars (same shape as v151 hongbomiao); velocity/age unestablished; metrics page-stated, not API-verified (§37.4). No #52.

6. **Engineering-reference notes (orthogonal-domain, observation only):** HybridApp (NewDot + OldDot via Mobile-Expensify submodule), Onyx offline-first optimistic state, React Compiler auto-memoization, `eslint-seatbelt` grandfathered-violations mechanism, dual typecheck paths (`tsgo` dev / `tsc` CI). Genuinely instructive RN-at-scale; not goal-#1 pattern-fuel.

## §28 accounting
- New standalones filed: **0** (cap 2; off-goal → none).
- Cluster strengthenings filed: **0** (the N=4 dev-exhaust thread is an observation track, not a registry cluster).
- Registry 06 edited: **NO** (correct for an off-goal capture — rule-5 "filing is an act" applies only when something is actually being minted/strengthened; nothing is).
- Confirmed counts UNCHANGED: **46 patterns / 9 Library-vocab CONFIRMED.**
