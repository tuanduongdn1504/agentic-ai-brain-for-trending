# (C) Ghost-Downloader-3 — Verdict

> **Wiki v219 · 2026-07-18 · `XiaoYouChR/Ghost-Downloader-3`** · author-generated (`(C)`).
> Verdict produced **INLINE + fully hand-verified** per `feedback_wiki_verify_independently_check_collisions` — **no workflow / no subagent** (the ~205K shim overflows subagent context → prompt-too-long; the v200→v218 self-throttle). Source hand-fetched; identity + landscape by WebSearch; collision by sanity-anchored hand-grep.

---

## Headline

**⚠️ OFF-GOAL CAPTURE INCLUDE 3/4 — [(a) FAIL · (b) FAIL keys the tier · (c) STRONG · (d) MODERATE]. NO MINT. Counts UNCHANGED 46/11.**

A well-engineered **consumer download manager** whose "AI-boost" headline is **AI-washing** (an adaptive-chunking heuristic; zero ML libraries; no README elaboration). It provides **zero Claude / agent / software-dev substrate** and is **off both goals**. Recorded OFF-GOAL CAPTURE (operator-requested — the intake-channel capture track, no override consumed), the **v216 Codex-Dream-Skin situation**. A reviewable §40-GOAL-ALIGNED alternative is recorded below.

---

## The four criteria

### (a) FAIL — cleanly

**XiaoYouChR** = a disclosed 18-year-old Chinese university student (Wuhan), on their **first Python project**. Not Anthropic; no registered (a)-7 vendor-direct source. Per **§41**, (a) PASSES only on a declared Anthropic affiliation or a registered (a)-7 source — no name / heritage / locale / notability / disclosed-individual rescue (the hallmark v204 [El Mghari] / agent-skills v184 [Osmani] / ui-skills v218 [Thibeaut] handling). A Chinese-locale inference is explicitly **not** an (a) signal (the v159→v218 discipline). **#19 19a** first `XiaoYouChR` author (a bookkeeping data-point).

### (b) FAIL → OFF-GOAL CAPTURE — keys the tier

This is the load-bearing call. The subject provides **no Claude / agent / agent-infra / agent-skill / MCP / LLM-architecture / coding-capability / methodology substrate**:

- It is a **human-facing GUI download manager**. Not Claude, not an agent, not a coding tool, not recruitment.
- The **"AI-boost" is marketing** (§5 of the Deep Dive): the README gives no technical explanation, and `pyproject.toml` has **zero** ML dependencies (`openai`/`anthropic`/`transformers`/`torch`/`onnx`/`sklearn`/`langchain` all absent). The "AI" is adaptive chunking — a ~20-year-old download-acceleration heuristic, rebranded.

It sits **below the (b) MODERATE floor** held by the recent off-domain-but-goal-adjacent INCLUDEs — each of which had a **real** architectural hook that this subject lacks:

| Subject | (b) MODERATE hook that Ghost-Downloader lacks |
|---|---|
| GLM-5 v176 | a frontier LLM routable as a Claude-Code backend + benchmarked-vs-Claude |
| AIRI v210 | a provider-agnostic 30+-LLM seam (`xsAI`) + an MCP Launcher |
| geti v213 | a first-party **agent-skill suite** (a hireui agent-nativity template) |
| wardrobe v217 | a vision→structured-JSON→QA **pipeline** (a hireui CV-parse template) + a genuine **agent skill** |

Ghost-Downloader has **none** of these. Its only conceivable hooks are all **thin**, and none is agent-facing:

1. **"It's genuinely-engineered async Python software you could read"** — true of literally any well-built OSS repo; Goal #1 is specifically *Claude and autonomous agents for software development*, not "software engineering in general." Thin.
2. **TLS-fingerprint emulation** = the same *technique class* as camofox v179 / Scrapling v149 — but used for **its own downloads**, not delivered to agents. A technique cross-ref, not a substrate. Thin.
3. **aria2-compatible RPC** makes it programmatically drivable — an agent *could* call it — but it's built for humans, ships no MCP, and downloading-a-file-for-a-person isn't a Goal-#1/#2 capability. Thin.
4. **yt-dlp / media parsing** overlaps in *dependency* with video-use v198 / Agent-Reach v174 / meetily v196 — domain-adjacent, not substrate. Thin.

Because the hooks are thin and non-agent-facing, **§40 does NOT apply** (its explicit carve-out: *"truly off-everything subjects still use OFF-GOAL CAPTURE"* — the v216 discipline). → **(b) FAIL → OFF-GOAL CAPTURE.**

> **⚠️ Is this more or less off-goal than v216?** At least as off. v216 (Codex-Dream-Skin) was at least **inside the AI-coding-tool ecosystem** (it themed the Codex desktop app, a Claude-Code competitor) → two thin hooks (a CDP-injection technique + Codex-ecosystem adjacency). Ghost-Downloader is a **consumer download utility, outside the AI-coding ecosystem entirely** → its Goal-#1 hooks are arguably even thinner. So OFF-GOAL CAPTURE is the honest primary call, exactly as at v216.

### (c) STRONG — with honest caveats

A genuinely substantial, mature, well-built application — **materially more so than v216's cosmetic skin** (arguably the strongest (c) of the recent OFF-GOAL captures):

- **61 releases**, v4.1.1, ~4 years of development, **cross-platform incl. Android**.
- A clean **multi-protocol async architecture** (HTTP / BT / magnet / FTP / M3U8 / MPEG-DASH / eD2k / QUIC-HTTP3) on `uvloop`/`winloop`.
- Real engineering deps: PySide6 + PyQt-Fluent-Widgets + libtorrent + FFmpeg + N_m3u8DL-RE + `wreq` (TLS fingerprinting) + nuitka + Inno Setup.
- A **TypeScript browser extension**, an **aria2-compatible RPC interface**, dedicated GitHub/HuggingFace/YouTube/Bilibili parsers, a coming plugin system.

⚠️ **Caveats:** the hard bits (BT, TLS-fingerprinting, HLS/DASH, extraction) are **upstream libraries** it orchestrates, not novel work; the **"AI-boost" is marketing, not real AI** (zero ML deps); a **solo 18-year-old's first Python project** (bus-factor 1); **NOT source-cloned** (WebFetch/README/pyproject/landscape only — the v200→v218 self-throttle); stars **page-stated §37.4** → **NOT #52**.

### (d) MODERATE

Off the core threads; the cross-refs are **technique/domain-adjacent, not substrate**:

- **Anti-detect / fingerprint-spoofing technique family** — camofox v179 (agent stealth browser) / CloakBrowser v69 / Scrapling v149 (scraping) / crawl4ai v29 / opencode-antigravity-auth v67. Ghost-Downloader uses the technique for **its own** downloads → a cross-ref, **NO N-bump**.
- **yt-dlp / media-handling shared-dependency** thread — video-use v198 / Agent-Reach v174 / meetily v196. A shared-dep cross-ref, **NOT #57**.
- **AI-washing observation** — a real-AI-vs-marketing calibration example (the §37 fact-provenance discipline + the many "unverified marketing" NON-claims).
- **PySide6 / Qt-Fluent desktop-app** UI-framework data-point — the corpus's **first PySide6/Qt-Fluent subject** (cc-switch v73 / CodexPlusPlus v117 / meetily v196 are **Tauri**; a different framework → a UI data-point, not a class).
- **Chinese solo-dev OSS** cluster; the **aria2-RPC interop** data-point.

Not STRONG — none of these is on the Claude/agent-substrate core.

---

## Pattern outcome: NO MINT

- **No new top-level pattern** (max stays **#85**).
- **No §C standalone.** "Download manager" is a **DOMAIN, not a mintable agent-capability class** (§C vocab is agent-capability-shaped — the meetily v196 / TimesFM v193 / geti v213 / Codex-Dream-Skin v216 domain-not-capability discipline). Collision-clean by sanity-anchored hand-grep (0 subject hits for Ghost-Downloader / XiaoYouChR / aria2 / libtorrent / bittorrent; the lone `_state/03c` "downloader" hit = the cortex-hub v181 phrase *"not a binary downloader"*; anchors camofox / scrapling / yt-dlp all hit → grep works) → the corpus's **first download-manager subject**. But corpus-first-for-a-DOMAIN ≠ a §C mint, **AND NOT world-first** (IDM / aria2 / JDownloader / FDM / Motrix / XDM / Persepolis + "AI-marketed" downloaders saturate the genre) → a "corpus-first AI download manager" mint = **§28 phantom-count inflation** on an off-goal, non-world-first consumer app → **DECLINED**.
- Recorded as a **corpus-knowledge data-point**; **no new watch axis** (a saturated consumer genre with no agent angle; the v217 "no new watch axis" restraint applies harder here — lean *don't carry*).
- **Counts UNCHANGED 46/11**; §C live standalones **44** unchanged; §C surface **≈51** unchanged.

**SECONDARY (NOT minted):** #19 19a first XiaoYouChR author + first PySide6/Qt-Fluent-desktop subject · TLS-fingerprint/anti-detect **technique** cross-ref (NO N-bump) · yt-dlp/media shared-dependency cross-ref (NOT #57) · aria2-RPC interop data-point · **AI-washing** observation · #66 supply-chain/legal (GPL-3.0; browser extension + libtorrent/FFmpeg/yt-dlp/wreq deps + Inno-Setup installer + nuitka binaries; arbitrary-content download = piracy/legal gray zone; NOT source-cloned → benign-to-moderate fence).

**NON-claims:** NOT #52 (~6.7k★/61 releases/v4.1.1 page-stated §37.4; Trendshift-recognized but a ~4-year mature project, not a fresh burst → velocity unestablishable) · NOT #57 (yt-dlp/libtorrent/FFmpeg/PyQt-Fluent-Widgets/N_m3u8DL-RE/wreq deps — none corpus subjects; the fingerprint-spoofing is a technique class, not a corpus-subject citation) · NOT #18 B1-MCP (aria2-RPC + browser extension; ships **NO MCP server**) · NOT world-first · NOT corpus-first for a mintable class · **NOT a real AI/LLM/agent subject** (the "AI-boost" = AI-washing) · NOT source-cloned (flagged).

---

## Tier

**T5 Application** — consumer utility, download-manager flavor (the Codex-Dream-Skin v216 / MoneyPrinterTurbo v123 / meetily v196 tier).

---

## Streak & §35

The 2-consecutive-GA post-v216 run (**v217 GA + v218 GA**) **BREAKS** →

**`GA:77 · OG:13 [7 ov]`** (OFF-GOAL CAPTURE, operator-requested — no override consumed).

**§35 CLEAR** — window {**v217 GA, v218 GA, v219 OG**} = **1 OG ≤ 1**.

> **⚠️ Reviewable §40-GA alternative:** if the operator elects to treat the thin hooks (genuinely-engineered software / the TLS-fingerprint technique adjacency / aria2-RPC agent-drivability) as **(b) MODERATE** under §40 (operator-requested goal-adjacent), it flips to **`GA:78 · OG:12`** and the GA run continues (3 consecutive GA). Recorded **OFF-GOAL primary on the merits** — the hooks do not reach the (b) MODERATE floor that GLM-5/AIRI/geti/wardrobe held.

---

## Verification note

Produced **INLINE + fully hand-verified** (no workflow/subagent — the shim overflows subagent context). Source hand-fetched (repo page + raw `README.md` + `pyproject.toml` + Releases + identity/landscape WebSearch). **AI-washing established by hand:** README gives no "AI" definition + `pyproject.toml` has zero ML deps. **Collision by sanity-anchored hand-grep:** 0 subject hits; the "downloader" hits are incidental phrase-matches; no download-manager §C standalone exists; anchors (camofox/scrapling/yt-dlp) hit → grep confirmed working. `inflation_check` HELD (0 mints; §C DECLINED per §28 domain-not-capability + not-world-first; counts 46/11 unchanged; max #85; no N-bumps).

---

## Bottom line

**A good download manager that is not about what this vault is about.** Built the full wiki because it was operator-requested; catalogued honestly as OFF-GOAL CAPTURE. There is **nothing to pilot** into hireui or the Claude/agents practice. The only take-aways are a 5-minute read (see the Pilot Methods Menu) — then pick an on-goal subject next.
