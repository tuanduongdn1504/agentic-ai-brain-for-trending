# (C) lazyagent — Phase 0.9 verdict (v165)

**Subject:** `illegalstudio/lazyagent` (`nahime0`) — terminal UI + macOS menu-bar app + HTTP API + iOS companion for **monitoring** AI coding agents (Claude Code, Cursor, Codex, Grok CLI, Kilo, Kimi, Amp, pi, OpenCode). Go 91.3%; MIT; ≈165★ / 373c / v0.12.5 (NOT API-verified §37.4).

**Routine:** v2.6. **Verdict: GOAL-ALIGNED INCLUDE 3/4.**

## Criterion-by-criterion

### (a) Cultural-peer / (a)-7 vendor-direct — **FAIL**
`nahime0` / `illegalstudio` is a bare handle + org name + an X handle. **No disclosed real name, affiliation, or location.** Per the v139 / v159-steipete / v160-ping-island / v164-rainnoon discipline (and the v151-audit rec (ii) — "require a *solid* (a) signal, not a name/heritage inference"), a bare handle yields **no cultural-peer inference** and is **not** an (a)-7 substrate vendor the vault depends on. **No axis minted.** "illegalstudio" is a brand name, not a geographic/cultural signal.

### (b) Goal-relevance — **STRONG (not STRONGEST)** → routes to GOAL-ALIGNED INCLUDE
Monitoring Claude Code + 8 peer agents (live activity / token usage / cost / sessions) from one place is **dead-center goal #1** ("master Claude and autonomous agents for software development") AND a real heavy-Claude-Code vault workflow (you run parallel agents to build these wikis; "what are all my agents doing + how much am I spending" is a genuine need).

**STRONG, not STRONGEST,** because it is an **observability surface *around* the agents**, not the agent substrate (Claude/Anthropic) or a methodology (v126). This is the exact calibration held at v154 agentpet, v157 ClaudeBar, v158 ai-token-monitor, v159 CodexBar, v160 ping-island, v164 oc-claw.

### (c) Engineering quality — **STRONG**
Go core (91.3%) + Wails menu-bar GUI + Bearer-token HTTP API + iOS App Store companion = four delivery surfaces from one codebase. Maintenance commands ship with dry-run previews + automatic backups (a safety discipline). Mature for its star count: **373 commits / v0.12.5**. Clean "watch, don't replace" architecture (no server, no account, observational).

### (d) Corpus connectivity — **STRONG**
- The **v158-CONFIRMED "AI-Agent Observability / Status-Surface Tool" sub-archetype** — sub-flavor (a) metering/usage-logs (real-time tokens/cost + `limits`).
- Pattern #84 84c cross-tool (9 agents, agent-agnostic).
- The **lazy\*-TUI lineage** (self-credited: lazygit / lazyworktree / pixel-agents).
- HTTP-API-for-clients + iOS remote-monitoring delivery axis (observation).
- The transcript-maintenance capability — nearest neighbor v144 headroom (distinguished).

## §35 ceiling
Window {v163 GA, v164 GA, **v165 GA**} = 0 OG → **STAYS CLEAR.** 13 consecutive goal-aligned ships v153→v165. NOT an override (override-frequency UNCHANGED, lifetime 10).

## Streak
GA:27 · OG:11 [7 ov] → **GA:28 · OG:11 [7 ov]** (28th GOAL-ALIGNED PASS; NOT an override).

## Honest non-claims
- (a) genuinely FAILS (no identity, no handle-inference, no axis).
- (b) STRONG **not** STRONGEST (surface around the agents).
- Subject overall is a **clean observability instance** (0 mints at the sub-archetype layer) — the v159/v164 shape.
- The ONE mint (transcript-maintenance §C standalone) is the *capability*, not a re-classification of the subject (the v158 shape).
- NOT a new observability sub-flavor (it's sub-flavor (a), not a 4th flavor).
- NOT LV-C7 #22 (observes, not controls) · NOT #18 #8 (no provider aggregation).
- NOT a #52 claim (creation date not shown; velocity unestablished; 165★ small).
- NO new top-level Pattern; NO confirmed-count change (46 / 10).

## ⚠️ Standing lever
13th consecutive "tool for operating/monitoring AI coding agents" ship (v153→v165), ZERO piloted; observability sub-archetype now N=11. The high-value next move is a **pilot** or the **overdue audit**, not a v166 in this niche (the v162/v163/v164 audits all said so).
