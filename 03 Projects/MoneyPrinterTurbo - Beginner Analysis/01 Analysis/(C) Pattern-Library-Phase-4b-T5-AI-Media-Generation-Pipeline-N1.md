# (C) Pattern Library — Phase 4b — MoneyPrinterTurbo (v123)

**Routine:** v2.4. **Subject:** `harry0703/MoneyPrinterTurbo` (faceless short-video generator). **Phase 0.9:** WEAK INCLUDE 1/4 ((a)-only). **Pattern Library state change: NONE.**

This is a WEAK off-goal-domain subject; the Phase 4b contribution is deliberately modest. No top-level pattern, no promotion, ZERO new Library-vocab. The genuine contributions are one structural sub-archetype candidate + within-pattern strengthening + one meta-observation surfaced to the mandated audit.

---

## PRIMARY — NEW T5 sub-archetype "AI Media-Generation Pipeline App" (PROVISIONAL N=1)

**Definition:** a standalone, self-hostable **application** whose core is an automated media-generation pipeline orchestrating an LLM (for content/script) plus media-processing stages into a finished media artifact. For MoneyPrinterTurbo: *topic/keyword → LLM script (13-provider pluggable) → stock-footage retrieval (Pexels/Pixabay-class royalty-free) → TTS voiceover (multi-voice, preview) → subtitles (Whisper model OR edge mode) → background music → MoviePy composite → 1080p 9:16 / 16:9 short video*, with batch generation, exposed as both a Streamlit WebUI and a FastAPI service (MVC).

**Why a NEW sub-archetype (and why N=1):**
- Distinct from **v84 image-blaster** (an image→3D *Claude-skill bundle* — a T1 agent-skill, not a standalone app).
- Distinct from the **v5x `automate-faceless-content`** subject (a markdown *course/tutorial*, OUTSIDE-SCOPE — teaches humans to use commercial SaaS; not an app).
- Distinct from T5 utility/application subjects like v80 SmartMacroAI (RPA), v100 Easydict (dictionary/translator), v9-class orchestration apps — none is an LLM-driven *media-generation pipeline*.
- So MoneyPrinterTurbo is the **corpus-first in-scope standalone AI media-generation pipeline application.** PROVISIONAL N=1. Promotion-eligible at N=2 if a second LLM-driven media-pipeline app appears in-scope; 5-wiki stale-watch ~v128; 15-wiki forced-retire ~v138 if still N=1.

**Honest scope caveat:** "AI media-generation pipeline app" is a fairly broad category; the N=1 registration is on the *structural form* (LLM-orchestrated multi-stage media pipeline shipped as a deployable app), not a claim of deep novelty. Pressure-test at the next audit per §15.

---

## SECONDARY — within-pattern strengthening (administrative; NO new standalones)

| Observation | Pattern | Note |
|---|---|---|
| 70,294★ / ~810 days = **~86.8★/day**, sustained | **#52 SUSTAINED-MODERATE-HIGH** (25–150/d) N+1 | One of the largest *cumulative* subjects in the corpus (70K★) at a *moderate* daily rate over a very long window — near the **LONG-SUSTAINED-MEDIUM** (60–150/d × 1000+d) boundary; at ~190 more days it would qualify for that sub-class. Worth a velocity-profile note: mega-cumulative ≠ high daily-rate. |
| 13 LLM providers via `config.toml` | **#84 Cross-Vendor Ecosystem-Tolerance** N+1 | OpenAI/Moonshot/Azure/gpt4free/one-api/Qwen/Gemini/Ollama/DeepSeek/MiniMax/ERNIE/Pollinations/ModelScope. Consumer-app multi-provider config. |
| inferred China-Mainland solo indie | China-Mainland sub-cluster N+1 | v82/v94/v100/v117/v119 + v123. Administrative. |
| "一键生成高清短视频" / "one-click" | **#82 Quantitative-Marketing** | Thin; the README is feature-list-heavy, not claim-dense (no "20× faster"-style superlatives). |
| MIT (LICENSE + API agree) | **#45** | Clean single license. |

---

## §28 — ZERO new Library-vocab minted (deliberate)

A WEAK off-goal-domain subject should not expand the methodology vocabulary. Registry `_patterns/06-library-vocab-registry.md` is **NOT moved** by this ship. The cap is ≤2 new standalones; the disciplined count for this subject is **0**. No cluster filings warranted (the multi-provider config is a #84 instance, not a new vocab item; the velocity profile is a within-#52 note).

---

## The one meta-observation — surfaced to the mandated ~v124 Phase 0.9 audit (NOT minted)

**"(a)-cultural-peer rescue of an off-goal-domain subject into WEAK-INCLUDE-on-(a)-alone" — N=2:**
- **v80 SmartMacroAI** — Windows RPA tool, (b)(c)(d) FAIL, (a) PASS (VN-LOCATED solo) → WEAK INCLUDE 1/4.
- **v123 MoneyPrinterTurbo** — faceless-video app, (b) FAIL + (c)(d) WEAK, (a) PASS (inferred China-Mainland) → WEAK INCLUDE 1/4.

Both are off goal #1; both clear the gate *only* because the author is an Asian-cluster cultural-peer. This is the (a)-PASS counterpart to the override channel (v84/v116/v122, where (a) FAILs and the operator force-includes). The ~v124 audit — already MANDATED to review Phase 0.9 STRICT after v122's 2-in-20 override trip — should weigh both channels together: **the corpus is absorbing notable-but-off-goal subjects via override (a-fails) AND via (a)-rescue (a-passes).** Candidate resolution: a lightweight **corpus-knowledge-outlier track** distinct from goal-aligned INCLUDEs (so neither channel quietly pads the streak/INCLUDE-rate with off-goal entries). Tracked here + in the verdict doc + the shim's next-audit-triggers; deliberately not minted as Library-vocab per §28.

---

## Honest non-claims

- **NOT** a Pattern #18 #8 "Multi-Source LLM Aggregator" (consumer app with multi-provider config ≠ dedicated aggregator/proxy/switcher; would dilute #8's just-CONFIRMED-N=3).
- **NOT** an agent / skill / Claude-Code / agentskills.io / MCP subject (zero agent artifacts at repo root, verified).
- **NOT** a Karpathy LLM-wiki lineage subject.
- **NO** new top-level Pattern; **NO** Library-vocab promotion or new standalone; **NO** Pattern Library state change.
- **NOT** a pilot. WEAK INCLUDE = corpus-knowledge of a popular cultural-peer subject, not goal-#1 progress.
