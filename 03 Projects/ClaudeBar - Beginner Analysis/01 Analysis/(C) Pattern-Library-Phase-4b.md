# (C) ClaudeBar — Pattern Library Phase 4b (v157)

**Outcome: NO change to confirmed counts (46 patterns / 9 Library-vocab CONFIRMED). PRIMARY = T2 observability sub-archetype → N=4 + metering sub-flavor → N=3 (a tier-sub-archetype strengthening, NOT a mint). §28: 0 new standalones; registry 06 §F running-log noted (rule-5).**

## PRIMARY — T2 "AI-Agent Observability/Metering Tool" sub-archetype → N=4 (metering sub-flavor → N=3)
- Sub-archetype members now (N=4): **v89 VibeCodingTracker** (CLI usage+cost metering) + **v109 cclog-cli** (log/metrics CLI) + **v154 agentpet** (live ambient pet) + **v157 ClaudeBar** (menu-bar real-time quota gauges).
- The genuinely valuable result: the sub-archetype now splits cleanly into **two sub-flavors, both at N=3**:
  - **Metering / usage / quota** (analytical, numbers): v89 + v109 + **v157 ClaudeBar** → **N=3.**
  - **Ambient / live-status (desktop-pet)** (affective, glanceable): v154 + v155 + v156 → **N=3** (this is the separately-tracked "desktop-pet-status-surface" standalone).
- Both sub-flavors at N=3 → strongly **PROMOTION-ELIGIBLE.** Recommend the audit (a) CONFIRM the T2 observability sub-archetype, and (b) formalize the two sub-flavors.
- **Layer note:** tier-sub-archetype (tracked in state + audit queue), not a Library-vocab registry-06 §C row.

## ⚠️ Honest overlap flagged for the audit (do NOT double-count)
The **ambient-pet sub-flavor** of this observability sub-archetype (v154/v155/v156) **is the same set of subjects** as the separately-tracked Library-vocab standalone "Ambient/Affective Multi-Agent Status Surface (reactive desktop-pet UI)" (N=3). These are two lenses on one set, not two independent N=3 findings. The audit must reconcile: is the pet-surface a **Library-vocab standalone**, OR the **ambient sub-flavor of the T2 observability sub-archetype**, or both-recorded-with-a-cross-ref? Recorded here so the audit doesn't bank it twice.

## Observations (NOT minted)
1. **Swift-native-macOS cluster N+1** — v99 cmux + v100 Easydict + v154 agentpet + v157 ClaudeBar (all Swift-primary native macOS). Observation (heterogeneous purposes); not a minted cluster.
2. **Pattern #84 cross-tool** — 10 providers (Claude/Codex/Gemini/Copilot/Antigravity/Z.ai/Kimi/Kiro/Amp/OpenCode-Go). N+1 instance note.
3. **v153 ai-switcher quota adjacency** — ClaudeBar *observes/displays* quota; v153 ai-switcher *manages + auto-switches* on quota exhaustion. A clean **observability-vs-management** split (do not conflate). Observation.
4. **LV-C4 high-release-cadence** — 106 releases / v0.4.65 (cf. v89 VibeCodingTracker's high-release-cadence; ClaudeBar is not micro-scale at 1.2k★, so it's a cadence note, not the micro-scale variant). Cluster LV-C4 instance note.
5. **`.claude/skills/add-provider/` TDD skill** — the team's internal product-extension tooling (TDD-based, fitting the `tddworks` org). Thin; the integration/extension layer, lightly product-relevant. Observation, not the v122 dev-exhaust thread (this is goal-aligned + the skill extends the product).
6. **`.itermcolors` theme import** — minor UX detail (imports terminal color themes). Observation.
7. **NOT a Pattern #52 claim** — 1.2k★ but creation date unstated → velocity/age unestablished; metrics not API-verified (§37.4).
8. **Niche-saturation meta-signal (now 5):** v153 ai-switcher + v154 agentpet + v155 openpets + v156 clawd-on-desk + v157 ClaudeBar = **5 consecutive "manage/monitor AI coding tools" ships, ZERO piloted.** ClaudeBar is the analytically most-useful of the run (it completes the metering sub-flavor N=3) — but the audit is now well past due with FOUR ripe promotion calls. Observation for the operator.

## §28 accounting
- New standalones filed: **0** (PRIMARY is a tier-sub-archetype strengthening).
- Strengthenings: **1** (T2 observability sub-archetype N=3 → N=4; metering sub-flavor → N=3).
- Registry 06 edited: **§F running-log note added** (rule-5: record the ship's Library-vocab activity = 0 standalones, PRIMARY at the tier layer). No §C row (no new standalone).
- Confirmed counts UNCHANGED: **46 patterns / 9 Library-vocab CONFIRMED.** Tracked PROVISIONAL surface UNCHANGED ≈23. **Audit queue now carries FOUR ripe promotion calls:** LV-C7 Tauri-management-GUI N=3 (v153) · T2 observability sub-archetype N=4 / metering N=3 (v157) · ambient-pet sub-flavor N=3 = desktop-pet-surface standalone N=3 (v156) [reconcile overlap] · (plus the standing dense MCP-B1 review + 7-in-20 override review).
