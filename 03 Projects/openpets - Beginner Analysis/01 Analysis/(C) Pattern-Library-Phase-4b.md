# (C) openpets — Pattern Library Phase 4b (v155)

**Outcome: NO change to confirmed counts (46 patterns / 9 Library-vocab CONFIRMED). PRIMARY = the v154-minted standalone → N=2 (a strengthening, NOT a mint). §28: 0 new standalones; registry 06 ACTUALLY edited (rule-5).**

## PRIMARY — "Ambient/Affective Multi-Agent Status Surface (reactive desktop-pet UI)" → N=2 (PROMOTION-ELIGIBLE at N=3)
- Members: **v154 agentpet** (native-Swift menu-bar pet; integration via Unix-socket daemon + universal `agentpet run --` wrapper) + **v155 openpets** (Electron tray pet; integration via **MCP**).
- A genuine, fast **N=2** for the standalone minted exactly one ship ago — and **independent** (different author, different stack, not a fork): native-Swift+socket vs Electron+MCP. The shared corpus-first idea (a reactive pet/avatar as the primary multi-agent status surface) is now corroborated across two stacks → **PROMOTION-ELIGIBLE at N=3.**
- **Two integration sub-flavors recorded:** (a) **socket-daemon / CLI-wrapper** (v154 agentpet — instrument any CLI agent) vs (b) **MCP** (v155 openpets — agents push reactions over MCP). One-class-or-two (is the pet-surface one class with two integration mechanisms, or do the mechanisms matter?) is an N=3-audit question.
- **Action:** registry 06 §C row updated N=1 → N=2; PROMOTION-ELIGIBLE at N=3; time-aware stale-watch (≥15 wikis AND ≥30 days). **§28: a strengthening, NOT a new standalone → ≤2 cap used at 0.**

## Observations (NOT minted)
1. **Pattern #18 B1 MCP N+1** — openpets connects agents via MCP (Claude Code + OpenCode + Cursor + Pi + generic MCP). The B1 line stays dense; MCP-server tier-sub-archetype review remains OVERDUE (a separate audit item). Instance note only.
2. **T2 "AI-Agent Observability/Metering Tool" sub-archetype — ADJACENT, NOT hard-counted.** openpets shows live agent status (an observability facet), but its center is a **companion/affective surface with productivity abilities** (focus timers, break nudges, ambient check-ins). The affective-pet-surface standalone (PRIMARY) is its cleaner home; counting it as observability N=4 would double-count + over-claim. Recorded as adjacency. (The v154-introduced T2-observability-N=3 promote-or-split call is unaffected.)
3. **Electron (cross-platform) vs native-Swift (v154) contrast** — the desktop-pet-status-surface now spans both a native-macOS Swift stack (v154) and a cross-platform Electron stack (v155). Observation (supports the "one class, multiple stacks" reading).
4. **Pattern #84 cross-tool** — Claude Code + OpenCode + Cursor + Pi + generic MCP. N+1 instance note.
5. **#66 privacy-conscious local-IPC** — "automatic hook speech is static and local," local IPC by design. Observation.
6. **Claude integration surface** — ships Claude memory instructions + hooks + OpenCode plugins (the `.claude` + hooks integration). Observation (this is the product's integration layer, NOT incidental dev-exhaust — it's how the pet connects TO the agent).
7. **NOT a Pattern #52 claim** — ≈968★ but creation date unstated → velocity/age unestablished; metrics not API-verified (§37.4). More popular than v154 agentpet (40★) — noted, not a velocity claim.
8. **Niche-saturation signal (meta):** v153 ai-switcher + v154 agentpet + v155 openpets = **3 consecutive "manage/monitor AI coding tools" ships**, 2 of them desktop-pets — a dense capture of one small niche, with ZERO piloted. Observation for the operator, not a pattern.

## §28 accounting
- New standalones filed: **0** (PRIMARY is a strengthening of the v154 standalone).
- Strengthenings filed: **1** (the v154 standalone N=1 → N=2).
- Registry 06 edited: **YES** (rule-5) — the v154 §C row updated to N=2 with the openpets member + integration sub-flavors; §F running log updated.
- Confirmed counts UNCHANGED: **46 patterns / 9 Library-vocab CONFIRMED.** Tracked PROVISIONAL surface UNCHANGED ≈23 (a strengthening within an existing row, no new entry). PROMOTION-ELIGIBLE list now also carries the desktop-pet-status-surface standalone at N=2.
