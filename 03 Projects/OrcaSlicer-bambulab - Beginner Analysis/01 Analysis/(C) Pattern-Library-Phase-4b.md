# (C) OrcaSlicer-bambulab — Pattern Library Phase 4b (v142)

**Routine v2.6 · §28.** Off-goal capture (0/4 SKIP, 6th override) = **corpus-knowledge, NOT pattern-fuel** (the v116/v122/v127/v139 discipline). **NO mint, NO promotion, NO state change.**

## Pattern Library state change: NONE
46 confirmed / ~25 active / 8 Library-vocab CONFIRMED → **UNCHANGED.** ZERO new Library-vocab minted (§28; registry 06 **NOT** moved). NO sub-archetype, NO new top-level Pattern.

## Observations only (NOT minted)
- **"Claude-Code-as-internal-dev-tool exhaust" thread → N=3** (v122 dograh + v128 Echo Loop + v142 OrcaSlicer-bambulab). A repo ships `.claude/commands` + `CLAUDE.md` + `AGENTS.md` to *develop* an off-goal product (here a C++ slicer). The deliberate corpus discipline: this is the team's engineering exhaust, **not** product corpus-relevance — counting it as relevance would blow scope wide open (near-ubiquitous now). Logged as an observation that the *intake signal* "has a CLAUDE.md" is noise; NOT minted.
- **Override-door-keeps-catching-off-goal meta-signal.** 6 lifetime overrides; of the forward 3 (v127 portfolio-list, v139 image/game-art, v142 3D-slicer), all are off-goal products the operator found interesting. Directly relevant to the v125/v130 recommendation: **make a corpus-knowledge-outlier track the default for off-goal captures rather than spending override budget.** The 4-in-20 / 3-in-30 re-trip hands the now-overdue audit a concrete, repeated pattern.
- **Fork-lineage observation** (not a pattern): a community fork whose entire value-add is *re-enabling a capability the upstream/vendor gated* (BambuNetwork cloud printing). Interoperability/right-to-repair gray area; recorded in the wiki's lineage mapping, not minted.
- **Pattern #45 (license) thin note:** AGPL-3.0 throughout the Slic3r→PrusaSlicer→BambuStudio→OrcaSlicer→this-fork chain (copyleft lineage); a fork re-bundling the proprietary BambuNetwork plugin against an AGPL base = a license-boundary curiosity. Thin; observation only.

## Audit hand-off note
v142 adds NOTHING to mint. It DOES sharpen two items the overdue ~v139–v140 audit already owes:
1. **Override-frequency systematic-miss review** — now **4-in-20** (v122+v127+v139+v142) + 3-in-30. The working hypothesis (v125/v130) is confirmed-by-repetition: this is an *intake-channel/usage* issue (off-goal subjects routed through the override door instead of an outlier track), NOT a Phase-0.9 criteria defect.
2. **"Claude-Code-as-internal-dev-exhaust" N=3** — worth a one-line codification that "ships a CLAUDE.md/AGENTS.md" is explicitly NOT a (b)/(d) relevance signal (it's near-universal), to stop it reading as connectivity.
