# (C) Pattern Library — Phase 4b — dograh (v122): the 3rd-override observation (deliberately thin)

**Routine:** v2.4. **Subject:** `dograh-hq/dograh` (voice-AI platform). **Phase 0.9:** 0/4 SKIP → **operator OVERRIDE #3**. **Pattern Library state change: NONE.**

This document is intentionally thin. An out-of-scope subject contributes nothing to the agent/skill Pattern Library; manufacturing promotions here would be the exact verdict-laundering the routine forbids. The substantive contribution is META (the override event + the frequency trip), not a pattern.

---

## A. The substantive registration — META: 3rd override + first-ever 2-in-20 trip

**The 3rd operator-elected Phase 0.9 OVERRIDE** (v84 image-blaster → v116 Sana → **v122 dograh**), and the **first time the override-frequency redesign-trigger fires in corpus history**:

- **2-in-20 TRIPS** — v116 + v122 within the last 20 wiki-ships → per routine v2.3 §7 this **mandates** a Phase 0.9 STRICT systematic-miss-pattern review at the next audit (~v124–v125).
- **3-in-30 does NOT fire** — v84 is ~38 ships back; only 2 overrides in the last 30.
- Streak **"47+2\*" → "47+3\*"**; post-v116 clean-PASS run (4: v117/v118/v119/v121) reset to 0.

**Common-cause hypothesis for the audit:** all three overrides are *notable / high-signal repos that FAIL (a) cultural-peer + (b) goal-relevance*. The valve is being used to **capture interesting off-scope subjects**, not to rescue STRICT misses. That is an *intake-channel* question, not a *criteria* defect — so the likely audit outcome is NOT a Phase 0.9 criteria redesign but a decision about whether to formalize a lightweight **corpus-knowledge-outlier** track (distinct from INCLUDE, with no streak/override accounting) so this kind of deliberate capture stops consuming the override budget.

---

## B. Domain-agnostic / incidental observations (NO state change; all carry OVERRIDE-EVIDENCE-QUALITY-CAVEAT)

| Observation | Pattern | Strength | Note |
|---|---|---|---|
| README marketing density | **#82 Quantitative-Marketing** | MODERATE | "from zero to a working bot in under 2 minutes", "Self-host in 60s", "100% open source" — present but not as dense as Sana's. |
| `CLAUDE.md` = `@AGENTS.md` + hierarchical `AGENTS.md` tree | **Library-vocab #12** LLM-routing-artifacts | N+1 (incidental) | Real instance, but incidental dev-tooling at an out-of-scope subject. Does NOT change #12's CONFIRMED state. |
| `review-agents-md` drift-guard skill | **#81 Manifest-Drift — PROPER pole** | N+1 (incidental) | Anti-drift discipline as an agent-skill (hierarchy "no-drift" test) — a clean contrast to the v66/v113 "names-the-drift-and-drifts-anyway" *inverse* pole. Genuinely interesting; still incidental. |
| Product is "MCP-native" | **#18** MCP sub-mechanism | thin / tangential | Product-host flavor (voice agents → MCP tools), not an agent-for-coding MCP server. |
| SKILL.md format used for *internal* tooling | (agentskills.io format-adopter) | NOT a 57k implementer | Honest non-claim: internal dev-tooling, not a published product/library → does NOT extend the Pattern #57 57k product-implementer chain. |
| BYOK across LLM/STT/TTS; Pipecat-based; pipecat/vllm/langfuse forks | **#84** cross-vendor-tolerance / **#45** BSD-2 | thin | Voice-domain flavor. |

**§28 discipline honored:** ZERO new Library-vocab standalones minted (cap = 2; an out-of-scope subject should mint none). The two incidental items above (LLM-routing-artifacts, anti-drift) are *strengthening notes on existing CONFIRMED/anchored items*, filed as observations — not new registrations. (Per the §28 rule-5 reminder added at the v120 audit: "filing is an act, not a claim" — these are deliberately recorded as observations-only, with no edit to `06-library-vocab-registry.md`, because an out-of-scope override should not move the registry.)

---

## C. Honest non-claims

- **NOT** an agent-for-software-development tool; **NOT** a Claude-Code skill/plugin as a product; **NOT** an agentskills.io product-implementer.
- **NOT** a Pattern #52 velocity instance (~14/d sustained-low; fork_ratio 0.21 is notable but velocity is below all sub-class floors).
- **NO** new top-level Pattern; **NO** sub-archetype; **NO** Library-vocab promotion; **NO** Pattern Library state change.
- The internal Claude-Code tooling is a **genuine, noteworthy real-world adoption specimen** — but it is the repo's *engineering exhaust*, not its product, and is recorded as an observation, not as grounds for INCLUDE.

---

## D. The one thing actually worth carrying forward

If the next audit wants a *useful* takeaway from this override (beyond the frequency trip): dograh is a clean real-world example of **"agent-skills as internal engineering discipline at a non-agent product"** — hierarchical AGENTS.md + a drift-guard skill + a domain-PR-review skill + `@`-import CLAUDE.md. If a 2nd such specimen ever appears *as an in-scope subject*, that combination ("anti-drift AGENTS.md governance via a dedicated maintenance skill") would be worth a real Library-vocab registration. Today it's N=1 and incidental — noted, not minted.
