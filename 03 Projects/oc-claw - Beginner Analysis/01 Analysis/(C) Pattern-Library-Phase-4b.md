# (C) oc-claw — Pattern Library Phase 4b (v164)

## PRIMARY: clean instance-strengthening — observability sub-flavor (b) ambient-pet → N=4 / sub-archetype → N=10. **0 mints.**

`rainnoon/oc-claw` is a clean member of the **v158-audit-CONFIRMED "AI-Agent Observability / Status-Surface Tool" sub-archetype**, specifically **sub-flavor (b) — "Ambient / affective status-surface (a reactive desktop-pet/avatar whose state tracks aggregate agent run-state as the *primary* display)."**

- **Sub-flavor (b) → N=4:** v154 agentpet (native-Swift + Unix-socket/wrapper) + v155 openpets (Electron + MCP) + v156 clawd-on-desk (Electron + hooks/log-polling) + **v164 oc-claw (Tauri v2 + JSONL-polling/hooks/plugins)**. Four independent projects, four integration mechanisms.
- **Sub-archetype overall → N=10** (metering (a) N=5 = v89+v109+v157+v158+v159 · ambient-pet (b) N=4 · attention-routing (c) N=1 = v160 ping-island).
- **This is the v159 CodexBar shape: a clean instance that mints NOTHING new.** Top-level pattern count UNCHANGED at **46**; CONFIRMED Library-vocab UNCHANGED at **10**; **0 new §C standalones.**

**Why no mint (the v158/v159 anti-inflation test applied):** a clean instance of an existing sub-flavor mints nothing (that was v159 CodexBar for sub-flavor (a)); a genuinely-neither instance mints a sub-flavor (that was v160 ping-island for (c)). oc-claw is **squarely sub-flavor (b)** — an animated pet whose state IS the display — so it mints nothing. Its distinctive details are stack/UI/blend, not structure:
- **First Tauri-stack desktop-pet** (v154 native-Swift; v155/v156 Electron). Stack note — the v120 "archetype ≠ stack-popularity" discipline.
- **Notch-placed pet.** ping-island (c) also uses the notch; the notch is a shared UI-location, not a sub-flavor marker. oc-claw is still (b) (status-animation + hover-detail), not (c) (no approval-interrupt routing / act-in-place).
- **Pet + metering blend.** The OpenClaw detail panel surfaces token/call metrics — a mild (b)+(a) cross-sub-flavor blend. Observation, not a mint.

## Boundary lines (anti-conflation)
- **vs sub-flavor (c) (v160 ping-island):** (c) surfaces *actionable* interrupts (tool-call approval / input / review), handles approve/deny/answer **in place**, and routes focus back. oc-claw does none of that — it monitors + animates + hover-reveals. → (b), not (c).
- **vs Library-vocab #22 LV-C7 "Tauri-Desktop Management-GUI":** built on Tauri but **OBSERVES** (reads/surfaces agent run-state); does NOT **CONTROL** provider/account/config (v73/v117/v153). The v158 "Tauri-but-NOT-LV-C7" line (drawn for v158 ai-token-monitor). No subject sits in both.
- **vs Pattern #18 #8 Multi-Source LLM Aggregator:** routes/aggregates no LLM providers; reads agent state. Not #8.

## SECONDARY observations (NOT minted)
- **SSH-forwarding within the observability cluster → N=2** (v160 ping-island core-workflow remote-forwarding + v164 remote SSH connectivity). Promotion-eligible to a tracked capability-note at N=3; observation for now.
- **OpenClaw-first integration** (oc = OpenClaw; JSONL auto-discovery is the first-class path) + OpenClaw corpus-recursive (v149 Scrapling clawhub / v156 / v160) + Hermes corpus-recursive (v78 ECC / v112 freellmapi / v160).
- **Pattern #84 84c cross-tool** — 7 agents (OpenClaw + Claude Code + Cursor + Codex + Gemini CLI + OpenCode + Hermes).
- **Independent of v156 clawd-on-desk** — different author (`rainnoon` vs `rullerzhou-afk`/鹿鹿) + different stack (Tauri vs Electron); "claw" = OpenClaw here vs a "Clawd" pun there. A parallel instance, not a fork (the v154→v155 independence shape).
- **KAON Hackathon origin** + **Notchi / Vibe Island** design-inspiration credit. Vibe Island is part of the forming external "Dynamic Island for AI coding agents" micro-genre named at v160 (NOT in corpus, NOT counted) — genre-corroboration, not a cited-to-subject elevation.
- **(a) FAILS** — `rainnoon` no disclosed identity; no axis minted.

## State after this ship
- **46 confirmed patterns (UNCHANGED) / 10 CONFIRMED Library-vocab (UNCHANGED).**
- Observability sub-archetype: **N=10** (a:5 / b:4 / c:1). Recorded in `_patterns/02b-confirmed-patterns-v42-plus.md` (sub-flavor (b) line N=3→N=4 + v164 ship note) + `_patterns/06-library-vocab-registry.md` §F running-log (rule-5 "filing is an act").
- **§28: 0 new standalones** (a tier-sub-flavor instance-strengthening).
- Streak GA:26 → **GA:27 · OG:11 [7 ov]**. §35 CLEAR.

## The standing lever (louder, 6th+ audit running)
**v153→v164 = 12 consecutive "tool for operating/monitoring AI coding agents" ships, ZERO piloted** — and oc-claw is the **4th desktop-pet** (v154/v155/v156/v164) in the most catalogue-saturated sub-niche, minting nothing. The v162 AND v163 audits both said it plainly: **pilot one or vary the domain, not another in this space.** The overdue audit now owes: the two N=2 orchestration buckets (v162 session-multiplexer / v163 orchestration-platform), the observability sub-archetype at N=10 with sub-flavor (b) now N=4, the standing override-review, and a shim reconcile. The high-value next move is a **pilot** or the **audit**, not a v165.
