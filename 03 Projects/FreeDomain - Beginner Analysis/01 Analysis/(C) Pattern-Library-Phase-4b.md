# (C) FreeDomain — Pattern Library Phase 4b (v145)

**Routine v2.6 · §28.** Off-goal capture (0/4 SKIP, 7th override) = **corpus-knowledge, NOT pattern-fuel** (the v116/v122/v127/v139/v142 discipline). **NO mint, NO promotion, NO state change.**

## Pattern Library state change: NONE
46 confirmed / ~25 active / 8 Library-vocab CONFIRMED → **UNCHANGED.** ZERO new Library-vocab minted (§28; registry 06 **NOT** moved). NO sub-archetype, NO new top-level Pattern.

## Observations only (NOT minted)
- **Override-door-keeps-catching-off-goal meta-signal — now 7 lifetime / 4-forward (v127+v139+v142+v145).** This is the strongest evidence yet for the v125/v130 recommendation: make a corpus-knowledge-outlier track the *default* for off-goal captures so they stop spending override budget + re-tripping the frequency triggers. The overdue audit owes a systematic review of this.
- **Huge-star off-goal subject** (174k★) — popularity ≠ goal-relevance (the v127 developer-portfolios / v129 lesson). Recorded as negative evidence, not a signal.
- **No internal-tooling thread** — unlike v142 OrcaSlicer (`.claude/` exhaust, N=3 with v122/v128), FreeDomain has zero AI surface, so there is not even an "engineering-exhaust" observation to make. Cleanest off-scope subject since v127.
- AGPL-3.0 (copyleft) — thin, not minted.

## Audit hand-off note
v145 adds NOTHING to mint. It sharpens ONE item the overdue audit already owes: the **override-frequency systematic-miss review**, now at **4-in-20** (v127+v139+v142+v145) + 7 lifetime. The diagnosis is confirmed-by-repetition (v125/v130): off-goal subjects are being routed through the override door instead of an outlier track — a usage/default issue, not a Phase-0.9 criteria defect. The concrete fix on the table: make outlier-track tagging the default for (b)-FAIL off-goal subjects; reserve override for genuine STRICT-miss rescues.
