# (C) Pattern Library Phase 4b — OpenHuman (v118)

**Routine:** v2.4. **Date:** 2026-05-29. **PRIMARY = Pattern #57 corpus-recursive sub-variant "Productized/Automated Karpathy-LLM-Wiki-Pattern at the Methodology-Influence Layer" → N=2** (v94 + v118). No state change asserted at ship (promotion is an audit decision). v2.4 §28 honored: zero new standalone Library-vocab minted.

---

## PRIMARY — Karpathy-LLM-Wiki-Pattern productized, corpus-recursive N=2

### The observation
Storm Bear's entire vault is built on **Pattern 1: the Karpathy LLM Wiki pattern** (the corpus's foundational methodology). OpenHuman's README **explicitly cites Karpathy's obsidian-wiki workflow** and implements it as a product: a **Memory Tree** that canonicalizes service data into ≤3k-token Markdown chunks, scores them, builds hierarchical summary trees, and stores them as an **Obsidian-compatible Markdown vault**. This is the vault's own pattern, productized and auto-ingesting.

### Why this is corpus-recursive (and which prior anchors)
- **v94 Understand-Anything** registered the NEW event-class "**CORPUS-FIRST Karpathy-LLM-Wiki-Pattern Corpus-Recursive Closure at the Methodology-Influence Layer**" — a tool that operates on / embodies the very pattern the corpus is built from.
- **v118 OpenHuman** is a **second** instance of that event-class: a commercial agent harness whose core memory layer *is* an automated Karpathy LLM-wiki.
- → **Corpus-recursive-at-methodology-influence sub-variant N=2 (v94 + v118).** Distinct from v78 ECC anchor-self-reference and v76↔v93 methodology-direction-reversal.

### Honest scoping (load-bearing)
- v94 *consumed/understood* existing wikis (a tool pointed at a Karpathy-pattern wiki); v118 *generates* a Karpathy-pattern memory by auto-ingesting 118 services. **Same lineage, different vector** (consume vs generate) — the sub-variant should be defined at the "embodies/operates-on the Karpathy LLM-wiki pattern" level, with v94 (consume) / v118 (generate) as sub-vectors. The audit must decide whether that's one event-class N=2 or two adjacent N=1s.
- **Product-purpose differs from the vault:** OpenHuman builds *personal-assistant memory* (auto-ingested work/life data); Storm Bear builds a *curated research corpus*. The shared thing is the *technique* (Markdown vault + summary trees + Karpathy lineage), NOT the purpose. Do NOT claim "OpenHuman automates the vault."
- Promotion-eligibility at N=3; **audit decision**, not asserted at ship. State UNCHANGED at v118.

Proposal: register the corpus-recursive-at-methodology-influence sub-variant at N=2 (v94 consume + v118 generate); promote toward CONFIRMED at N=3.

---

## SECONDARY (honest)

1. **Pattern #85 Platform-Primitive Foundation / agentmemory (v66) — N+1, with an identity flag.** OpenHuman optionally wires its memory into `rohitg00/agentmemory` to give **Claude Code + Cursor + Codex + OpenCode** shared persistent memory = memory-as-platform-primitive across coding agents (Pattern #85 lineage; v66 agentmemory). **FLAG:** verify whether the v66 corpus subject "agentmemory" is the *same repo* as `rohitg00/agentmemory` before asserting a hard v66 link — recorded as connectivity either way, identity-match deferred to audit. (Note: rohitg00 is also the v113 author.)
2. **Pattern #84 cross-vendor ecosystem-tolerance — N+1.** Explicit Claude/Cursor/Codex/OpenCode multi-agent compatibility via the shared memory backend.
3. **LV-C7 Tauri-desktop-harness cluster → N=3.** v73 cc-switch + v117 CodexPlusPlus + **v118 OpenHuman** = 3 Tauri-desktop agent tools. The cluster (filed yesterday at v117 as a 2-instance watch) is now **N=3** — becoming a promotable sub-claim. File into LV-C7; audit judges promotion. Honest caveat: "Tauri desktop" is a popular stack, so the audit should check whether the cluster is a *meaningful* archetype or just stack-popularity.
4. **Pattern #18 B1 MCP + 118-Composio aggregation — N+1.** MCP Registry integration + a 118-connector Composio layer = large-scale tool aggregation.
5. **TokenJuice ↔ v97 distill / Token-Economy-Quantification — connectivity.** A token-compression layer ("up to 80%") in front of the LLM; same theme as v97 distill. File observation into the token-economy theme; no new mint.
6. **Pattern #52 HIGH-NOT-EXTREME — N+1, notable.** ~294 stars/day × ~100 days = upper-edge HIGH-NOT-EXTREME (just under the >300/d EXTREME floor) sustained **>90 days**. Notably strong/long for the sub-class; NOT a promotion (it's HIGH-NOT-EXTREME, already CONFIRMED v75).
7. **Pattern #82 quantitative-marketing — STRONG.** "Personal AI **super intelligence**", "the **first** agent harness that gets to know you in minutes", "**up to 80%**" compression, a competitor comparison table. Dense superlatives.
8. **Pattern #66 supply-chain — NOTE.** The "**Private**" headline vs the managed-by-default reality (model routing + web search + 118 OAuth flows proxied through OpenHuman-hosted servers via Composio, paid subscription). Documented, not hidden, but a real privacy-vs-marketing tension + a genuine trust surface. Honest security caveat, not a new pattern.
9. **Library-vocab #12 LLM-routing artifacts — N+1.** `.claude/` + `AGENTS.md` + `CLAUDE.md` + `.agents/`.

---

## Library-vocab (v2.4 §28) — ZERO new standalones

Observations filed into existing clusters, not minted:
- **Tauri-desktop agent harness** (v73+v117+v118) → **LV-C7**, now a 3-instance watch (promotable-sub-claim candidate; audit checks archetype-vs-stack-popularity).
- **Managed-backend-proxy-behind-"private/local-first"-framing** → could seed an LV cluster on "local-first marketing vs managed-default reality," but it's N=1-distinct here → **omit per cap + clustering-first** (re-register on a genuine 2nd instance).
- TokenJuice → token-economy theme (existing).

**No standalone Library-vocab registered.**

---

## State after v118

| Field | Value |
|---|---|
| Confirmed Patterns | **46** (UNCHANGED at ship) |
| Active candidates | ~25 (UNCHANGED) |
| Library-vocab CONFIRMED | **8** (UNCHANGED) |
| Library-vocab PROVISIONAL | ~11 tracked (UNCHANGED — 0 new mints; observations filed into LV-C7 + token-economy) |
| Routine version | **v2.4 CURRENT** |
| Streak | **"46+2\*"** (was "45+2\*"; +1 clean PASS; 2nd consecutive clean PASS post-v116 override) |

**Queued for next audit (~v119–v120):** (1) **Pattern #57 corpus-recursive-at-methodology-influence sub-variant N=2** decision (v94 consume + v118 generate — one class or two?); (2) **LV-C7 Tauri-desktop-harness N=3** promotion check (archetype-vs-stack); (3) **agentmemory v66↔rohitg00/agentmemory identity verification**; (4) Pattern #18 #8 → CONFIRMED N=3 (carried from v117); (5) carry-forwards: 44-vs-45-PASS discrepancy + 2-data-point override-frequency review (from v116).

---

## Storm Bear's blunt take

The most interesting subject in a while, and genuinely on-corpus — but read it with clear eyes. The headline is real: **OpenHuman productizes the exact Karpathy LLM-wiki pattern your vault is built on** (Memory Tree + Obsidian Markdown vault + summary trees), and it can back Claude Code with persistent memory via agentmemory — that's the corpus's foundation showing up as a shipped commercial product for the second time (after v94). That's the PRIMARY, and it's worth registering. Three honest brakes: (1) **(a) FAILs** — Dubai-based "Builder & Hustler" founder running a subscription org, not a cultural-peer; (2) the **"Private" headline is doing heavy lifting** — by default your model calls, searches, and 118 OAuth flows proxy through *their* servers, and truly-local mode costs you features; (3) the **product purpose isn't your purpose** — it auto-ingests your work/life into assistant-memory, which is a different thing from a hand-curated research corpus, so "it automates the vault" would be wrong. Net: highest methodology-relevance since v94, a real but *fenced* pilot candidate (BYO-models + snapshot + don't OAuth your sensitive accounts), and no inflation — STRONG INCLUDE 3/4 with (a) honestly failing.
