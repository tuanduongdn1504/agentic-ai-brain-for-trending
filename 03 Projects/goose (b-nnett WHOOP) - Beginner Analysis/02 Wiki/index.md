# goose (b-nnett, WHOOP companion) — Wiki (v148, OFF-GOAL CAPTURE via 9th override + 2nd §35 `[ceiling-override]`)

> ⚠️ **Out-of-corpus-scope, and a name-collision.** `b-nnett/goose` is **NOT** Block's AI agent (`block/goose`) — it's a **WHOOP 5.0 fitness-band companion app**. Force-built at operator election (STRICT 0/4 SKIP → 9th lifetime override → 2nd consecutive `[ceiling-override]`, §35 stays breached). Corpus-knowledge, NOT goal-#1 progress. No Pattern Library change.
> **Repo:** `b-nnett/goose` · no license specified · Rust 73% + Swift 26%.
> **§37 provenance:** ≈883★ / 275 forks / alpha PoC (as of 2026-06, repo page — *stated, NOT API-verified; env mocks GitHub API*).

## What it is
README opener (verbatim):
> "Alpha proof of concept. This build is for developers to evaluate whether a project of this scope is viable."

Full name on the repo: *"Goose — Local Companion for WHOOP 5.0."* A **SwiftUI iOS app + Rust backend core** that connects to WHOOP 5.0 fitness bands over Bluetooth and displays health metrics (sleep, recovery, strain, stress, cardio, energy). A local-first health-tracking companion. Its only agent-ish element is a **"Coach" tab** that *locally summarizes* metrics and explains missing data — explicitly **not** an LLM/Claude/MCP integration.

## ⚠️ The name collision (the one thing worth remembering)
This is **not** `block/goose`, Block's well-known open-source extensible AI coding agent. The names coincide; the projects are unrelated. **Cataloguing-hazard note:** a repo sharing a name with a famous agent/Claude tool is NOT thereby goal-relevant — verify the actual subject. (Had the operator meant Block's goose, that would have been a clean goal-aligned ship; this one is a health app.)

## Why it's in the corpus at all
Force-built via the **9th lifetime operator override** (v84+v116+v122+v127+v139+v142+v145+v146+v148) + the **2nd consecutive logged `[ceiling-override]`** (after v146). §35 was already breached and only satisfied-not-cleared by v147; an off-goal v148 **keeps it breached** (window {v146 OG, v147 GA, v148 OG} = 2 OG). It is **corpus-knowledge-of-an-outlier**, not goal-#1 progress, and **not a pilot**.

## Phase 0.9 = 0/4
(a) FAIL (individual, no identity, not (a)-7) · (b) FAIL (WHOOP health app; "Coach" tab is a local summary, not an LLM) · (c) WEAK (Rust+Swift health app, orthogonal) · (d) FAIL/WEAK (only the coincidental name collision). See `01 Analysis/` for the override + §35 accounting (6-in-20 / 9 lifetime / §35 twice-overridden).

## Corpus connectivity
**None.** No agent/MCP/skill/Claude surface. NO Pattern Library mint. The meta-fact: §35 has now been overridden on two consecutive ships — the strongest evidence yet that the lever against off-goal drift is upstream subject-selection, not the soft ceiling. ⚠️ **v149 must be GOAL-ALIGNED** to clear §35.
