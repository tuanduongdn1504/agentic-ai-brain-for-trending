# (C) OmniRoute — Verdict

> LLM-wiki **v208** · `diegosouzapw/OmniRoute` · 2026-07-16
> Verdict produced **INLINE + fully hand-verified** per `feedback_wiki_verify_independently_check_collisions`.

## Headline

**GOAL-ALIGNED INCLUDE 3/4 — [(a) FAIL · (b) STRONG · (c) STRONG · (d) STRONG].**
**NO NEW MINT.** OmniRoute is the cleanest possible **N=2 instance-strengthening** of the §C standalone this vault minted one ship ago at **v207 CLIProxyAPI** — *"Self-Hosted Multi-Provider API Gateway that Re-Exposes CLI-Tool OAuth Subscriptions as OpenAI/Gemini/Claude/Codex-Compatible Endpoints."* (OmniRoute is literally an openly-credited **TypeScript port of CLIProxyAPI**.)
**Counts UNCHANGED 46/11.** §C live standalones stay **42**; the v207 standalone's tally goes **N=1 → N=2** (bookkeeping, not a count change); tracked PROVISIONAL surface stays **≈49**.

## The four criteria (routine v2.7 §31 — the tier keys on (b))

**(a) FAIL.** Diego Rodrigues de Sa e Souza (`diegosouzapw`, São Paulo BR, CDWA Solutions) = a **disclosed individual, not Anthropic**. §41: no Anthropic affiliation, no registered (a)-7 vendor-direct source; a Brazilian-heritage/locale inference is **not** an (a)-rescue; the disclosed-individual axis is answered NO. Clean FAIL. **#19 19a** first `diegosouzapw` author. (Matches v207's bare-org FAIL, v205's disclosed-individual FAIL.)

**(b) STRONG — keys the tier.** OmniRoute is canonical LLM-access / provider-routing / token-compression infrastructure sitting on the **exact substrate the vault studies** (cc-switch v73 / freellmapi v112 / CodexPlusPlus v117 / ai-switcher v153 / **CLIProxyAPI v207** / opencode-antigravity-auth v67), it is a **TypeScript port of the immediately-prior corpus subject (v207)**, it **integrates the corpus's own headroom v144** as a compression engine, and it lands **dead-center on the `claude-api-cost-optimization` thread** (a 10-engine compression stack + cost-optimized routing). **STRONG-not-STRONGEST** because: third-party gray-zone infra + Claude is one of ~250 providers + its flagship "free Claude via subscription-OAuth" path is ToS-violating / Anthropic-blocked (April-4-2026) / ban-risk → **pilot-AVOID-for-Claude**. Gray-zone ≠ off-goal (the v67/v69/v179/v190/v207 dual-use precedent) → **GOAL-ALIGNED per §31**.

**(c) STRONG (with foregrounded caveats).** TypeScript ~100% / MIT / ~5.2k–18k★ (conflicting) / v3.8.49 / ~5,032 commits + ~250 providers + 18 routing strategies + a 10-engine compression stack + MCP server (94 tools) + A2A protocol + memory (FTS5 + vector) + guardrails + multimodal `/v1` (chat/completions/embeddings/images/audio/OCR) + 42 locales + web/Electron/Android/PWA + one-click import of v207's credential store. **Caveats:** (1) **NOT source-cloned/verified at the TS level** — WebFetch/README/landscape-verified only (the ~205K shim overflows subagent context → no workflow; the v200→v207 self-throttle); (2) the enormous feature/metric claims (250 providers, 18 strategies, 10 engines, 94 tools, 21,000+ tests, 1.6B free tokens/mo, 15–95% savings) are all page/README-stated + marketing-flavored → not independently verified; (3) stars page-stated + conflicting (§37.4) → **NOT #52**; (4) the ToS gray-zone; (5) **no prominent ToS disclaimer surfaced**; (6) a firehose repo (v3.8.49, ~5,032 commits in a short window — README churn moves the numbers between snapshots).

**(d) STRONG.** The LLM-access-tooling neighborhood (cc-switch v73 / freellmapi v112 / CodexPlusPlus v117 / ai-switcher v153) + **CLIProxyAPI v207** (its direct port-parent — the §C standalone it instantiates + a genuine #57) + **headroom v144** (compression-engine integration — a 2nd genuine #57) + opencode-antigravity-auth v67 (single-provider OAuth-bridge cousin) + Pattern #18 #8 Multi-Source LLM Aggregator + the token-compression cluster (headroom v144 / distill v97 / Library-vocab #20 Token-Economy) + #18 B1-MCP + the claude-api-cost-optimization thread + hireui's future vendor-seam (the mosh-ai A2 thread).

## Pattern outcome — NO NEW MINT; N=2 instance-strengthening

OmniRoute IS the surface v207 minted: it re-exposes CLI-tool subscription-OAuth as OpenAI/Gemini/Claude/Codex-compatible endpoints, with a Subscription→API→Cheap→Free fallback + OAuth-refresh, connecting Claude Code/Codex/Cursor/Cline/Copilot. It is a **different author** (Diego Souza, disclosed individual, BR) in a **different language** (TypeScript vs Go) with a **materially broader** platform (compression stack + 18 routing strategies + MCP server + A2A + memory + guardrails + multimodal + desktop/PWA). → a **genuine N=2** for the surface.

- **§C standalone "Self-Hosted Multi-Provider API Gateway that Re-Exposes CLI-Tool OAuth Subscriptions…" → N=1 (CLIProxyAPI v207) + N=2 (OmniRoute v208).** This is the clean-2nd-instance precedent (camofox v179 / codebase-memory-mcp v172 / Strix v190) and it directly **satisfies the "PROMOTION-ELIGIBLE at N=2" note already recorded on that registry row**.
- It also **RESOLVES v207's boundary-alternative #2** (the "NO-MINT — just an instance of Pattern #18 #8" reading): a clean cross-author 2nd implementation confirms the surface is a *recurring capability class*, so the v207 mint was correct, not inflation.
- **No new §C standalone** (OmniRoute's extra platform features = within-species richness on the same core capability, not a new primitive — the camofox v179 "don't draw the circle" discipline). **No new top-level pattern (max #85).**

⚠️ **One reviewable nuance recorded for the ~v212 audit:** OmniRoute is a *port of* the N=1 anchor (shared lineage), not a fully-independent re-derivation. My read: it still counts as a genuine N=2 (different author + language + a much larger platform merging a 2nd fork-parent, 9router). **But recommend the audit require a fully-independent (non-port) 3rd instance** — the landscape shows candidates (kRouter, one-api, new-api, 9router itself) — before promoting the standalone to CONFIRMED. **N=2 recorded, PROMOTION-ELIGIBLE, promotion DEFERRED to the audit.**

## Secondary (NOT minted)

- **#57 GENUINE ×2 (doubly corpus-recursive).** OmniRoute openly credits + builds on **two** corpus subjects: (1) it *is a TypeScript port of* **CLIProxyAPI v207** (openly credited — the strongest lineage kind, cf. page-agent v199 ← browser-use v41); (2) its `headroom` compression engine + `ccr` retrieve-marker are *inspired by* **headroom v144** (`headroomlabs-ai/headroom` → `chopratejas/headroom`, author Tejas Chopra/Netflix). Distinguish from v181 cortex-hub's *silent* bundling (NOT #57) — OmniRoute's are open influence-citations. (N-tally = audit bookkeeping, recorded not self-incremented.)
- **Pattern #18 #8 (Multi-Source LLM Aggregator, CONFIRMED N=3)** instance-strengthening — OmniRoute is a clean #8 instance (live multi-provider routing across ~250 providers; at/beyond the 8b headless-proxy layer). Different axis from the §C standalone (§C = the subscription-OAuth-source + unified-multi-protocol-front distinctive; #8 = general multi-source aggregation) → **no double-count** (the v207 handling).
- **#18 B1-MCP** — OmniRoute ships an MCP server (94 tools, 3 transports). A clean B1-MCP instance (⚠️ note: this differs from v207, which ships **no** MCP server — so on the MCP axis OmniRoute is *more* than a v207 port). N-bump = audit bookkeeping.
- **token-compression cluster cross-ref** — the 10-engine stack is the densest token-compression instance in the corpus; **Library-vocab #20 Token-Economy-Quantification QUALIFIED-ADJACENT** (the "15–95% savings" is page-stated/unverified → N stays 4). Cluster: headroom v144 / distill v97 / OmniRoute v208.
- **LV-C2 cost-economics cross-ref** — free-tier stacking ("~1.6B free tokens/mo", 11 free-forever providers) + cost-optimized routing = the subscription-arbitrage / free-tier-stacking sub-theme (DEFERRED watch axis with relay-reseller v117 + free-tier-stacking v112 + subscription-arbitrage v207).
- **A2A (Agent-to-Agent) protocol** — plausibly the corpus's first A2A-protocol implementation (JSON-RPC 2.0 at `/.well-known/agent.json`). Recorded as a **DEFERRED watch axis**, NOT minted (a protocol implementation ≠ a mintable capability class at N=1; the §28 discipline).
- **#84 84c** cross-harness (one gateway consumed by 6 coding agents; NO N-bump — a gateway consumed by many, the v207 handling).
- **#19 19a** first `diegosouzapw` author. **#12** `CLAUDE.md` present (incidental).
- **#66 supply-chain / ToS / DUAL-USE** — subscription-OAuth reverse-engineering (Anthropic-blocked April-2026 + ban-risk); `npm install -g` global install; Docker; persists provider OAuth tokens + reads v207's credential store; **TLS-fingerprint stealth (`wreq-js`)** = anti-detection dual-use; no prominent disclaimer.

## Non-claims

NOT a new §C standalone (N=2 of the v207 standalone) · NOT a new top-level pattern (max #85) · NOT #52 (page-stated + conflicting §37.4) · NOT world-first (LiteLLM / OpenRouter / one-api / new-api / Portkey / kRouter / 9router populate the multi-provider-gateway space; OmniRoute's distinctive = subscription-OAuth-for-coding-agents + the compression stack — but as an N=2 of the v207 surface, world-firstness isn't claimed) · NOT source-verified at the TS level (flagged) · NOT #18 B1-MCP-as-the-primary (it ships an MCP server, tracked, but its primary shape is a REST gateway) · the "caveman/RTK/LLMLingua/Troglodita" inspirations are **non-corpus** (only CLIProxyAPI v207 + headroom v144 are corpus subjects).

## Tier & streak

**Tier T2 Service** (self-hosted gateway; the CLIProxyAPI v207 / agentmemory v66 / codegraph v70 / camofox v179 / cortex-hub v181 family).
**Counts UNCHANGED 46/11.** §C live standalones **42** (unchanged); v207 standalone N **1→2**; PROVISIONAL surface **≈49** (unchanged).
**§35 CLEAR** — window {v206 GA, v207 GA, **v208 GA**} = 0 OG (v203 = audit).
**Streak GA:67 → GA:68** (54 consecutive goal-aligned ships v153→v208).

## Pilot posture

**On-goal as a landscape/knowledge subject + directly relevant to the claude-api-cost-optimization thread — but ⚠️ pilot-AVOID-for-Claude** (its "free Claude via subscription-OAuth" path is ToS-violating + Anthropic-blocked since April 2026 + account-ban-risk; it is literally a port of the v207 subject already flagged this way). The two genuinely on-goal borrows: **(1)** the **token-compression architecture** (10-engine stack) as a reference for the vault's own context-compression discipline + the headroom pilot; **(2)** the **multi-protocol provider-routing architecture** as a reference for hireui's future vendor-seam (the mosh-ai A2 thread) — with **official, paid API keys**, per hireui's CONSTITUTION.
⭐ One-thing path **A1 → A3 → B7** — read the compression-stack + routing-strategy landscape + the ToS boundary (zero install) → reinforce hireui's *"official API keys, never subscription-OAuth reverse-engineering / CLI-token extraction"* ADR rule (already written for v207) and record **OmniRoute as the confirming 2nd instance** of that anti-pattern. See the 24-method Pilot Methods Menu.
