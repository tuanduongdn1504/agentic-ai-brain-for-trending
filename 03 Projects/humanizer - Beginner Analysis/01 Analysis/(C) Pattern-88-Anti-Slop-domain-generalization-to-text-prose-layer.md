# (C) Phase 4b PRIMARY — Pattern #88 Anti-Slop domain-generalization to the TEXT/PROSE layer

**Vehicle:** routine v2.3 §3 Phase 4b — within-pattern strengthening + **scope-generalization proposal** for a CONFIRMED top-level pattern.
**Confidence:** HIGH.
**Subject:** v108 `blader/humanizer`.

## The observation

Pattern #88 **"Anti-Slop-Design-Curation"** was CONFIRMED at the v86 audit (N=3, 3-arc-in-2-days) with a 3-variant sub-typology:
- **88a** framing-anchor (v81 taste-skill)
- **88b** named-rules-with-enumeration (v82 huashu-design)
- **88c** machinery-with-enforcement (v83 open-design)

Every instance to date — v81, v82, v83, v85 ui-ux-pro-max, and v105 op7418 ("去 AI 味/Stop Slop") — has been at the **design / visual-aesthetic layer**.

**v108 humanizer is the first TEXT/PROSE-layer instance.** It is the same mechanism — named anti-slop rules + enumeration + detect-and-rewrite machinery (88b + 88c) — applied to a different domain: it removes 30 documented **AI-writing** patterns (em-dash overuse, "Additionally"/"testament" vocabulary, copula avoidance, title-case headings, sycophantic tone, chatbot artifacts, filler/hedging). And it is **MEGA-viral** (21,421★) with the **most enumerated ruleset in the #88 lineage**, making it the canonical text-domain anchor.

## Proposal

> **Generalize Pattern #88 from "Anti-Slop-Design-Curation" to domain-general "Anti-Slop-Curation"**, with two sub-domains:
> - **88-design** (CONFIRMED) — v81 / v82 / v83 / v85 / v105
> - **88-text/prose** (NEW, anchored by v108 humanizer) — canonical instance; 30-pattern enumerated ruleset

The 88a/88b/88c sub-typology (framing / named-rules / machinery) is **orthogonal to domain** and carries over to both sub-domains unchanged. humanizer is 88b+88c (named-rules + machinery) in the text sub-domain.

**Why generalize rather than register a separate pattern:** the core curation mechanism is identical across domains; only the slop-target medium differs (pixels vs prose). A scope-generalization keeps the corpus from fragmenting one mechanism into two near-duplicate top-level patterns. This is administrative (Pattern #88 is already CONFIRMED) — no new top-level pattern is created.

## Promotion ladder
- **Domain-generalization registered at v108** (administrative; #88 already CONFIRMED). 88-text sub-domain N=1 (humanizer canonical anchor); op7418 v105 "去 AI 味" was a *design* instance with text-adjacent framing — note as borderline, do not double-count.
- N=2 watch for 88-text: any future text/prose anti-slop subject.
- Formal sub-domain confirmation at N=2.

## SECONDARY observations (held at OC layer per cascade-discipline)

1. **Pattern #52 HIGH-NOT-EXTREME N+1** — ~163.5 stars/day × 131 days (band-mates v73 cc-switch 195/d, v99 cmux 166.8/d).
2. **Pure-Markdown MEGA-Viral Single-Purpose Skill** — Library-vocab candidate. 4 files / 94 KB / 21K★. Pure-Markdown-T1 N=2 with v87 claude-comstyle (micro-scale) — same form, opposite scale (≈764× star gap).
3. **Voice-calibration personalization** — Library-vocab candidate (provide-samples-to-match-style); plausibly explains the unusually high fork count (2,032 forks = fork-and-customize-my-voice).
4. **In-doc versioning (1.0.0 → 2.7.0) with 0 GitHub releases** — documentary-versioning artifact note.
5. **Pattern #19 candidate (OC layer only, NOT registered):** "SF-Founder-CEO-Investor-Influencer-with-Viral-Single-Skill" (Siqi Chen). Held at OC layer — graveyard-pressure caveat.
6. **AGENTS.md** = Library-vocab #12 LLM-routing artifact; multi-harness (Claude Code + OpenCode) = Pattern #84 84c light (2-harness).
7. **30-pattern enumerated AI-writing-slop ruleset** = a concrete, reusable artifact (the most-enumerated #88 instance) — directly applicable to de-slopping the vault's own published content.

## Honest non-claims
- **agentskills.io conformance NOT claimed** — SKILL.md + AGENTS.md present, no spec-conformance declared → v108 NOT added to the Pattern #57 57k chain.
- **No new top-level pattern** — this is a scope-generalization of the already-CONFIRMED #88, not a #89.
- **(a) FAILS** — SF-located; ethno-lineage non-(a) per v106.
