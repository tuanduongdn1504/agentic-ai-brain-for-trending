# (C) ai-switcher — Pattern Library Phase 4b (v153)

**Outcome: NO change to confirmed counts (46 patterns / 9 Library-vocab CONFIRMED). PRIMARY = LV-C7 → N=3 (PROMOTION-ELIGIBLE). SECONDARY = 1 NEW section-C standalone. §28: 1 new standalone (≤2) + 1 cluster strengthening; registry 06 ACTUALLY edited (rule-5).**

## PRIMARY — LV-C7 "Tauri-Desktop Management-GUI for a Coding Agent" → N=3 (PROMOTION-ELIGIBLE)
- Members now: **v73 cc-switch** (Tauri-2 provider/config control-plane for Claude Code) + **v117 CodexPlusPlus** (Tauri-2 management/enhancement harness for Codex) + **v153 ai-switcher** (Tauri account/quota control-plane for Claude Code + Codex + Antigravity).
- This is a **genuine 3rd management-GUI**, which is exactly the bar the v120-audit watch set: *"N=3 watch — needs a 3rd management-GUI, not just a 3rd Tauri app"* (the v118 OpenHuman near-miss was a standalone assistant, correctly NOT counted). ai-switcher manages *other coding agents'* accounts/configs/quota = a clean management-GUI-for-another-agent.
- **Action:** filed to registry 06 LV-C7 (N=2 → N=3); **PROMOTION-ELIGIBLE → recommend CONFIRMED** (or, if the audit prefers, reconcile against the Pattern-layer "MCP-server / control-plane" review) at the now-badly-overdue audit. Promotion itself is the **audit's** call, not this ship's — filed, not asserted.

## SECONDARY — 1 NEW section-C standalone (N=1, CORPUS-FIRST)
**"Multi-Account / Quota Manager with Auto-Switch-on-Quota-Exhaustion for AI coding tools."**
- The distinctive *capability* (orthogonal to the LV-C7 *shape*): juggle **multiple accounts per tool** + **monitor each account's quota** (5h/weekly for Claude & Codex; per-model for Antigravity) + **auto-rotate to a fresh account when one hits its limit** + keep **isolated per-account CLI configs**.
- Distinct from **cc-switch (v73 8a)**, which switches *provider/config* (picks ONE active provider, manually) — it does **not** hold multiple accounts of the same tool, monitor quota, or auto-rotate. Distinct from CodexPlusPlus (v117, provider-switch + plugin-unlock). Distinct from Pattern #18 #8 (provider/source aggregation — a different axis; see below).
- **Action:** filed to registry 06 §C, N=1, PROMOTION-ELIGIBLE at N=2; time-aware stale-watch (≥15 wikis AND ≥30 days, per §28.3). §28 cap: 1 of ≤2 new standalones used.

## Observations (NOT minted)
1. **Pattern #18 #8 "Multi-Source LLM Aggregator" — ADJACENT, not counted.** ai-switcher rotates *accounts of the same tools* by quota; #8 aggregates *providers/sources*. Adds to the multi-{provider / agent / account}-management **layer-taxonomy** the v151 audit started (PROVIDER=#8 / AGENT=v150 Paseo / ACCOUNT=v153) — flagged for the audit, NOT minted as a #8 instance.
2. **Antigravity-as-managed-target corpus-recursive** — Google Antigravity appears again as a managed target (v67 opencode-antigravity-auth = OAuth bridge; v85 ui-ux-pro-max harness; v153 = account/token-swap + IDE-restart). Observation, heterogeneous, not minted.
3. **Pattern #84 cross-tool** — one control-plane spanning Claude Code + Codex + Antigravity. N+1 instance note.
4. **VN cultural-peer cluster N+1 (inferred)** — `hoangpm96` reads Vietnamese (v76/v80/v85 cluster). Inferred-only, NOT minted (the (a)-8 over-minting trap; name-heritage is not a located signal).
5. **Pattern #29 / #83 no-license-yet honest-disclosure** — license "none yet," author states intent to "add MIT before wider distribution"; unsigned `.dmg` with documented first-launch bypass. Observation.
6. **`.claude/` present** — incidental internal dev tooling; thin, and here the *subject itself* is goal-aligned, so it's not the v122 dev-exhaust thread. Noted only.
7. **NOT a Pattern #52 claim** — ≈13★ / 11 commits, nascent; metrics page-stated not API-verified (§37.4).

## §28 accounting
- New standalones filed: **1** (account/quota auto-switch) — within the ≤2 cap.
- Cluster strengthenings filed: **1** (LV-C7 N=2 → N=3).
- Registry 06 edited: **YES** (rule-5 "filing is an act") — LV-C7 member updated to N=3, new §C row added, §F count + promotion-eligible list updated.
- Confirmed counts UNCHANGED: **46 patterns / 9 Library-vocab CONFIRMED.** Tracked PROVISIONAL surface ≈21 → ≈22 (+1 standalone). PROMOTION-ELIGIBLE list now also carries LV-C7 at N=3.
