# (C) Scrapling — Pattern Library Phase 4b (v149)

**Routine v2.6 · §28 (single registry / ≤2-new-standalones / clustering-first / filing-is-an-act).**

## PRIMARY — LV-C1 "Vendor-Official Agent-Tooling for Own Product/Offering" → N=3 (FILED to registry 06 §B, LV-C1)

### The cluster sub-claim reaches a genuine 3rd instance
- **v114 gsap-skills** — GreenSock (a **software-library vendor**) ships official agent-skills teaching agents to use GSAP, its own product.
- **v126 compound-engineering-plugin** — Every (a **methodology/editorial vendor**) ships the executable plugin form of its own published "Compound Engineering" methodology.
- **v149 Scrapling** — D4Vinci (a **software-library vendor**) ships a **built-in MCP server** + an **official agent-skill** (`/agent-skill` + clawhub/OpenClaw "scrapling-official") so agents can use Scrapling, its own product.

**Why it's a clean N=3, not a stretch:** Scrapling is a structural twin of v114 — both are software-library vendors shipping *official agent-tooling for their own library* (v114 = skills; v149 = MCP server + skill, both within LV-C1's "agent-tooling" scope as generalized at v126). It is cross-author, cross-product, cross-vertical (front-end animation / editorial-methodology / web-scraping). It also **resolves the audit's open sub-question** ("library-vendor v114 vs methodology-vendor v126 — one theme or two sub-types?"): the **library-vendor sub-type is now N=2** (v114 + v149), with v126 the lone methodology-vendor — so the broad LV-C1 theme is genuine AND a library-vendor sub-type is emerging.

- **Promotion-eligibility:** LV-C1's "Vendor-Official-Agent-Tooling-for-Own-Product/Offering" sub-claim was PROMOTION-ELIGIBLE at N=3 (per the v126 filing). v149 makes it **N=3 → recommend promotion to CONFIRMED at the next (overdue ~v139–v140) audit**, OR split into library-vendor (v114+v149) + methodology-vendor (v126) sub-types — the audit's call. **NO ship-promotion** (conservative; the ship files the N=3 strengthening, the audit confirms — cf. v117 #18 #8 handling).
- **§28 new-standalone count this ship = 0.** This is a cluster strengthening, not a new standalone. Registry 06 §B (LV-C1) + §F edited (rule-5: filing is an act).

## SECONDARY — instance notes + observations (§28-disciplined, NOT minted)

- **Pattern #18 sub-mechanism B1 "MCP" — N+1.** Scrapling's built-in MCP server (`scrapling[ai]`) exposes scraping/extraction capabilities to any MCP client (Claude/Cursor). The B1 line is now **dense** (v66/v70/v119/v132/v134/v140/v141/v144/v149) → an **"MCP-server" tier-sub-archetype review is overdue** — flagged for the next audit, NOT minted here. Top-level count UNCHANGED.
- **Pattern #84 multi-harness / agent-tooling — N+1.** MCP server + agent-skill across Claude / Cursor / OpenClaw. Instance note.
- **OpenClaw / clawhub.ai = a parallel agent-skill registry — OBSERVATION (NOT minted).** Scrapling's official skill is distributed via `clawhub.ai/D4Vinci/scrapling-official` (OpenClaw), a registry parallel to agentskills.io. This is the v121 CodexKit "parallel skill-authoring standard" thread (the ecosystem now spans agentskills.io / Codex-native / Hermes-lineage / OpenClaw-clawhub). **Explicitly NOT asserted as a Pattern #57 57k `npx skills add` agentskills.io implementer** — the `/agent-skill` dir may or may not contain an agentskills.io `SKILL.md`; flagged as a candidate for the chain reconciliation, not claimed (the v143 57k-candidate discipline).
- **"Tool-FOR-agents vs app-that-uses-an-LLM" direction calibration — OBSERVATION.** Scrapling = capability exposed TO agents (v140/v143 class, goal-aligned); contrast v123 MoneyPrinterTurbo = app that internally calls an LLM (off-goal). A useful calibration note for the off-goal/goal-aligned boundary; recorded, not minted.
- **Multilingual READMEs (AR/RU/CN/JP) — OBSERVATION.** Library-vocab #12-adjacent / locale-breadth; the AR README is the main signal for the (a) MENA/Arabic-speaking inference. Recorded, not minted.
- **Adaptive / self-healing selectors + stealth-fetch tiers — OBSERVATION.** Genuinely instructive engineering (the (c) basis); not a corpus pattern.

## Pattern Library state change: NONE
46 confirmed / ~25 active / 8 Library-vocab CONFIRMED → **UNCHANGED.** LV-C1 cluster sub-claim strengthened to N=3 (promotion-eligible; promotion is the audit's administrative act). 0 new standalones; tracked standalone surface UNCHANGED. NO top-level Pattern, NO tier sub-archetype, NO confirmed-count movement.

## Audit hand-off note
v149 hands the **overdue ~v139–v140 audit**: (1) **LV-C1 N=3** — promote-to-CONFIRMED vs split into library-vendor(v114+v149) + methodology-vendor(v126) sub-types; (2) the **MCP-server B1 line is now very dense** (9 servers v66/v70/v119/v132/v134/v140/v141/v144/v149 + v141's 11 connectors) → an "MCP-server / MCP-exposure" tier-sub-archetype review is overdue, NOT minted here; (3) the **OpenClaw/clawhub** registry as another parallel skill-standard (ecosystem now ≥4 standards) → fold into the v121 "parallel standard" tracking; (4) the still-open **override-frequency 6-in-20** review (v149 is a clean goal-aligned ship, adds nothing to it, and CLEARS §35 — the first clean clearance since the v146/v148 breach). NOTHING for the override thread from v149 itself.
