# (C) ping-island — Phase 0 scope gate + Phase 0.9 four-axis verdict

**Subject:** `erha19/ping-island` (v160 wiki ship, 2026-06-05). **Routine:** v2.6 (§31 / §35 / §37).
**Requested:** "build LLM wiki for erha19/ping-island ... macOS app to manage multiple AI coding tools."

## Phase 0 — scope gate
**In scope.** ping-island surfaces + routes the *attention-needing moments* of AI coding agents (Claude Code + 12 others). Squarely goal #1 and a real heavy-multi-agent vault workflow. It belongs to the v158-audit-CONFIRMED "AI-Agent Observability / Status-Surface Tool" sub-archetype — but at a genuinely distinct sub-flavor (see Phase 4b).

## Phase 0.9 — STRICT four-axis verdict: **GOAL-ALIGNED INCLUDE 3/4**

**(a) Cultural-peer / vendor-direct — FAIL.**
The repo discloses **no real name, affiliation, or location** for `erha19`. There is no declared/located cultural-peer signal and no (a)-7 vendor-direct status. Inferring China-Mainland (or any) cultural-peer from the handle alone would be exactly the v139 / v97→v115 anti-pattern — and the v151 audit's pending (a)-rescue tightening recommendation explicitly targets heritage/handle inference. **(a) FAILS; no rescue; no axis minted.** (b) carries the include.

**(b) Goal-relevance — STRONG (not STRONGEST).**
An attention/approval router for Claude Code + 12 agents handles the exact interrupt that breaks multi-agent flow (an agent blocked on approval/input you didn't notice) and lets you act in place. That is dead-center goal #1 and a genuine vault workflow — arguably the most *operationally useful* shape in the v153–v160 niche, because it is the actionable layer, not a read-only display. **STRONG-not-STRONGEST** = still a surface/utility *around* the agents, not the agent substrate or a methodology (the established cluster calibration).

**(c) Engineering quality — STRONG.**
Native Swift 6.1 (98.5%); notch/Dynamic-Island UI; managed-hook installation across 13 heterogeneous clients; live `codex app-server` WebSocket; a `PingIslandBridge` bootstrapped over SSH with remote-hook rewriting; IDE extensions; multi-client transcript backfill. **433 commits / v0.21.1** — a mature, sophisticated multi-integration native app.

**(d) Corpus connectivity — STRONG.**
Observability sub-archetype (mints NEW sub-flavor (c), bringing the sub-archetype to N=9); Swift-native-macOS cluster N+1 (v99 cmux + v100 Easydict + v154 agentpet + v157 ClaudeBar + v159 CodexBar + v160); Pattern #84 cross-tool (13 agents); **hooks-integration mechanism** shared with v156 clawd-on-desk; **permission-approval affordance** shared with v156 (sub-feature there, core here); Hermes / OpenClaw / Qwen / Antigravity corpus-recursive refs.

## §35 — STAYS CLEAR
Window {v158 GA, v159 GA, **v160 GA**} = 0 OG (eight consecutive goal-aligned ships v153→v160). NOT an override → override-frequency UNCHANGED (lifetime 10). Streak GA:22·OG:11 [7 ov] → **GA:23·OG:11 [7 ov]** (23rd GOAL-ALIGNED PASS).

## §37 — provenance (NOT API-verified)
≈**832★** / 89 forks / 1 watcher / 3 issues / **433 commits** / v0.21.1 (Jun 5 2026) / Apache 2.0 / Swift 6.1 (98.5%) + Shell / macOS 14+. Page-stated via WebFetch + erha19.github.io/ping-island, **NOT independently API-verified (§37.4)** — env mocks the GitHub API. **Created date NOT shown → velocity unestablished → NOT a #52 claim.** Owner `erha19` (no disclosed identity).

## Honest non-claims
- **(a) genuinely FAILS** — no disclosed identity; no cultural-peer inferred from the handle (v139 / v151-(a)-rescue discipline). No axis minted.
- **This DOES mint** — unlike v159 CodexBar (a clean metering instance, 0 mints), ping-island is structurally distinct from both confirmed sub-flavors → a genuine NEW sub-flavor (c). That is NOT manufacturing a mint to justify a niche entry (the v158-audit caution): it is honestly recording that ping-island is neither metering nor a pet. The minimal-inflation home is a 3rd *sub-flavor inside the existing confirmed sub-archetype* (N=1), not a new top-level standalone.
- **NOT LV-C7 #22** — handling the agent's own permission prompts is a pass-through (v156's affordance), not managing config/accounts (cc-switch/CodexPlusPlus/ai-switcher). OBSERVES + ROUTES ≠ CONTROLS.
- **NOT Pattern #18 #8** — routes attention/sessions, not LLM providers.
- **NOT a #52 claim** (creation date unverified; API mocked).
- **AGENTS.md + the managed-hook/bridge artifacts** = the product's integration layer + thin internal tooling, recorded as mechanism, not minted as separate patterns.
- **External "Dynamic Island for agents" genre** (CodeIsland / xisland / Vibe Island) is NOT in the corpus → NOT counted toward N; noted only as corroboration that sub-flavor (c) is a real emerging class.
