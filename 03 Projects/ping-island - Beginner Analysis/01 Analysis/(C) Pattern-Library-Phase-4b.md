# (C) ping-island — Pattern Library Phase 4b

**Subject:** `erha19/ping-island` (v160). **Verdict:** GOAL-ALIGNED INCLUDE 3/4. **Ships into:** the v158-audit-CONFIRMED Pattern Library.

## PRIMARY — NEW 3rd sub-flavor (c) of the observability sub-archetype (PROVISIONAL N=1, CORPUS-FIRST)

The v158 audit CONFIRMED the **"AI-Agent Observability / Status-Surface Tool"** sub-archetype with two formal sub-flavors:
- **(a) metering / quota / usage-logs** — read-only numeric/dashboard surfacing (v89/v109/v157/v158/v159, N=5).
- **(b) ambient / affective status-surface** — reactive desktop-pet (v154/v155/v156, N=3).

ping-island is **neither.** It mints a **3rd sub-flavor:**

> **(c) Attention / Approval-Interrupt-Routing Surface** — surfaces the *actionable* moments an agent needs a human (tool-call approval, input/follow-up prompt, review, intervention), lets you **approve / deny / answer in place**, and **routes focus back** to the originating terminal/IDE; characteristically **notch / Dynamic-Island-first** (compact-until-needed, not an always-visible dashboard); with **SSH-forwarding** of remote agent sessions as a first-class workflow. **PROVISIONAL N=1** (v160 ping-island). CORPUS-FIRST.

**Why this is a genuine mint, not manufactured (the v158-audit discipline applied):**
- The test the v158 audit set: a clean *instance* of an existing sub-flavor mints nothing (that was v159 CodexBar — a 5th metering tool). ping-island is **not** a clean instance of (a) or (b) — it is functionally distinct: *actionable triage + focus-routing*, not read-only metrics, and not an affective character (its mascots are cosmetic).
- Would I register (c) absent the 8th-in-niche pressure? **Yes** — the interrupt-routing-back-to-source + notch-first model is genuinely a different thing. External corroboration (NOT counted toward N): a whole "Dynamic Island for AI coding agents" micro-genre exists (CodeIsland, xisland, Vibe Island) — evidence (c) is a real class, not my invention.
- **Minimal-inflation home:** a 3rd *sub-flavor inside the existing confirmed sub-archetype* (the 8a/8b-inside-#8 / (a)/(b)-inside-observability precedent), NOT a new top-level standalone and NOT a top-level Pattern. **Sub-archetype N=8 → N=9; new sub-flavor (c) N=1.** Promotion-eligible at a genuine in-corpus N=2 (a 2nd attention/interrupt-router).

**§28 / counts:** this is a tier-sub-archetype-layer registration (a new sub-flavor), not a §C standalone → **0 new §C standalones; 0 new CONFIRMED Library-vocab (stays 10); top-level pattern count UNCHANGED at 46.** Filing acts (rule-5): `02b` observability sub-archetype section gets sub-flavor (c) + N=8→N=9; registry `06` §F v160 note.

## SECONDARY — observations (NOT minted)
- **Light-control affordance ≠ LV-C7 #22:** approving/denying the agent's *own* permission prompts is a pass-through (v156 clawd-on-desk had the same as a sub-feature) — observe+route, not manage config/accounts. The observe-vs-control line (v154/v158) holds; ping-island stays on the observe side.
- **Hooks-integration mechanism** (managed JSON hooks across 13 clients) — shared with v156 clawd-on-desk; instance note (#84 84c.1 adjacent — hook-based rather than log-parse-based state acquisition).
- **SSH-forwarding of remote agent sessions** as a first-class workflow — corpus-first capability *within* sub-flavor (c); recorded as the sub-flavor's distinguishing capability, not a separate mint.
- **Swift-native-macOS cluster N+1** (v99/v100/v154/v157/v159/v160) — heterogeneous domains → observation.
- **Pattern #84 cross-tool** (13 agents); **Hermes / OpenClaw / Qwen / Antigravity** corpus-recursive refs.
- **NOT #18 #8** (routes attention/sessions, not providers). **NOT a #52 claim** (832★, creation date not shown → velocity unestablished; API mocked §37.4).
- **(a) FAILS** — no disclosed identity; no cultural-peer inferred from the handle (v139 / v151-(a)-rescue discipline). No axis minted.

## Counts after this ship
46 patterns (UNCHANGED) / **10 Library-vocab CONFIRMED** (UNCHANGED) / observability sub-archetype **N=8 → N=9** with **NEW sub-flavor (c) N=1** / tracked PROVISIONAL surface ≈23 → **≈24** (+1 sub-flavor). Streak **GA:23·OG:11 [7 ov]**; §35 CLEAR.

## The blunt note (carried to the response)
ping-island earns its keep where v159 didn't — it mints a real 3rd sub-flavor (attention/approval-interrupt-routing, notch-first), and it's arguably the most *operationally useful* of the whole v153–v160 niche. But it is still the **8th consecutive "monitor/manage AI coding tools" subject with ZERO piloted.** A genuine mint is more valuable than v159's clean instance — and still not a pilot. The high-value next move is to **pilot one** (ping-island is a strong candidate — it targets a real flow-break) or **vary the domain.**
