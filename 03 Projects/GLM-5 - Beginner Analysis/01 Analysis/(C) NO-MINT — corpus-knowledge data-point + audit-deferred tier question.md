# (C) NO-MINT — v176 GLM-5 — Corpus-Knowledge Data-Point + Audit-Deferred Tier Question

**No Library-vocab §C standalone is minted; no new top-level pattern; counts UNCHANGED 46/10; §C surface stays ≈33.** This doc records *why no mint*, the data-point logged instead, and the audit question deferred.

---

## Why NO mint (the anti-inflation reasoning)

GLM-5 is a **frontier foundation model** — a different *kind* of object from everything the §C Library-vocab catalogues (which is tool/capability/artifact-shaped: scanners, bridges, proxies, memory engines, capability layers). The temptation is to mint something like *"Open-weights frontier agentic-coding LLM positioned as a Claude-backend alternative."* That is declined, for three reasons:

1. **§28 clustering-first / count-discipline.** Competitor models have appeared in the corpus before, but always as *backends referenced by tools* (DeepSeek V4 behind DeepSeek-TUI v72; the 9-provider matrix behind cc-switch v73). A single goal-*adjacent* model is not a demonstrated recurring capability class — minting at N=1 here is precisely the "phantom-count" inflation the v2.4 audit cleaned up.
2. **Goal-adjacency, not core.** This subject is INCLUDE via an operator-directed (b) MODERATE (with OFF-GOAL defensible). Minting a permanent vocab class off a borderline subject over-weights it.
3. **No clean home.** It is not an instance of any existing §C standalone, and it does not start a cluster (no 2nd instance in view). The honest record is a *data-point*, not a *class*.

---

## What IS logged instead

**A corpus-knowledge data-point — "competitive-landscape / substrate-alternative reference":**
- GLM-5.2 (744B / 40B-active MoE, 1M ctx) is a **near-frontier open-weights agentic-coding model** that trails Claude Opus 4.8 on the headline coding bench (Terminal-Bench 2.1: 81.0 vs 85.0, page-stated) and closes much of the gap elsewhere (Vending Bench 2, CC-Bench-V2 "approach/narrow gap to Claude Opus 4.5").
- It is a **concrete, strong target** for the cc-switch v73 "route Claude Code to an alternative provider" move (Z.ai OpenAI-compatible API).
- It joins the corpus's **Chinese-AI-lab set** (Zhipu AI / OpenBMB v175 / ByteDance v143) as a #19 19a institutional data-point.

---

## Audit-deferred tier-taxonomy question

The corpus tier taxonomy (T1 Coding Agent/Framework · T2 Service · T3 Education · … · T5 out-of-scope) has **no "model / inference substrate" tier**. GLM-5 is plausibly the **first frontier general-purpose / agentic-coding LLM catalogued as the primary subject**.

⚠️ **Precision (corrected via collision grep):** GLM-5 is **NOT** the first *model-as-subject* — **fish-speech** (a TTS / speech model, `_state/04-projects-v30-v39.md`) preceded it. The accurate claim is narrower: first *frontier general-purpose LLM* subject. Prior LLM-adjacency was always mediated by a tool (DeepSeek-TUI v72) or an educational companion (LLMs-from-scratch v74) or a provider switcher (cc-switch v73).

**Deferred to the (already overdue) audit, NOT decided now:** whether a recurring "model / inference-substrate" subject type warrants a tier category, or whether such subjects should default to OFF-GOAL CAPTURE / out-of-scope. One data-point (GLM-5) is not enough to decide; a 2nd frontier-LLM-subject would force it.

---

## Non-claims (guardrails)

- **NOT #52** — 4.9k★ / 509 forks / 0 releases page-stated, NOT API-verified (§37.4) → velocity unestablishable.
- **NOT #57** — benchmarking against Claude Opus is competition, not influence-citation.
- **NOT a new §C standalone, NOT a new top-level pattern** (max #85).
- **NOT a re-tier of any existing subject.**
- **Counts UNCHANGED: 46 / 10.**
