# (C) meetily — Verdict

*v196 · 2026-07-04 · Phase 0.9 STRICT (routine v2.6) · verdict produced INLINE + hand-verified per `feedback_wiki_verify_independently_check_collisions`*

## One-line verdict

**GOAL-ALIGNED INCLUDE 3/4** *(per operator direction — OFF-GOAL CAPTURE is defensible; the GLM-5 v176 / TimesFM v193 off-domain-goal-adjacent situation)* — **`(a) FAIL · (b) MODERATE · (c) STRONG · (d) STRONG`**. **NO MINT** — a corpus-knowledge data-point in a new domain (privacy-first local AI meeting assistants) + a deferred watch axis; **counts UNCHANGED 46/11**, §C surface unchanged. The value to *this* operator is the **provider-agnostic LLM-client + templated summarization architecture** (directly pilotable into hireui's first LLM feature) and the **app itself as a privacy-first Scrum/recruitment notetaker**, NOT a new pattern.

## Phase 0.9 STRICT — the four criteria

**(a) cultural-peer / Anthropic → FAIL (clean).** Zackriya Solutions is a disclosed AI-automation consultancy (Bengaluru + Atlanta), **not Anthropic** ((a)-7 is Anthropic-only). India-located / Indian-heritage is **not an (a)-rescue** (the v159→v195 discipline; cf. v98 Berlin-Indian-diaspora + v118 Dubai-Indian-diaspora, both NOT registered as axes). It's a real company, not a bare handle → the disclosed-builder situation, operator-reviewable, **not minted**. First `Zackriya-Solutions` author → a #19 19a institutional data-point.

**(b) goal-relevance → MODERATE (keys the tier; ⚠️ OFF-GOAL defensible).** This is the crux and it's honestly borderline. meetily is a **consumer/end-user desktop APPLICATION** (a meeting-notetaker) — **not** a coding agent, agent infrastructure, a Claude-first tool, a skill/MCP/methodology. The corpus substrate is "tools-around-Claude for software dev / autonomous agents," and a meeting-notetaker sits outside it. **Held at MODERATE (not FAIL, not STRONG) by three genuine on-goal threads:**
1. Its **provider-agnostic LLM-client** (`generate_summary()` — 7 providers, one OpenAI path + Claude's own shape + a local sidecar) is a clean, source-verified **reference architecture for hireui's first LLM feature** — literally the "vendor seam" pattern in the `mosh-ai-powered-apps` pilot thread (A2 seam → A1 summarizer → A6 evals).
2. Its **map-reduce + templated summarization engine** (token-aware chunking, English-cache-reuse, JSON section-templates) is a directly-borrowable long-document-summarization design.
3. **The operator is a Scrum coach** and meetily ships `daily_standup.json` / `retrospective.json` / `project_sync.json` — a privacy-first, local standup/retro notetaker is a real personal-workflow fit, and interview-notes capture is a real hireui fit.

The operator **explicitly asked to "apply into my working flow, show me many methods"** → the applied value is the point. But I will **not inflate to STRONG**: STRONG is for subjects dead-center on the agent/Claude substrate (devspace, codebase-memory-mcp, agent-skills). meetily is a consumer app whose *architecture* and *use* are goal-adjacent. **(b) MODERATE → GOAL-ALIGNED per §31** (b MODERATE+); the OFF-GOAL CAPTURE reading (a consumer meeting-notetaker, off Goal #1, rescued by architecture-study + Scrum/recruitment use) is equally defensible and recorded operator-reviewable.

**(c) engineering substance → STRONG.** 556 commits / ~18 months / Rust+TS+C++; ~100+ Tauri commands; a sophisticated dual-path audio pipeline (RMS-ducked pro mixing ‖ VAD-filtered STT; EBU R128; per-platform WASAPI/ScreenCaptureKit/ALSA); pluggable Whisper+Parakeet STT via a trait; a clean 7-provider LLM dispatcher; map-reduce token-aware summarization; SQLite/sqlx with 5 repositories; code-signed + notarized distribution; a 235-line Bluetooth-resampling engineering notice; a 5-backend GPU matrix. **Caveats (foregrounded):** v0.x pre-1.0; `audio_v2/` is unwired scaffolding; near-zero audio-layer test coverage; stale `"main": "electron/main.js"`; README/updater still point at the old `meeting-minutes` repo name; PostHog write-key exposed in the public repo; Claude `max_tokens` hardcoded 2048 + a stale "60 seconds" timeout string.

**(d) corpus connectivity → STRONG.** **fish-speech v20** (the corpus's other speech/voice-model subject — TTS there, STT here) · the **Tauri-desktop LV-C7 cluster** (cc-switch v73 / CodexPlusPlus v117 / OpenHuman v118 / PilotDeck v175) · the **provider-agnostic / multi-provider family** (#84 84c + freellmapi v112 proxy + cc-switch v73 switcher + OpenHuman v118 routing + the mosh-ai vendor-seam thread) · **privacy-first/local-first** tension cousin OpenHuman v118 · **Ollama local-inference** thread · the **GLM-5 v176 / DeepSpec v186 / TimesFM v193** off-domain-goal-adjacent tier precedent · content-fingerprint no-op guard echo of **openwiki v195**.

## Pattern outcome — NO MINT (and why)

meetily is **corpus-first for a DOMAIN** (AI meeting assistants — collision-clean grep) but that does **not** earn a §C standalone. Applying §28 + the **TimesFM v193 discipline** verbatim: *"a single off-domain / goal-adjacent subject is not a recurring capability class; §C vocab is tool/capability-shaped; off-domain subjects enter only as goal-adjacent knowledge data-points."* A consumer meeting-notetaker is an **end-user-app vertical**, not an agent-capability class. The **Kilo-Code v177 / agency-agents v185 "mint N=1 for the world-class exemplar of a recurring class"** precedent is explicitly scoped to **in-scope agent-capability classes** — it does not reach a consumer app. So: **NO new §C standalone, NO new top-level pattern (max #85), counts UNCHANGED 46/11, §C surface unchanged.** Recorded as a **corpus-knowledge data-point + a DEFERRED watch axis** ("consumer/end-user application whose LLM-integration *architecture* is the goal-relevant part, not the app's domain" — the GLM-5/DeepSpec/TimesFM template).

**Tier: T5 Application** (consumer end-user desktop app) — the MoneyPrinterTurbo v123 / Echo Loop v128 tier, but **cleared on (b) MODERATE via architecture + Scrum/recruitment use**, *not* an (a)-rescue WEAK-include; provisional label "Privacy-first local-first AI meeting assistant (device-audio capture + local STT + provider-agnostic LLM summarization, no meeting bot)."

**SECONDARY (NOT minted):** #19 19a first Zackriya-Solutions author · #84 84c provider-agnostic (7 providers, one dispatcher; NO N-bump; not the ponytail 14-platform mechanism) · **fish-speech v20 cross-ref** (2nd speech-domain subject; STT mirror of TTS) · **LV-C7 Tauri-desktop cluster data-point** (+meetily; audit bookkeeping, no promotion) · #66 supply-chain BENIGN-to-MODERATE (signed+notarized, no checksum, stale updater endpoint, exposed PostHog write-key, opt-in default-OFF analytics — the "on by default" agent claim **corrected**) · openwiki-v195 content-fingerprint-cache echo · OpenHuman-v118 privacy-first cousin.

**NON-claims:** NOT a §C mint (corpus-first for a domain, not a mintable capability class — the TimesFM v193 discipline) · NOT #52 (~9.5k–14k★ page-stated across sources §37.4, Trendshift-#1 unverified → velocity unestablishable) · NOT #57 (credits Whisper.cpp/Screenpipe/transcribe-rs/NVIDIA-Parakeet/istupakov — none corpus subjects; mentions ≠ recursion) · **NOT #18 B1-MCP** (ships **NO** MCP server — a real agent-integration limitation; the MCP-exposing peer is Hyprnote/Anarlog, not meetily) · NOT the first model-subject (fish-speech v20 preceded; meetily *uses* models, it isn't one) · NOT a new top-level pattern (max #85).

## Streak / §35

streak → **GA:57** *per operator direction* (⚠️ OFF-GOAL alt reading — consumer app rescued by architecture/use-adjacency — joins v176/v186/v191/v193 → **GA:52·OG:16**). **§35 CLEAR:** window {v194 GA, v195 GA, **v196 GA**} = 0 OG (GA reading); under the OFF-GOAL reading {v195 GA, v196 OG} = 1 OG ≤ 1 → still CLEAR. **43 consecutive goal-aligned ships v153→v196** (GA reading).

## Verification (inline + by hand)

A **7-agent read-only research workflow** (`wf_05f64337-a15`, ~1.29M subagent-tokens, 143 tool-uses; a first attempt `wf_7bb0f8fc-50d` was killed by a session limit and re-run) did **source-reading + upstream/landscape/identity/security research ONLY**. **ALL corpus/collision/identity/tier claims were hand-verified:**
- Collision grep over `_state/` + `_patterns/` + `03 Projects/` → **clean** (no prior meetily/Zackriya/meeting-assistant/notetaker/transcription-app/Whisper-app subject); fish-speech v20 = the prior speech-domain subject.
- Full §C standalone-title list read from `_patterns/06` → **no meeting/notetaker/STT §C standalone** → corpus-first-for-domain, and the TimesFM-v193 no-mint discipline applied.
- Source hand-read: README, LICENSE.md, CLAUDE.md, `docs/architecture.md`, PRIVACY_POLICY.md, `summary/llm_client.rs`, `summary/processor.rs`+`service.rs` (line counts), `summary/templates/defaults.rs` + `standard_meeting.json`, `analytics/analytics.rs` + `analytics/commands.rs` + `lib/analytics.ts` + `AnalyticsProvider.tsx`; `git rev-list`/`git log`/tags/remote/updater-endpoint verified.

**Confabulations caught + corrected:** (1) the security agent's "**CRITICAL: analytics on by default**" — **wrong**; `init_analytics()` is gated behind a frontend opt-in store defaulting to `false` (`AnalyticsProvider.tsx:41-57`) → "off by default" is accurate; the exposed PostHog *write-key* is the real (low) finding. (2) the docs agent's "**50 commits / 3 months old**" — **wrong**; 556 commits since 2024-12-26 (~18 months); it confused the v0.3.0→v0.4.0 window. (3) star counts disagreed across agents (9.5k/12.9k/14.2k) — all page-stated → **no #52 claim**.

`inflation_check` = **discipline HELD** (0 mints; the tempting "corpus-first AI-meeting-assistant" §C mint DECLINED as §28 domain-not-capability inflation, the TimesFM v193 precedent; max #85; counts 46/11 unchanged; the analytics overclaim + the commit-count overclaim caught).

## Bottom line

A well-built, genuinely privacy-first consumer meeting notetaker with a **clean, borrowable LLM-integration architecture** — that architecture (not the notetaker domain) is what's worth your time. It's **off Goal #1 as a product** but **goal-adjacent as a blueprint**, and it's a real personal/Scrum/recruitment tool. See **(C) meetily — Pilot Methods Menu.md** for the 24 ways to apply it — the ⭐ arc is **A1 → B5 → D16** (read the vendor-seam → borrow it → build hireui's first LLM feature on it).
