# (C) tabularis — Verdict

> LLM-wiki **v212** · `TabularisDB/tabularis` · 2026-07-17 · routine **v2.7**

## Scoreboard — GOAL-ALIGNED INCLUDE 3/4

| Axis | Call | Why |
|---|---|---|
| **(a) Anthropic / cultural-peer source** | **FAIL** | Andrea Debernardi (`debba`), a disclosed freelance full-stack dev in Genova, Italy — **not Anthropic**, no registered (a)-7 source. Per §41 no name/heritage/locale rescue; the disclosed-individual axis is answered NO. *(He built it **with** Claude Code — a Goal-#1 data-point, not authorship.)* → **#19 19a** first `TabularisDB`/`debba` author. |
| **(b) Goal-relevance** | **STRONG** (keys the tier; cleanly GA — no §40 needed) | A **first-party MCP server** that gives Claude / coding agents managed, secret-free, EXPLAIN-gated **database access** = a genuinely useful new agent capability on the exact MCP substrate the vault studies (`google_workspace_mcp` v140 / `devspace` v171 / `codebase-memory-mcp` v172 / `cortex-hub` v181 / OfficeCLI v206), + a provider-agnostic text-to-SQL seam, + **directly pilotable into hireui's data layer** (a recruitment SaaS is database-backed). **STRONG-not-STRONGEST:** third-party; Claude is one of several agents; the core product is a developer database GUI; capability-augmentation not the Claude/agent core. Calibrates at/near palmier-pro v192's (b) STRONG — arguably a touch stronger (databases are more dev-core than a video timeline). |
| **(c) Substance** | **STRONG** | Mature, production-grade cross-platform desktop app (Tauri v2 + Rust + React 19), 59 releases, v0.15.0, 16+ DB drivers via a language-agnostic JSON-RPC plugin system, a first-party MCP server with 4 tools + read-only mode + approval gates + pre-flight EXPLAIN, SQL notebooks, visual EXPLAIN, ER diagrams, real sponsors (JetBrains/DO/Vercel/1Password). **Caveats:** NOT source-cloned (README/site/posts only, per the shim-overflow self-throttle); stars page-stated → NOT #52; "built by AI" is the author's own framing; text-to-SQL correctness is model-dependent; some plugins in "various development stages." |
| **(d) Connections** | **STRONG** | The palmier-pro v192 §C standalone (direct **N=2** anchor) · the agent-tool MCP-server cluster (v140 / v171 / v172 / v181 / OfficeCLI v206) · #18 B1-MCP · the Tauri-desktop LV-C7 cluster (cc-switch v73 / CodexPlusPlus v117 / OpenHuman v118 / PilotDeck v175 / meetily v196) · the provider-agnostic-LLM / text-to-SQL seam (#18 #8 + #84 84c + meetily v196 `generate_summary()` + OmniRoute v208 / AIRI v210 `xsAI`) · the capability-layer-gives-agents-a-new-domain thread (OfficeCLI v206 / serve-sim v183 / Agent-Reach v174 / browser-use v41) · #66 (agent-with-SQL-rights) · **hireui data layer** · **Kilo Code v177 = a tabularis SPONSOR** (corpus-recursive cross-ref) · the solo-dev-with-Claude-Code dogfooding thread. |

**Decision: GOAL-ALIGNED INCLUDE.** (b) STRONG keys the tier; cleanly goal-aligned (RAG-adjacent — no, *agent-database-access* — is on-domain; no §40/OFF-GOAL question arises).

---

## Pattern outcome: NO NEW MINT — N=2 instance-strengthening of the palmier-pro v192 §C standalone

**`tabularis` is a genuine second instance of the v192 §C standalone "Product-First Native Application Retrofitted with a First-Party MCP Server."** That standalone (row C40) goes **N=1 → N=2**. No new §C row is created (the OmniRoute v208 handling of the v207 standalone exactly).

- **Why N=2, not a new mint:** the load-bearing, already-minted classification is the *relationship* — a product-first, independently-usable human GUI application whose maker also ships that application's own first-party MCP server as a secondary agent-access path. tabularis satisfies that relationship precisely. A database GUI vs. a video editor is **within-class domain variation**, not a new capability mechanism (the camofox v179 / OmniRoute v208 "don't draw the circle" discipline).
- **It SATISFIES the v192 row's "PROMOTION-ELIGIBLE at N=2" note and RESOLVES v192's recorded NO-MINT boundary alternative** — the surface IS a recurring class, so the v192 mint was correct, not inflation.
- **A *stronger* N=2 than OmniRoute v208's:** OmniRoute was a TypeScript **port** of its N=1 anchor (v207); tabularis and palmier-pro are **fully independent, cross-author, cross-domain, non-port** instances (video/Swift/macOS vs database/Rust+TS/Tauri-cross-platform). → **Promotion of the v192 standalone to a CONFIRMED Library-vocab item is a STRONG candidate for the ~v212 audit** — recorded, **NOT self-promoted** (a headline-count-changing promotion is an audit act).

**Counts UNCHANGED: 46 top-level patterns / 11 CONFIRMED Library-vocab.** §C live standalones **42** (unchanged — v192 standalone N 1→2, no new row). Tracked PROVISIONAL surface **≈49** unchanged.

### Reviewable alternatives recorded (operator / ~v212 audit)
1. **NEW §C standalone "MCP-Native Multi-Database SQL Client / Workspace for AI Agents" (N=1, corpus-first for the database-GUI+MCP surface).** Defensible on the OfficeCLI v206 / serve-sim v183 "corpus-first-for-a-tool-surface" precedent (OfficeCLI minted a new standalone for the Office-document *surface*). **LOSES** to the v192 N=2 reading: OfficeCLI is *agent-first* (the MCP surface IS the product; no standalone human GUI), whereas tabularis is decisively *product-first* (a full human SQL workspace + a bolt-on MCP server) — which is precisely the v192 relationship, already minted. Minting a database-GUI standalone = the draw-the-circle over-reach.
2. **The "non-coding domain" clause nuance.** tabularis is developer/data-tooling (coding-adjacent), a notch closer to the coding core than palmier-pro's video editing. Recommend the ~v212 audit generalize the v192 standalone's "in a non-coding domain" clause to "any domain where the product's primary function is **not being an agent tool**" (a database GUI is product-first even though it is developer-facing).

Either alternative → counts UNCHANGED 46/11.

---

## SECONDARY (recorded, NOT minted)

- **#18 sub-archetype B / B1-MCP instance-strengthening → ≈N=12.** One built-in MCP server serving many clients (Claude Desktop / Claude Code / Cursor / Windsurf / Devin). The v203 audit put B1-MCP at ≈N=11; +tabularis → ≈N=12. Capability-vs-distribution → **no double-count** with the §C standalone (the palmier-pro v192 row explicitly tracks this: "the standalone is about the product-first *relationship*, not the one-server-many-clients shape"). Exact tally = audit bookkeeping.
- **#84 84c provider-agnostic text-to-SQL** (OpenAI / Anthropic / MiniMax / OpenRouter / Ollama / any OpenAI-compatible). **NO N-bump** (not the ponytail v168 14-platform generator mechanism). Cross-ref the vendor-seam thread: meetily v196 `generate_summary()` / OmniRoute v208 / AIRI v210 `xsAI`.
- **LV-C7 Tauri-desktop cluster data-point** (+tabularis; cc-switch v73 / CodexPlusPlus v117 / OpenHuman v118 / PilotDeck v175 / meetily v196).
- **#19 19a** first `TabularisDB` / Andrea Debernardi (`debba`) author.
- **Kilo Code v177 = a tabularis SPONSOR** — a corpus-recursive cross-reference (a corpus subject sponsors this subject). ⚠️ **NOT #57** — sponsorship ≠ influence-citation; no corpus subject is cited as an *influence*.
- **Language-agnostic plugin architecture** (JSON-RPC 2.0 over stdin/stdout) — cross-ref the extensibility/plugin thread; the author credits it as the growth flywheel (users → contributors). NOT a mint.
- **Solo-dev-with-Claude-Code dogfooding** — the palmier-pro "built for/with AI" cousin; a Goal-#1 case-study data-point.
- **#66 supply-chain / data-safety** — agent-with-`run_query`-rights over real DBs; text-to-SQL against prod; keychain secrets; SSH tunnels; one-click config writes into agent configs. **Mitigated by design** (read-only mode + approval gates + pre-flight EXPLAIN failing closed on ambiguous/stacked-multi-statement + credentials-stay-local + native-package install, no `curl|bash`). Credited.

---

## NON-claims

- **NOT #52** — 3.7k★ / 247 forks / 59 releases / v0.15.0 page-stated §37.4 → velocity unestablishable (the "0→1000 in 10 weeks" arc is blog-stated).
- **NOT #57** — Kilo Code v177 *sponsors* it, but sponsorship ≠ influence-citation; no corpus subject cited as an influence.
- **NOT world-first** — DBeaver / TablePlus / DataGrip / Beekeeper Studio precede as DB clients; a growing MCP-database-server space (`postgres-mcp`, `dbhub`, `mcp-alchemy`, …) precedes for DB-over-MCP. tabularis's distinctive = a full human GUI + a first-party *multi-DB* MCP server + notebooks + language-agnostic plugins in one product — **corpus-first-for-the-conjunction, not world-first.**
- **NOT a new top-level pattern** (max stays #85).
- **NOT a new §C standalone** (N=2 of v192; the new-standalone reading recorded as reviewable-alternative #1).
- **NOT the first MCP-server subject** (`google_workspace_mcp` v140 etc. precede).
- **NOT the first Tauri-desktop subject** (cc-switch v73 etc. precede).
- **NOT corpus-first for AI-assisted database work generally** (scoped to the product-first-app-with-first-party-MCP conjunction + the database-GUI *domain*).

---

## Tier

The **palmier-pro v192 provisional tier** — *"Native application with an embedded agent-capability layer (first-party MCP server)."* A **T5 Application** (a full, human-usable, installable GUI product) with a **T2-Service** agent-capability facet (the MCP server). Audit-reviewable, matching palmier-pro's provisional label.

## Streak & §35

- **Streak: GA:71 → GA:72** — 58 consecutive goal-aligned ships v153 → v212 (cleanly GA on (b) STRONG; no OFF-GOAL question).
- **§35 CLEAR** — rolling-3-ship window {v210 GA, v211 GA, **v212 GA**} = 0 OFF-GOAL.

## Verification method

Verdict produced **INLINE + fully hand-verified** per `feedback_wiki_verify_independently_check_collisions` — **no workflow / no subagent relied on** (the ~205K CLAUDE.md shim snapshots into every subagent > 200K → `prompt-too-long`; the v200→v211 self-throttle precedent). Source hand-fetched (repo page + raw README + `tabularis.dev` + `tabularis.dev/solutions/mcp-database-client` + the author's DEV/HackerNoon posts); identity by WebSearch (Andrea Debernardi, Genova IT); **collision + domain + pattern-anchor by sanity-anchored hand-grep** of `_state/` + `_patterns/` (anchors OfficeCLI v206 / `google_workspace_mcp` v140 / Kilo Code v177 all hit richly → the empty tabularis/`debba`/DBeaver/SQL-client sections are trustworthy → collision-clean + corpus-first database-GUI domain); the palmier-pro v192 §C standalone (row C40, N=1, PROMOTION-ELIGIBLE-at-N=2 + NO-MINT alternative) hand-read from `_patterns/06`; the OmniRoute-v208-N=2-of-v207 handling template hand-read as the direct precedent. `inflation_check` = discipline HELD (0 new mints; N=1→N=2 bookkeeping on an existing standalone; counts 46/11 unchanged; max #85; no double-count [#18 B1 vs the §C standalone]; the two reviewable alternatives recorded rather than glossed).

**PILOT posture:** on-goal + directly pilotable into hireui's data layer, but **read-only-first, scratch/staging-DB-first** — an agent with SQL rights over your real databases is the load-bearing risk. See `(C) tabularis — Pilot Methods Menu.md`.
