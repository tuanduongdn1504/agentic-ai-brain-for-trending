# OpenCut — Wiki (v146, OFF-GOAL CAPTURE via 8th override + first §35 `[ceiling-override]`)

> ⚠️ **Out-of-corpus-scope.** A free open-source video editor, force-built at operator election (STRICT Phase 0.9 = 0/4 SKIP → 8th lifetime override → **first logged `[ceiling-override]`: §35 BREACHED**). Recorded as corpus-knowledge, NOT goal-#1 progress. No Pattern Library change.
> **Repo:** `OpenCut-app/OpenCut` · MIT · TypeScript 97.9%.
> **§37 provenance:** ≈52.7k★ / 5.7k forks (as of 2026-06, repo page — *stated, NOT API-verified; env mocks GitHub API*).

## What it is
README opener (verbatim):
> "OpenCut: A free and open source video editor for web, desktop, and mobile."

A **video-editing application** positioned as an open-source CapCut alternative, currently undergoing a complete architectural rewrite. TypeScript, MIT.

## The AI angle (roadmap-only — does NOT make it goal-relevant today)
The README roadmap mentions *planned* features: a **"MCP server (for AI agents)"**, **headless mode** (automation, batch rendering), and a **scripting tab** in the editor. These are **unbuilt roadmap items** — there is no SKILL.md / `.claude/` / MCP in the repo. So the subject is off goal #1 *today*; a planned AI feature is roadmap-noise, not current relevance (the v122/v142 discipline, thinner still). ⚠️ If the MCP server actually ships later, the subject could be re-evaluated then on real grounds.

## Why it's in the corpus at all
Force-built via the **8th lifetime operator override** (v84+v116+v122+v127+v139+v142+v145+v146), AND it carries the **first logged `[ceiling-override]`** since §35 was codified at v130 — the off-goal ceiling is now **BREACHED** (2 off-goal in the last 3 ships). It is **corpus-knowledge-of-an-outlier**, not goal-#1 progress, and **not a pilot**.

## Phase 0.9 = 0/4
- (a) FAIL — org, no cultural-peer/identity signal, not (a)-7.
- (b) FAIL — a video editor; planned-MCP is roadmap-only.
- (c) WEAK — substantial TS app, orthogonal domain.
- (d) WEAK — only a future/planned MCP thread.

See `01 Analysis/` for the full verdict + the §35-breach + override accounting (5-in-20 / 8 lifetime).

## Corpus connectivity
**None current.** No shipped agent / MCP / skill / Claude surface (planned MCP = roadmap-noise). NO Pattern Library mint. The meta-fact: this is the first §35 ship-breach — the strongest evidence yet that the lever against off-goal drift is upstream subject-selection, not the soft ceiling (which the operator can always elect to override). ⚠️ **v147 must be GOAL-ALIGNED** (§35.2); v147 + v148 both goal-aligned are needed to fully clear the ceiling.
