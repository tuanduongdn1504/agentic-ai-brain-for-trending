# (C) Phase 4b PRIMARY — Operator-Elected Phase 0.9 Override Policy registration (v84 first instance) + NEW T1 sub-archetype "Linear-Workflow-Pipeline Skill Bundle" PROVISIONAL N=1

**Document type**: NEW routine v2.3 codification candidate registration + NEW T1 sub-archetype registration
**Wiki**: v84 neilsonnn/image-blaster
**Date**: 2026-05-21
**Verdict**: **Library-vocabulary candidate PROVISIONAL N=1** (override policy) + **T1 sub-archetype PROVISIONAL N=1** (linear-pipeline)
**Confidence**: **MEDIUM-HIGH** (clear corpus-first observation; v85+ stale-check determines durability)

## ✅ OPERATOR DECISION (recorded 2026-05-21 post-build)

**Operator declares v84 a ONE-TIME EXCEPTION**, not a precedent.

- Phase 0.9 STRICT discipline remains LOAD-BEARING going forward
- Future Phase 0.9 0-PASS verdicts → default SKIP (not override)
- v84 override is a singular event recorded for audit history; not a normalized pathway
- Override-frequency threshold tracking continues (v84 = 1; 2-in-20 / 3-in-30 thresholds still active)
- If v85+ Phase 0.9 returns 0-PASS, default action = SKIP (not invoke override)

**Streak treatment**: Option B2 (streak-with-override-asterisk "19+1*") accepted by operator for v85 audit administrative.

## 1. The trigger event

At Phase 0.9 STRICT for v84, the 4-criterion check returned **0 clear PASS**:

| Criterion | Verdict | Reasoning |
|---|---|---|
| (a) Author archetype peer | WEAK | Neilson SF creative-tech vs Storm Bear VN Scrum coach; solo-indie axis shared but cultural-locale + domain-vertical distinct |
| (b) Vault-deployable | FAIL | Image-to-3D-world pipeline not Scrum-applicable |
| (c) Methodology-influence-node | WEAK → PROBABLE-PASS-post-settings.json-discovery | 8-skill + per-skill allowlist + 2-hook observable |
| (d) Corpus-citation | FAIL | No v60+ corpus subjects cited |

= **0 clear PASS + 1 WEAK + 1 PROBABLE-PASS-on-revision → SKIP under strict interpretation**.

**Operator-elected force-build override invoked 2026-05-21** with explicit acknowledgement that routine v2.2 SKIP gate would have triggered.

= **FIRST OPERATOR-ELECTED PHASE 0.9 OVERRIDE in routine v2.2 window** (v57-v83 = 19 consecutive auto-Phase-0.9-PASS + 2 SKIPs; no overrides).

## 2. Override policy proposal — routine v2.3 codification candidate

### Override-trigger conditions (when to invoke)

Operator MAY invoke override when:
- Subject is corpus-knowledge-only interest (not vault pilot candidate)
- Phase 0.9 STRICT verdict is 0-PASS but subject has 1+ PROBABLE-PASS-on-revision
- Operator has explicit reason routine cannot see (cross-corpus pattern visible to operator)
- Build size will be COMPACT (no fluff-padding to justify build)

Operator MUST NOT invoke override when:
- Subject has 0 PROBABLE-PASS axes (no path to any criterion)
- Override would be the 3rd+ in a 20-wiki window (threshold breach → v2.3 Phase 0.9 redesign required)
- Override is being used to preserve streak achievement (verdict-launder anti-pattern)

### Override-artifact-trail discipline (how to document)

1. **CLAUDE.md `## ⚠️ OPERATOR-ELECTED PHASE 0.9 OVERRIDE` block** with 4-criterion verdict + override-date + operator-acknowledgement
2. **`(C) index.md` Phase 0.9 override notice** at top
3. **`(C) log.md` honest verdict statement** + override-invocation timestamp
4. **Every entity page** references override openly (no glossing)
5. **Phase 4b PRIMARY proposal-document** (this file) registers the override as routine v2.3 codification candidate
6. **Phase 5 build-learnings** documents override-rationale + dissent

### Streak treatment options (v85 audit must decide)

| Option | Description | Trade-off |
|---|---|---|
| **B1: RESET** | v84 = NOT PASS; streak counter resets to 0 at v85 if Phase 0.9 returns to PASS | Most strict; preserves 19-consecutive-PASS achievement at v83 as historical record |
| **B2: streak-with-override-asterisk** (RECOMMENDED) | v84 = "19 + 1 OVERRIDE" notation; preserves momentum-tracking + routine-discipline integrity | Audit-metric refinement; clear precedent for future overrides |
| **B3: verdict-launder** (NOT RECOMMENDED) | Retroactively call v84 (c) PASS via settings.json discovery; streak counter = 20 | Performative; degrades Phase 0.9 STRICT into rubber-stamp |

**RECOMMENDED**: **Option B2** — streak counter at v84 = "19+1*"; asterisk denotes override.

### Override-frequency threshold (v2.3 trigger)

If overrides reach **2 in 20-wiki window** → trigger v2.3 Phase 0.9 redesign (gate is leaking).
If overrides reach **3 in 30-wiki window** → trigger immediate audit (gate is broken).

v84 = first override; threshold not yet approached.

## 3. PRIMARY 2 — NEW T1 sub-archetype "Linear-Workflow-Pipeline Skill Bundle" PROVISIONAL N=1

### Sub-archetype definition

**Linear-Workflow-Pipeline Skill Bundle** = T1 Assistant Skill organized as **ordered pipeline-step decomposition** with these properties:

1. **Explicit run-order** enforced via SKILL.md prose (e.g., "Use before other image-blast skills")
2. **Workflow-orchestration sub-skill** (entry-point + state-manager + downstream-recommender role; e.g., `image-blast-project`)
3. **State propagation via JSON state files** (e.g., project.json + scene.json + image.json + object.json)
4. **Provider-abstraction with explicit parameter shape** (multi-provider switch with distinct parameter taxonomies)
5. **Atomic-unit discipline at sub-skill granularity** (per-invocation = one atomic unit; not pairs/clusters)

### v84 evidence (anchor instance)

| Property | v84 image-blaster evidence |
|---|---|
| Explicit run-order | `image-blast-project` SKILL.md: *"Use before other image-blast skills"* + downstream-recommendation logic |
| Workflow-orchestration sub-skill | `image-blast-project` = entry-point + state-manager + recommends sequential next-step agents |
| State propagation via JSON | `project.json` + `scene.json` + `image.json` + `object.json` files |
| Provider-abstraction | `image-blast-3d` supports Hunyuan + Meshy with distinct parameter sets |
| Atomic-unit discipline | `image-blast-3d` "atomic object creation — excluding pairs, clusters, or adjacent items" |

= **5-of-5 properties match** at v84.

### Distinction from prior T1 sub-archetypes

| Sub-archetype | Anchor | Organization principle |
|---|---|---|
| Single-skill-multi-harness | v75 impeccable | 1 skill × N harnesses |
| Multi-skill-brand-portfolio | v81 taste-skill | Tri-axis taxonomy (aesthetic + workflow + platform) |
| Monolithic-single-SKILL.md | v82 huashu-design | One 60KB SKILL.md with internal 5-mechanic methodology |
| OSS-Alternative-to-Vendor-Product | v83 open-design | 31 featured + ~280 leaf-skill + 16-harness + 4-distribution-method |
| **Linear-Workflow-Pipeline** | **v84 image-blaster** | **N skills in ordered run-sequence with state propagation + orchestration-skill** |

### Promotion criteria evaluation (PROVISIONAL N=1)

| Criterion | Status |
|---|---|
| 1. Corpus-first | ✅ PASS — first ordered-pipeline T1 skill with explicit run-order in v60+ window |
| 2. Mechanism distinction | ✅ PASS — distinct from prior 4 T1 sub-archetypes at organization-principle layer |
| 3. Cross-instance recurrence at N≥2 | ⏳ PENDING — N=1 at v84; v85+ stale-check |
| 4. Variant-within-pattern | ⚠️ STRETCH — single-instance; not yet a taxonomy |
| 5. HIGH-confidence verdict | ✅ PASS — direct evidence in 3 SKILL.md + settings.json + run-order prose |

= **PROVISIONAL N=1 registration**; v85+ stale-check.

## 4. Routine v2.3 codification candidates contributed by v84

1. **Operator-Elected Phase 0.9 Override Policy with Explicit-Document-Trail** (THIS DOCUMENT)
2. **Streak-with-override-asterisk notation** as audit-metric refinement
3. **Override-frequency threshold (2-in-20 / 3-in-30) as v2.3-trigger**
4. **NEW T1 sub-archetype "Linear-Workflow-Pipeline Skill Bundle"** taxonomy candidate
5. **Workflow-orchestration sub-skill role** as identifiable architectural pattern within multi-skill bundles
6. **State-propagation-via-JSON-files** as pipeline-step-decomposition convention
7. **CORPUS-FIRST per-skill `Skill(name)` allowlist syntax** at settings.json scope
8. **CORPUS-FIRST 2-hook SessionStart + UserPromptSubmit combination** at project-CLAUDE.md scope
9. **CORPUS-FIRST tight Bash-scoping discipline** at settings.json scope
10. **Library-vocab "Multi-Vendor BYOK at Asset-Generation Layer (Non-LLM)"**
11. **Library-vocab "Subtractive Prompt Synthesis Pattern"**
12. **Library-vocab "Atomic-Unit Skill Discipline"**

= **12 candidates contributed** — exceeds v83's 8 because override-policy-itself counts as multi-axis codification.

## 5. v85 audit administrative steps

1. **Decide streak treatment** — RECOMMENDED Option B2 (streak-with-override-asterisk)
2. **Register override-policy as Library-vocabulary item** (PROVISIONAL N=1 → promotion at N=2 in future override)
3. **Register T1 sub-archetype "Linear-Workflow-Pipeline Skill Bundle"** PROVISIONAL N=1; v90 stale-check
4. **Pressure-test v84's 12 corpus-firsts** — flag ad-hoc-coined frames; downgrade where appropriate
5. **Verify Bun-primary CORPUS-FIRST claim** — audit v60-v83 packaging-tool taxonomy
6. **Cross-reference v84 sub-mechanism candidates with Pattern #66** — decide formal Pattern #66 sub-mechanism registration at v85 audit
7. **Update Storm Bear (a) axis-call discipline** — codify when stretch axis-calls (cultural-locale-shared, domain-vertical-distinct) should PASS vs FAIL

## 6. Honest dissent

- **v84 should not be treated as a precedent for routine INCLUDE**. The override is a controlled exception, not a normalized pathway. If overrides become routine, Phase 0.9 STRICT becomes performative.
- **The 8 NEW routine v2.3 codification candidates from the Phase 0 + Phase 2-3 observation work are genuine and would stand even if v84 had been Phase 0.9 STRICT PASS through other axes**. The override does not validate the candidates; the evidence does.
- **Some of the 12 corpus-firsts at v84 are ad-hoc-coined** (e.g., "CORPUS-FIRST SF-located solo creative-tech subject" is a frame Claude invented rather than a naturally-emerging corpus pattern). v85 audit should pressure-test which sub-mechanism candidates are durable + which are reactive.
- **Bun-primary CORPUS-FIRST claim is CANDIDATE** — verification against v60-v83 packaging-tool taxonomy needed at v85 audit before formal Library-vocabulary registration.
- **Streak preservation should not influence override decisions**. If operator finds themselves invoking override "to keep the streak alive" that's verdict-launder territory — the right response is to accept the SKIP and RESET.

## 7. Cross-references

- Entity 3: `02 Wiki/(C) entity-3-operator-override-and-linear-pipeline-sub-archetype.md`
- CLAUDE.md `## ⚠️ OPERATOR-ELECTED PHASE 0.9 OVERRIDE` block (this project)
- Routine v2.2 skill: `05 Skills/llm-wiki-routine-v2.2.md` (vault root)
- Comparison precedents: v74 LLMs-from-scratch (WEAK 1/4) + v80 SmartMacroAI (WEAK 1/4) + 2 prior SKIPs in v57-v83 window
- Streak achievement (pre-v84): `_state/03c-projects-v61-v83.md` v83 entry "streak=19 NEW CORPUS-RECORD"

## 8. Predicted next-wiki signals to watch

- **N=2 candidate for Linear-Workflow-Pipeline Skill Bundle**: future T1 skill with ordered pipeline + orchestration-skill + state-propagation
- **N=2 candidate for Operator-Elected Override**: any future v85+ wiki where Phase 0.9 STRICT returns 0-PASS and operator force-builds (if N=2 within 20 wikis → trigger v2.3 redesign)
- **Bun-primary N=2**: future v85+ subject using Bun as primary packaging tool
- **Per-skill `Skill(name)` allowlist N=2**: future subject using same settings.json `permissions.allow` per-skill syntax
- **2-hook SessionStart + UserPromptSubmit N=2**: future subject using same hooks combination at settings.json scope

If 2+ of these N=2 candidates materialize within 10-wiki window v85-v94 → v84's PROVISIONAL N=1 registrations promote to active candidates at v95 audit.
