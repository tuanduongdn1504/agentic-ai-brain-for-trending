# (C) Phase 4b — Pattern Library contribution — obsidian-second-brain (v134)

**Subject:** `eugeniughelbur/obsidian-second-brain` · **Routine v2.6** · 2026-06-01
**PRIMARY:** Pattern #57 corpus-recursive-at-methodology-influence-layer → **N=3** (PROMOTION-ELIGIBLE; promotion DEFERRED to the imminent ~v134–v135 audit).

---

## PRIMARY — Pattern #57 corpus-recursive-at-methodology-influence-layer → N=3

### The thread, and where v134 lands

The corpus has tracked a recursive event-class: **corpus subjects that are themselves about the corpus's own foundational methodology** (Karpathy's LLM Wiki pattern — the ECC v1/v78 anchor and this vault's premise).

| Instance | Subject | Vector | Note |
|---|---|---|---|
| v94 | Understand-Anything | **consume** | builds a knowledge graph from a codebase; designed "for Karpathy-pattern LLM wikis" |
| v118 | OpenHuman | **generate** | productized Karpathy-LLM-wiki desktop app (Memory Tree + Obsidian-compatible vault) |
| **v134** | **obsidian-second-brain** | **generate (purest)** | **a Claude Code skill that operates your *existing* Obsidian vault AS a self-rewriting Karpathy-LLM-wiki** |

- **Broad event-class → N=3** (v94 + v118 + v134) → **PROMOTION-ELIGIBLE.**
- **"Generate" sub-vector → N=2** (v118 + v134), with **v134 the purest instance**: it doesn't ship a *separate* product that rhymes with the vault (v118) — it's an installable skill that would operate THIS exact substrate (Obsidian + Claude + Karpathy-LLM-wiki).

### Why this is the genuine 3rd instance (and not manufactured)

This is the instance the thread was waiting for, and the discipline trail proves it isn't forced:
- **v132 supermemory was correctly DECLINED** as a 3rd instance, because supermemory is a fact-extraction **memory DB** (RAG + profiles), **not** a Karpathy-LLM-wiki / markdown vault. That decline was the v126-audit "no manufactured corpus-recursive" discipline working.
- **v134 genuinely qualifies** on the same test: it *is* a Karpathy-LLM-wiki operator — it says so explicitly ("an evolution of Karpathy's LLM Wiki pattern"), it operates **Obsidian markdown vaults**, and it implements the maintainer jobs (rewrite-not-append, reconcile contradictions, synthesize, lint/health, AI-first notes-for-future-Claude). The methodological mirror to this vault's own `CLAUDE.md` is near line-for-line (future-Claude-not-human-review; never-fabricate / mark-TBD; recency markers; mandatory wikilinks).

So the same test that *excluded* v132 *includes* v134 — which is exactly the kind of evidence that makes the N=3 honest rather than convenient.

### The honest open question (the audit's call, not mine)

The **v120 audit HELD this at N=2 PROVISIONAL** with the explicit caveat: *"v94 consume + v118 generate — different vectors, not yet one confirmed class."* v134 sharpens, but does not by itself resolve, that question. Two readings, both legitimate, for the ~v134–v135 audit to decide:
1. **Promote the broad class to CONFIRMED** (N=3: any subject that is about / productizes the corpus's own Karpathy-LLM-wiki methodology — regardless of consume/generate direction). v134 makes this defensible.
2. **Split into two adjacent classes**: corpus-recursive-**consume** (N=1, v94) + corpus-recursive-**generate** (N=2, v118+v134). Under this reading the *generate* sub-vector is the one that just reached N=2 and is promotion-eligible at a future N=3.

**I register v134 as the 3rd broad-class instance + 2nd generate-vector instance, flag PROMOTION-ELIGIBLE, and defer the formal promotion + the class-vs-split decision to the audit.** No unilateral promotion; **NO Pattern Library state change at ship** (46 confirmed / 8 Library-vocab CONFIRMED unchanged).

## SECONDARY (observations — §28-disciplined; filed as ACTS)

1. **NEW Library-vocab standalone (N=1) — FILED to `_patterns/06-library-vocab-registry.md` §C this session:** **"Bi-Temporal Fact Discipline in an Agent-Maintained Knowledge Vault."** When a fact changes, never overwrite — append to a `timeline:` array preserving **both** the event-time **and** when-the-vault-learned-it, enabling history/audit queries. CORPUS-FIRST in the v60+ window; promotion-eligible at N=2. (1 new standalone, within the §28 ≤2 cap.) *Notably: a discipline this vault does NOT currently have — a genuine candidate to borrow by hand.*
2. **Pattern #84 84c.2 "CLI-generates-native-formats-from-canonical-source" — N+1** (the v76 agent-skills-standard sub-mechanism). `scripts/build.sh --platform [codex-cli|gemini-cli|opencode]` compiles one canonical source → Claude/Codex/Gemini/OpenCode native command formats + dispatchers. CONFIRMED sub-mechanism; instance noted, not re-minted.
3. **Library-vocab #12 LLM-routing artifacts — N+1** (CLAUDE.md + AGENTS.md + GEMINI.md dispatchers + SKILL.md). CONFIRMED; instance noted.
4. **Pattern #18 sub-mechanism B1 (MCP, one-server-many-clients) — N+1** — MCP server is 1 of 3 vault-access methods (SessionStart hook / MCP / filesystem fallback).
5. **Pattern #82 quantitative-marketing** — "1,000+ stars in 7 weeks", "43 commands", "4 CLIs", "trending repo" badge.
6. **Pattern #66 supply-chain** — `curl -fsSL …/quick-install.sh | bash` install (classic caution) + optional BYOK research keys (xAI/Perplexity/Gemini/YouTube/OpenAI). Load-bearing for the pilot trust assessment (this skill *writes to your vault*).
7. **Versioning-reset observation (NOT minted):** releases ran v2.2.0→v4.0.0 (Apr 5–9) then **reset to v0.5.0 (Apr 26)**. A deliberate rebrand/renumber — **NOT** a clean LV-C3 manifest-drift member (that's cross-*file* drift, not an intentional reset). Noted, not filed.

## Honest non-claims
- **Pattern #57 N=3 is a registration + promotion-eligibility flag, NOT a unilateral promotion.** The consume-vs-generate class question is genuinely open; the audit decides.
- **NOT an agentskills.io 57k implementer.** It ships its OWN `quick-install.sh` + `build.sh` multi-CLI build; there is no evidence of `npx skills add` / agentskills.io registration. Counting it in the 57k chain would be wrong. (Conceptually closer to the v121 CodexKit "parallel distribution mechanism" framing, but spanning Claude/Codex/Gemini/OpenCode.)
- **(b) STRONG, not STRONGEST** (PKM domain not software-dev shipping; influencer positioning; curl|bash footprint).
- **NOT a Pattern #52 promotion** (~30★/d × ~57d = lower-edge SUSTAINED-MODERATE-HIGH).
- **Only 1 new Library-vocab standalone** (bi-temporal), within §28 ≤2; everything else is a Pattern-layer or CONFIRMED-item instance note.
- **NO new tier sub-archetype minted.** It's a T1 Assistant Skill that sits within the corpus-recursive thread; minting a "Knowledge-Vault-Operating Skill" sub-archetype would be redundant with the #57 PRIMARY and inflationary.

## What the audit (~v134–v135) should carry from v134
1. **Pattern #57 corpus-recursive class: promote to CONFIRMED (N=3) or split into consume(N=1)+generate(N=2)?** — v134 forces the decision.
2. The bi-temporal-fact standalone (N=1) — watch for a 2nd instance.
3. Confirm the broad-class definition against Pattern #57's exact wording (as with the v132 Pattern #85 caveat — I'm reading the thread from the shim/audit record, not a line-by-line pattern-definition match).
