# (C) agentpet — Pattern Library Phase 4b (v154)

**Outcome: NO change to confirmed counts (46 patterns / 9 Library-vocab CONFIRMED). PRIMARY = T2 Observability sub-archetype → N=3 (PROMOTION-ELIGIBLE, sub-flavor split flagged). SECONDARY = 1 NEW section-C standalone. §28: 1 new standalone (≤2); registry 06 ACTUALLY edited (rule-5).**

## PRIMARY — T2 "AI-Agent Observability/Metering Tool" sub-archetype → N=3 (PROMOTION-ELIGIBLE)
- Members now: **v89 VibeCodingTracker** (Rust CLI; usage + cost metering across Claude Code/Codex/Copilot/Gemini) + **v109 cclog-cli** (log/metrics CLI) + **v154 agentpet** (native-macOS menu-bar live multi-agent run-state monitor + ambient notification).
- This is the **3rd instance** of the tier sub-archetype registered PROVISIONAL N=1 at v89 and N=2 at v109 → **N=3 → PROMOTION-ELIGIBLE.**
- ⚠️ **Honest sub-flavor split (flagged for the audit, NOT pre-resolved):** v89 + v109 are **usage-metering / log-analysis** (after-the-fact cost/usage observability); v154 is **live, real-time, multi-agent RUN-STATE monitoring + ambient notification** (operational, not analytical). The audit decides: **confirm the broad "AI-agent observability" sub-archetype at N=3**, OR **split** into *metering/usage* (v89+v109, N=2) vs *live-status/operational* (v154, N=1). Recorded as N=3-broad with the split surfaced — not asserted as a clean homogeneous promotion.
- **Layer note:** this is a **tier sub-archetype** (tracked in project state + the audit queue), not a Library-vocab registry-06 item — so the "filing" is this doc + the state-chapter entry + the §F audit note, not a registry-06 row.

## SECONDARY — 1 NEW section-C standalone (N=1, CORPUS-FIRST)
**"Ambient/Affective Multi-Agent Status Surface (reactive desktop-pet UI)."**
- The novel UX device: a **desktop-pet/avatar character that reacts to the AGGREGATE run-state of multiple agents** (working → waiting → done → celebrate) as the primary status display — an *affective/ambient* signal rather than a dashboard, log, or status-bar table.
- Distinct from the metering/dashboard observability of v89/v109 (numbers + logs) and from the LV-C7 Tauri management-GUIs (config/account control-planes). No prior corpus subject uses a reactive pet/avatar as the agent-state surface.
- **Action:** filed to registry 06 §C, N=1, PROMOTION-ELIGIBLE at N=2; time-aware stale-watch (≥15 wikis AND ≥30 days, §28.3). §28 cap: 1 of ≤2 new standalones used.

## Observations (NOT minted)
1. **Swift-primary native-macOS cluster N+1** — v99 cmux + v100 Easydict + v154 agentpet (all Swift-primary native macOS). Observation; NOT a minted cluster (heterogeneous purposes — multiplexer / dictionary / agent-monitor).
2. **NOT a Tauri / LV-C7 instance** — agentpet is native Swift/SwiftUI, NOT Tauri. Explicitly **not** counted toward LV-C7 (which is Tauri-specific). Avoids the "uses-Tauri-is-incidental" trap the v120 audit warned about, inverted.
3. **Pattern #84 cross-tool** — one monitor spanning 7 named agents + a universal `agentpet run -- <cmd>` wrapper. N+1 instance note.
4. **VN cultural-peer cluster N+1** — Nguyễn Thành Đạt (declared-VN); the cluster (v76/v80/v85/v153/v154) is now firmer (v154 = declared full name). **VN observability micro-cluster:** v109 cclog-cli (Lê Thanh Quảng) + v154 agentpet — both VN authors in the observability sub-archetype. Observation; inferred/declared-cultural, NOT minted (the (a)-8 over-minting trap).
5. **Universal-CLI-wrapper-for-any-agent** (`agentpet run -- <command>`) — a capability-to-instrument-any-agent note (cf. the v140/v143 "capability for agents" direction). Observation.
6. **Pattern #18 #8 NOT applicable** — agentpet observes agents; it does not aggregate LLM providers. Not counted (keeps the multi-{provider/agent/account} layer-taxonomy clean: PROVIDER=#8 / AGENT-orchestration=v150 / ACCOUNT-mgmt=v153 / AGENT-observability=v89/v109/v154).
7. **NOT a Pattern #52 claim** — ≈40★ / 114 commits, micro-scale; metrics page-stated, not API-verified (§37.4).
8. **"Online pet library + one-click download"** — a content-distribution sub-mechanism for the affective layer. Observation.

## §28 accounting
- New standalones filed: **1** (ambient/affective pet-UI status surface) — within the ≤2 cap.
- Sub-archetype strengthenings: **1** (T2 observability N=2 → N=3; tier layer, recorded in state + audit queue, sub-flavor split flagged).
- Registry 06 edited: **YES** (rule-5) — new §C standalone row added; §F running log updated.
- Confirmed counts UNCHANGED: **46 patterns / 9 Library-vocab CONFIRMED.** Tracked PROVISIONAL surface ≈22 → ≈23 (+1 standalone). Audit queue now also carries the T2-observability-N=3 promote-or-split call (alongside v153's LV-C7 N=3).
