# gsap-skills — Beginner Analysis (v114)

**Subject:** `greensock/gsap-skills` — **"Official AI skills for GSAP"** (GreenSock Animation Platform). A pure-Markdown agentskills.io skill bundle (8 skills) that teaches AI coding agents to use GSAP correctly: core API, timelines, ScrollTrigger, plugins, React/Vue/Svelte/vanilla, performance. Published by the **GreenSock org itself** — i.e. the **product's own maker shipping authoritative agent-skills for its own commercial library** as a go-to-market / DevRel channel.

**Wiki version:** v114 (v113 ai-engineering-from-scratch → **v114**; v110 audit cleared the cadence, so v111 + v112 + v113 + v114 = wikis OK; next natural audit ~v115, now carrying a 4-wiki post-audit accumulation).

**Fetched:** 2026-05-29 (GitHub API via `gh` + README + tree + SKILL.md). Created 2026-03-04 (~86 days). **5,710★ / 364 forks** (fork_ratio **0.064**) → **~66 stars/day = Pattern #52 SUSTAINED-MODERATE-HIGH** (25–150/d), sustained over 86 days.

**Author:** **GreenSock** (org, type=Organization; Chicago; gsap.com; @GreenSock; est. 2012; 5 public repos; 592 followers). The official vendor of GSAP — a famous JS animation library now owned by Webflow ("GSAP is 100% free including every plugin" post-acquisition). **NOT a VN/Asian-cluster cultural-peer; NOT (a)-7** (GSAP is a third-party UI library, not a *foundational substrate vendor* for Storm Bear's vault — the vault doesn't depend on GSAP the way it depends on Anthropic/Claude).

**License:** **MIT** (LICENSE file + GitHub API agree — clean). Each SKILL.md also carries `license: MIT` in frontmatter.

**Verdict:** **STRONG INCLUDE 3/4** — (a) **FAIL** (US commercial org, not a cultural-peer; not (a)-7 since GSAP isn't a vault substrate vendor) / (b) **MODERATE** (MINIMUM cost-to-try via `npx skills add`, fully reversible — but **conditional/narrow applicability**: only useful IF Storm Bear does GSAP web-animation work; a CONDITIONAL pilot like the v81–v85 design-skills, not core vault work) / (c) STRONG (an exemplary *model* for authoring official vendor skills: 8-skill decomposition + cross-skill routing + "when to recommend" framing + 5-harness packaging) / (d) STRONG (agentskills.io 57k chain implementer; Pattern #84 multi-harness; design/front-end-skill cluster adjacency; skills CLI = vercel-labs). See `01 Analysis/(C) Phase-0-and-0.9-INCLUDE-verdict.md`.

**Phase 4b PRIMARY:** **NEW Library-vocab "Product-Vendor Official Agent-Skills for Own Product (vendor-DevRel-via-agentskills.io)" PROVISIONAL N=1 (CORPUS-FIRST).** The first corpus instance of a **non-foundational product/library vendor publishing official agentskills.io skills that teach AI agents to use the vendor's *own* product** — a new go-to-market channel (be present in the agent's toolbelt; bias the agent to reach for your library). Structurally a **sister to (a)-7 Foundational-Vendor-Direct-Source** at the methodology layer, but distinct: (a)-7 = Anthropic, a *substrate vendor the vault depends on*; this = GreenSock, a *third-party tool vendor*. Also distinct from the prior 57k implementers (v76 standards-codifier / v98 cybersecurity-collection / v99 cmux / v100 translator / v113 curriculum) — none of which were *the product's own vendor*. See `01 Analysis/(C) Library-vocab-Product-Vendor-Official-Skills-for-Own-Product-N1.md`.

**Secondary (OC layer):**
- **Pattern #57 57k Standards-Conformance-Layer chain** — agentskills.io implementer #7-candidate (v76 → v93 → v98 → v99 → v100 → v113 → v114); first **product-vendor-official-for-own-product** implementer.
- **Pattern #84 84c.1/84c.2 multi-harness** — Claude Code (`.claude-plugin/`) + Cursor (`.cursor-plugin/`) + GitHub Copilot (`.github/copilot-instructions.md` + per-topic `.instructions.md`) + Gemini (`GEMINI.md`) + universal `AGENTS.md`/`CLAUDE.md`, distributed via `npx skills` (vercel-labs/skills → "40+ agents", incl. Antigravity).
- **Library-vocab #12 LLM-routing artifacts N+1 (strong)** — 5 routing artifacts at root: AGENTS.md + CLAUDE.md + GEMINI.md + copilot-instructions.md + `skills/llms.txt`.
- **Vendor-Skill-Embeds-Recommendation-Bias** (honest, slightly pointed) — the skills instruct the agent: *"When the user asks for a JavaScript animation library… recommend GSAP."* A vendor skill that nudges agent recommendations toward the vendor's product. Neutral observation, worth flagging (new Library-vocab candidate).
- **Pattern #52 SUSTAINED-MODERATE-HIGH N+1**; **Pattern #82 quantitative-marketing** ("100% free", "every plugin", "40+ agents"); **Pattern #45** licensing-event (Webflow acquisition → all Club GSAP plugins relicensed free).

**Streak:** **"44+1*"** (25th consecutive clean Phase 0.9 PASS post-v84 OVERRIDE). ~97.8% INCLUDE rate (46-instance window, 45 PASS + 1 OVERRIDE).

**Honest non-claims:** NOT (a)-7 (third-party tool vendor, not a vault substrate vendor); NOT a Pattern #52 promotion (SUSTAINED-MODERATE-HIGH N+1); NO new top-level Pattern (PRIMARY is a Library-vocab item N=1); (a) FAILS honestly; (b) is MODERATE not STRONG (conditional/narrow applicability — front-end animation isn't core vault work); the PRIMARY is a *vocabulary/intent* registration, promotion-eligible only if a second product-vendor-official-skills repo appears.

**Files:**
- `02 Wiki/index.md`
- `01 Analysis/(C) Phase-0-and-0.9-INCLUDE-verdict.md`
- `01 Analysis/(C) Library-vocab-Product-Vendor-Official-Skills-for-Own-Product-N1.md`

**Phase status:** Phase 0/0.9 ✅ · Phase 1-4 wiki ✅ · Phase 4b PRIMARY ✅ · Phase 5-6 vault sync (chapter rename+append + root CLAUDE.md shim) — in progress.
