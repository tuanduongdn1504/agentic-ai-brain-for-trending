# (C) Pattern Library — Phase 4b — Echo Loop (v128): off-goal-capture observation (deliberately thin)

**Routine:** v2.5. **Subject:** `echo-loop/Echo-Loop` (consumer AI English-learning app). **Phase 0.9:** 1/4 WEAK INCLUDE (a-only) → **OFF-GOAL CAPTURE via (a)-rescue.** **Pattern Library state change: NONE.**

This document is intentionally thin. An off-goal subject should contribute corpus-knowledge, not pattern-fuel — minting patterns/vocab from it is the exact inflation the v125 audit warned against. The substantive contribution is META (the (a)-rescue capture count + the v2.5 soft-watch), not a pattern.

---

## A. The substantive registration — META: (a)-rescue off-goal capture N=3 + v2.5 soft-watch firing

**(a)-rescue off-goal capture is now N=3** (v80 SmartMacroAI + v123 MoneyPrinterTurbo + **v128 Echo Loop**): off-goal-domain subjects rescued into a WEAK INCLUDE by criterion (a) cultural-peer alone. The v125 audit flagged this channel at N=2; Echo Loop takes it to N=3.

**v2.5 soft off-goal-capture-rate watch is FIRING.** Of the 3 ships under routine v2.5:
- v126 compound-engineering-plugin = **GOAL-ALIGNED** (GA)
- v127 developer-portfolios = **OFF-GOAL CAPTURE via override** (OG [ov])
- v128 Echo Loop = **OFF-GOAL CAPTURE via (a)-rescue** (OG)

→ **GA:1 · OG:2 [1 ov]** — 2 of the last 3 ships are off-goal, and the most recent two consecutively. The corpus is capturing faster than it advances goal #1; v2.5's accounting now makes this visible instead of hiding it inside a "WEAK INCLUDE" rate. **Recommend the next audit formally review the off-goal-capture rate** and decide the outlier-track invocation default (the v127 finding) — both override and (a)-rescue captures now have honest tags, but neither has a default that *discourages* off-goal intake.

**Internal-Claude-Code-discipline-at-an-off-goal-subject is now N=2** (dograh v122 + Echo Loop v128): both ship `.claude/` + `AGENTS.md` + `CLAUDE.md` as internal engineering discipline while being out-of-corpus-scope products. Per the v122 §D note this becomes worth a real Library-vocab registration only if an *in-scope* specimen appears; both instances so far are off-goal, so it is **noted, not minted**.

---

## B. Domain-agnostic / incidental observations (NO state change)

| Observation | Pattern | Strength | Note |
|---|---|---|---|
| `.claude/` + `AGENTS.md` + `CLAUDE.md` | **Library-vocab #12** LLM-routing-artifacts | N+1 (incidental) | Internal dev tooling at an off-goal subject; does NOT change #12's CONFIRMED state. |
| China-Mainland developer + academic anchor | China-cluster | N+1 | v82/v94/v100/v117/v123 + v128; informal cluster strengthening, not a pattern. |
| AGPL-3.0 | **#45** licensing | thin | Strong copyleft for a consumer app; a note, not a typology change. |
| On-device ASR via `sherpa_onnx` + native recognizers | (architecture) | thin | Privacy-leaning on-device speech; off-domain. |

**§28 discipline honored:** ZERO new Library-vocab standalones minted (cap = 2; an off-goal capture should mint none). The registry `_patterns/06-library-vocab-registry.md` is **not** moved. (v120 rule-5: "filing is an act, not a claim" — these are deliberately observations-only.)

---

## C. Honest non-claims

- **NOT** an agent-for-software-development tool; **NOT** a Claude-Code skill/plugin; **NOT** an agentskills.io implementer; **NOT** an MCP server; **NOT** a dev library.
- **NOT** a 2nd instance of v123's T5 "AI Media-Generation Pipeline App" sub-archetype (Echo Loop is a *language-learning/tutoring* vertical, not media generation); **v123's T5 N=1 stays N=1.**
- **NO new T5 sub-archetype minted** for Echo Loop — minting a sub-archetype for every off-goal consumer AI app is the inflation the v125 audit warned against. The informal "off-goal consumer AI app" grouping (v123 + v128) is noted, NOT registered.
- **NOT** a confirmed Pattern #52 instance (~28/d over ~25 days — nascent, not yet sustained).
- **NO** new top-level Pattern; **NO** Library-vocab promotion; **NO** Pattern Library state change.

---

## D. The one thing actually worth carrying forward

Not a pattern — a process signal, and it's the same one v127 raised, now reinforced. Two consecutive off-goal captures (v127 override + v128 (a)-rescue) immediately after v2.5's adoption show that **honest tagging alone does not slow off-goal intake** — it only measures it. v2.5 gave off-goal captures an honest home; it did not give the corpus a reason to prefer goal-aligned subjects. If the operator wants the off-goal-capture rate to fall, the lever is upstream (subject selection), not in the accounting. The corpus now records goal-drift faithfully; closing it is a choosing problem, not a labeling one.
