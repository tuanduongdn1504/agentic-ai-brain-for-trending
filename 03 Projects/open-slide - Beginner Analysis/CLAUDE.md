# open-slide — project context (v135)

**(C) AI-generated project folder.** Subject: [`1weiho/open-slide`](https://github.com/1weiho/open-slide). Wiki #135, built 2026-06-02 under LLM Wiki Routine v2.6.

## One-liner
"The slide framework built for agents" — a TypeScript/React framework + CLI (`npx @open-slide/cli init`) that AI coding agents (Claude Code / Codex / Cursor) drive to **write React** slide decks; fixed 1920×1080 canvas, component-per-slide, pnpm+Turbo monorepo; ships `/create-slide` + `/slide-authoring` + `/apply-comments` skills + `CLAUDE.md`/`AGENTS.md`. By Yiwei Ho (declared Taiwan). MIT. ≈4.4k★.

## Why it's here (and the one honest tension)
- **Cited-to-subject elevation:** open-slide was already in the corpus as a *vendored citation* (v91 html-anything borrowed its 1920×1080 canvas convention and logged it "not yet corpus-relevant"). v135 promotes it to a subject — that elevation is the PRIMARY Pattern-Library contribution (Library-vocab "Cited-to-Subject Elevation" → N=2 with v93).
- **The (b) tension:** it makes *slides*, which is adjacent to (not the center of) goal #1 — but the agent writes a **real React codebase**, which is genuine agent-driven software development. That's why (b) = MODERATE (GOAL-ALIGNED), not FAIL like v133 GordenPPTSkill (template→.pptx, no code). The contrast is documented in the verdict doc so the next audit can challenge it.

## Verdict + Pattern Library impact
- **GOAL-ALIGNED INCLUDE 4/4** — (a) PASS (declared-Taiwan solo dev) + (b) MODERATE + (c) STRONG + (d) STRONG.
- **NO Pattern Library state change** (46 confirmed / 8 Library-vocab CONFIRMED). PRIMARY = "Cited-to-Subject Elevation" N=2 re-registration (not a promotion). Secondaries = instance notes only (Pattern #84 84c N+1, Library-vocab #12 N+1, presentation-tooling cluster observation). ZERO new standalones (§28). No sub-archetype. Not a #52 promotion (velocity unverified). Not a 57k implementer.

## §35 / streak
- §35 **STAYS CLEAR** — {v133 OG, v134 GA, v135 GA} = 1 OG ≤ 1. No override.
- Streak: **`GA:5 · OG:4 [1 ov]`** ("49+3\*" frozen @v125). v135 = 5th GOAL-ALIGNED PASS.

## Pilot
**CONDITIONAL** — reversible scratch trial if a deck is needed in a 30-day window (`npx @open-slide/cli init` + Claude Code `/create-slide` + `pnpm dev`, then discard). Not a vault daily-driver; does not join the operational stack. Study value: a clean model for skill-drives-a-framework authoring.

## Files
- `01 Analysis/(C) Phase-0-and-0.9-INCLUDE-verdict.md` — the gate.
- `01 Analysis/(C) Pattern-Library-Phase-4b.md` — PRIMARY + secondaries + honest non-claims.
- `02 Wiki/index.md` — the wiki page.

## Data caveat
Sandbox mocks the GitHub API → created_at + exact star count NOT API-verified; metadata from repo page / README / releases. Velocity approximate; no Pattern #52 claim made.
