# (C) Pattern Library Phase 4b — Sana (v116)

**Routine:** v2.4. **Date:** 2026-05-29. **Honest headline: this subject contributes essentially NOTHING to the agent/skill Pattern Library, because it is out of scope.** The only substantive registration is META (the 2nd operator override). Everything below is deliberately thin and explicitly non-inflated.

---

## PRIMARY (meta-layer): 2nd operator-elected Phase 0.9 OVERRIDE

This is the real Phase 4b contribution — not a fact about Sana, but a data point about the routine.

- **v84 image-blaster** = 1st operator override (a Claude-Code skill bundle that scored ~0 clear PASS).
- **v116 Sana** = 2nd operator override (0/4; further out of scope — not even an agent skill).
- The two are **32 wikis apart** → does NOT trip the v2.3 override-frequency redesign triggers (2-in-20 / 3-in-30). **No Phase 0.9 redesign forced.**
- But there are now **2 override data points**. Worth a one-line note at the next audit (~v119–v120): is the override mechanism being used to import genuinely-interesting-but-out-of-scope subjects (a healthy escape valve), or is it scope-creep? Two points isn't a trend; flag, don't act.
- **Streak treatment (per v84 Option B2):** "44+1\*" → **"44+2\*"**. The 25-consecutive-clean-PASS-post-v84 record (held at v114) is **broken/reset to 0** (v116 is itself an override).

**Promotion status:** none. This strengthens the existing "Operator-Elected Phase 0.9 Override Policy" v2.3 codification candidate from N=1 → N=2 (still a routine-policy candidate, not a corpus Pattern).

---

## SECONDARY (domain-agnostic, genuinely present)

These hold regardless of Sana's domain, because they're about how a repo *presents/licenses/serves itself*, not what it generates.

1. **Pattern #82 Quantitative-Marketing — STRONG, clean instance.** The README header is saturated with comparative numbers: *20× smaller · 100× faster than Flux-12B · 23.3× speedup · 4K in 20 s · 2K in 4 s · 8 GB VRAM · VBench 84.05 · GenEval 0.82 · FID 5.70.* This is one of the densest #82 surfaces in the corpus. N+1 strengthening; no state change (already CONFIRMED).

2. **Pattern #45 license sub-typology — THIN N+1.** Code is Apache-2.0; model weights carry separate per-collection licensing on Hugging Face. A **code-vs-model-weights license boundary**, structurally adjacent to v98's "Inter-License Boundary at Code-vs-Research-Output Layer." PROVISIONAL/tangential — file as a NOTE, not a new sub-typology (weights-vs-code is a well-known ML-repo convention, low novelty for this corpus).

3. **Pattern #18 sub-mechanism B3 "OpenAI-compatible-API" — TANGENTIAL N+1.** SGLang serving exposes an OpenAI-compatible endpoint, the same protocol thread as v112 freellmapi (B3). Coincidental, not architectural intent toward the agent ecosystem. NOTE only.

---

## NEGATIVE evidence (worth recording)

- **Pattern #52 — NOT an instance.** ~13.3 stars/day sustained over ~595 days = sustained-low, below every Pattern #52 sub-class floor. Profile resembles v100 Easydict (~10.3/d): a long-lived steady-accrual repo, not a viral pulse. Recording this prevents a future reader from mistaking 7,919★ for a velocity signal.
- **No agentskills.io / Pattern #57 57k chain** — no SKILL.md, AGENTS.md, or .claude/. Sana does NOT extend the 10-implementer agentskills.io standards-conformance chain.
- **No (a)-7, no Karpathy lineage, no Pattern #84 multi-harness** — none of the corpus's signature agent/skill patterns are present.

---

## Library-vocab (per v2.4 §28 discipline)

**Zero new registrations.** Nothing here clusters into LV-C1…LV-C7, and the registration cap (≤2 new standalones/wiki) is best spent on nothing rather than minting out-of-domain noise. The one candidate one *could* coin — "generative-vision research model imported via override" — is an N=1 outlier with no plausible 2nd instance; per §28 clustering-first + cap discipline, **omit**.

---

## State after v116

| Field | Value |
|---|---|
| Confirmed Patterns | **46** (UNCHANGED) |
| Active candidates | ~25 (UNCHANGED) |
| Library-vocab CONFIRMED | **8** (UNCHANGED) |
| Library-vocab PROVISIONAL | ~11 tracked in registry (UNCHANGED — no new registrations) |
| Routine version | **v2.4 CURRENT** |
| Streak | **"44+2\*"** (was "44+1\*"; 2nd override; post-override clean-PASS counter reset to 0) |

**Known pre-existing inconsistency flagged (NOT reconciled here):** the v114 state carries both a "44 PASS" streak figure and a "45 PASS / 46-instance-window" figure that disagree. This is a count-bookkeeping discrepancy that predates v116; reconciling it is an **audit task**, not a wiki-ship task. Flagged for the next audit (~v119–v120) alongside the override-frequency note. I am preserving the existing "44+N\*" notation rather than silently picking a number.

---

## Storm Bear's blunt take

This wiki exists because you told me to build it, and I built it honestly: Sana is a genuinely excellent piece of efficient-vision research that has **nothing to do with your goal or your corpus.** The page is worth keeping as a clearly-labeled outlier and a clean Pattern #82 specimen, but it is not progress on "master Claude and autonomous agents" — it's a detour you chose with eyes open. The one thing the next audit should actually look at is the override series: 2 overrides now (v84, v116), 32 wikis apart, both used to import out-of-scope subjects. That's fine as an escape valve; it'd be a problem if it became a habit.
