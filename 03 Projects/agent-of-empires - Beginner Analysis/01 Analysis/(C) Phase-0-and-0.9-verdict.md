# (C) agent-of-empires — Phase 0 scope gate + Phase 0.9 four-axis verdict

**Subject:** `agent-of-empires/agent-of-empires` (AoE) — canonical owner-fork `njbrake/agent-of-empires`. **v162 wiki ship, 2026-06-15.** **Routine:** v2.6 (§31 / §35 / §37).
**Requested:** "build LLM wiki for https://github.com/agent-of-empires/agent-of-empires."

> **⚠️ Version-numbering catch (recorded up front):** the request said nothing about a number, and the root-shim was **stale at v160**. Git is authoritative: commit `6c40e6a` already shipped **v161 = CodePilot** (op7418's desktop GUI client for Claude Code, GOAL-ALIGNED INCLUDE 4/4, which advanced the streak to GA:24), and the chapter file was already `03c-projects-v61-v161.md`. The shim never recorded CodePilot (`grep CodePilot CLAUDE.md` = 0). So **agent-of-empires = v162**, the true streak goes **GA:24 → GA:25**, and the §35 window is {v160, v161, v162}. This would have been a v157-style collision had the shim been trusted. The shim is brought current + the drift flagged in this ship.

## Phase 0 — scope gate
**In scope.** AoE is a **session manager / multiplexer for AI coding agents** — it runs multiple agents (Claude Code + 12 others) in parallel, each in its own isolated tmux session on a git worktree, driven from a terminal TUI or a browser web dashboard (+ PWA / phone). Squarely goal #1 ("master Claude and autonomous agents for software development") and — uniquely in the recent run — **literally Storm Bear's own workflow** (running parallel Claude Code agents across branches to build these wikis). It belongs to the **agent-orchestration / session-multiplexer layer** of the corpus's "tools to operate coding agents" map (see Phase 4b), distinct from the v153→v160 observability/monitor niche.

## Phase 0.9 — STRICT four-axis verdict: **GOAL-ALIGNED INCLUDE 3/4**

**(a) Cultural-peer / vendor-direct — FAIL.**
Author = **Nate (Nathan) Brake** (`njbrake` / @natebrake), an ML Engineer at **Mozilla.ai**; "Maintained by the Agent of Empires community, with support from Mozilla.ai." A named **US individual** + a **foundation-lab-backed community OSS** project. That is **not** a VN/Asian-cluster cultural-peer, and **not** (a)-7 Foundational-Vendor-Direct-Source. The Mozilla.ai-(a)-7 question was **raised and rejected**: (a)-7 is reserved for a substrate vendor the **vault depends on** (Anthropic-tier — the thing Claude Code runs on); Mozilla.ai ships agent OSS but the vault does not run on Mozilla.ai infrastructure, so it is not a substrate dependency. Notable backing ≠ (a) PASS (the v139 / v159-steipete discipline — a notable author/backer mints **no axis**). **(a) FAILS; no axis minted.** (b) carries the include.

**(b) Goal-relevance — STRONG (not STRONGEST).**
A session manager that **runs multiple coding agents in parallel** — isolated tmux sessions, one git-worktree per agent/branch, optional Docker/Podman/Apple-Container sandboxing, drivable from a TUI, a web dashboard, or your phone, with an HTTP API for external orchestrators — is dead-center "operate autonomous agents for software development." It is **more operationally on-goal** than the v153→v160 observability run (which only *watched* agents and still scored (b) STRONG), and it is **the single most pilotable subject of the entire v153→v162 run** for this vault specifically. **STRONG-not-STRONGEST** because the **STRONGEST bar is reserved for the agent *substrate*** (Claude/Anthropic — the thing you master) **or a *methodology*** (v126 compound-engineering = STRONGEST). AoE is a third-party **harness around** the agents — the exact layer where v99 cmux, v150 Paseo (held "near-STRONGEST" but explicitly *not* STRONGEST), and v153 ai-switcher all sit. STRONG is the calibrated rating; STRONGEST would break calibration with its nearest neighbors; MODERATE/OFF-GOAL is indefensible (it operates agents; it is not a finance app / video editor / English guide).

**(c) Engineering quality — STRONG.**
Rust 68.7% + TypeScript 29.7%; **1,370 commits / 103 releases / v1.11.0** (Jun 10 2026) in ~5 months. tmux sessions + git worktrees + Docker/Podman/Apple-Container sandboxing + ratatui TUI + a `--features serve`-gated Axum-style HTTP server + a React 19 / Vite / Tailwind v4 / xterm.js PWA + an `acp-worker/` Agent-Client-Protocol worker + Nix flake. The standout engineering pattern: a **single-source-of-truth config via a `#[setting(...)]` Rust attribute macro** — one field declared once auto-generates the TUI + web + server config surfaces through schema derivation ("no hand-maintained allowlists or override structs"); theming is unified the same way (a `Theme` struct → a server-projected `ResolvedTheme`; a new theme is "a one-file drop"). Heavy CI rigor for a ~5-month solo-led project: Codecov 75% patch-coverage gate + no-regression, dual Playwright suites (mocked + live-backend) with a required `coverage-matrix.json`, git-cliff auto-changelog, enforced conventional-commit PR titles, weekly automated staging-PR releases.

**(d) Corpus connectivity — STRONG.**
The genuine **2nd instance of cmux's "session-hosting multiplexer for AI coding agents (human-driven)" species** → that species reaches **N=2** (v99 cmux native-desktop + v162 AoE cross-platform-TUI+web); decomposes the v151/v158 "AI-agent orchestration theme HELD heterogeneous" finding (see Phase 4b). Distinct from v150 Paseo (agent-of-agents platform, stays N=1 — AoE is the *inverse*: a thing **to be** orchestrated via its HTTP API) and from v161 CodePilot (GUI-client front-end, not a multiplexer). Pattern #84 cross-tool (13 auto-detected agents — among the broadest in corpus, ties v156/v160). OpenClaw corpus-recursive (the HTTP API "integrates with tools like OpenClaw"; v149/v156/v160) + Hermes corpus-recursive (one of the 13 agents; v78 ECC / v112). ACP (Agent Client Protocol) made architecturally first-class — a Pattern #18 sub-mechanism-B protocol-variant *candidate* (B1 MCP / B2 CDP precedent), NOT a §C standalone. Pattern #66 supply-chain (curl|bash install + Tailscale/Cloudflare-Tunnel public-internet exposure). The internal CLAUDE.md/AGENTS.md/DESIGN.md = the team builds *with* Claude Code (the v122 internal-dev-exhaust thread — observation, not product-relevance).

## §35 — STAYS CLEAR
Rolling-3 window {v160 GA, v161 GA, **v162 GA**} = 0 OG (**10 consecutive goal-aligned ships v153→v162**). NOT an override → override-frequency UNCHANGED (lifetime 10). Streak GA:24·OG:11 [7 ov] → **GA:25·OG:11 [7 ov]** (25th GOAL-ALIGNED PASS).

## §37 — provenance (page-stated, NOT API-verified)
≈**2.6k★** / 226 forks / 14 watchers / **105 open issues** / **1,370 commits** / latest **v1.11.0** (Jun 10 2026) / **103 releases** / MIT / **Rust 68.7% + TypeScript 29.7%** (+ JS/Astro/CSS/Shell) / Linux + macOS; tmux required, Docker optional.
- Page-stated as of 2026-06 via WebFetch of the rendered repo + agent-of-empires.com + the Better Stack community guide — **NOT independently API-verified (§37.4)**; this environment mocks the GitHub API.
- **Age/velocity (inferred, not a claim):** Show HN **2026-01-12** (HN item 46588905, 118 points / 44 comments, by "river_otter") + earliest release glimpsed **v0.0.4 "Jan 9"** → created **~Jan 2026, ~5 months old**, ≈ **~17★/day** = sustained-low/moderate. The ~17★/day is **derived from an inferred birth date** → **NOT a velocity claim and NOT a Pattern #52 claim.**
- **105 open issues vs ~2.6k stars** = an "active-but-rough" ratio (the v150 Paseo 395-issues precedent) — consistent with a fast-moving (~1 release every 1–2 days) ~5-month project.

## Honest non-claims
- **(a) genuinely FAILS** — US individual + Mozilla.ai-backed community OSS; not a cultural-peer, not (a)-7 (Mozilla.ai raised-and-rejected: ships agent OSS but is not a vault substrate dependency); notable ≠ (a) PASS (v139). No axis minted. The Mozilla.ai backing is a (d)-connectivity note.
- **(b) STRONG, not STRONGEST** — STRONGEST is reserved for the substrate (Claude/Anthropic) or a methodology (v126); AoE is a third-party harness around the agents (the cmux/Paseo/v153 calibration).
- **0 new §C standalones** — the PRIMARY is a tier-layer sub-archetype strengthening (cmux-species N=2), not a mint. ACP is downgraded to an observation (NOT corpus-first; future home = Pattern #18 sub-mechanism B protocol-variant, not a §C standalone). §28 cap used at 0.
- **NOT Pattern #18 #8** — AoE auto-detects/multiplexes agent **sessions**; it does not aggregate LLM **providers** (the PROVIDER-vs-AGENT-vs-MCP-server layer line, v151 audit). **NOT** the v158 observability sub-archetype — AoE **orchestrates/hosts** sessions (status-detection / diff-viewing / ACP plan-panels are OBSERVE *sub-features*; file-editing is a mild CONTROL *sub-feature*; the **primary** layer is orchestration). **NOT** LV-C7 #22 (not Tauri; manages session/execution, not provider/account config). **NOT** v161 CodePilot's "Desktop GUI Client" standalone (AoE multiplexes a *fleet* of terminal sessions; CodePilot is a single-agent *visual front-end* that replaces the terminal — different species; AoE's web dashboard is a fleet-management surface, not a single-agent client).
- **NOT a #52 claim** (velocity inferred-from-inferred + NOT API-verified §37.4).
- **Internal CLAUDE.md / AGENTS.md / DESIGN.md** = the team builds *with* Claude Code (a skill-routed dev workflow) — the **v122 internal-dev-exhaust** discipline: that is the build process, **not** the product; recorded as an observation, not pattern-fuel. (Notably the **first GOAL-ALIGNED member** of that thread — prior members v122/v128/v142/v152 were all off-goal.)
- **The "Age of Empires" pun is intentional branding** (DESIGN.md's namesake "Empire" theme), not a coincidence — a stylistic note, not corpus-relevant.
