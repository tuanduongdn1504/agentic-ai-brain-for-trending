# (C) agent-of-empires — Pattern Library Phase 4b

**Subject:** `agent-of-empires/agent-of-empires` (AoE), canonical fork `njbrake/agent-of-empires` (v162). **Verdict:** GOAL-ALIGNED INCLUDE 3/4. **Ships into:** the v158-audit-CONFIRMED Pattern Library.

## PRIMARY — the genuine 2nd "session-hosting multiplexer for AI coding agents" instance → species N=2 (DEFER confirm/rename to the audit)

**The corpus's current orchestration-theme record** (02b layer-taxonomy line, v151 audit): the "AI-agent orchestration" theme — **v150 Paseo** + **v99 cmux** [recorded only as *"session-hosting, human-driven — adjacent"*] + **v131 harness** [team-architecture *generator* — adjacent] — was held **HETEROGENEOUS → NOT minted**; the Paseo standalone stays N=1.

**AoE is the genuine 2nd instance of the *session-hosting multiplexer* species that cmux anchored:**

> **Session-hosting multiplexer for AI coding agents (human-driven):** hosts N parallel coding-agent sessions, one per branch/worktree, a terminal-multiplexer underneath, a UI with attention-signaling — a **human** picks branches, spawns sessions, and approves/answers. cmux: libghostty + Swift + native-desktop + notification-rings. **AoE: tmux + Rust/ratatui TUI + a React web dashboard/PWA + swipe-to-approve + push.** Same core abstraction; the deltas (native-desktop vs cross-platform-TUI+web delivery; git-worktree-per-agent; Docker/Podman/Apple-Container sandboxing; remote-phone-over-HTTPS) are **delivery/implementation variation, not a different KIND.**

So the **session-multiplexer species reaches N=2** (v99 cmux + v162 AoE) → **PROMOTION-ELIGIBLE.** This **refines** the held "heterogeneous" finding: the theme is less heterogeneous than it looked — it **decomposes** into

- a **session-multiplexer species** (cmux + AoE, **N=2**, human-driven, hosts/multiplexes parallel sessions),
- **Paseo's agent-of-agents orchestration platform** (**N=1**, distinct — see below), and
- **harness's team-architecture generator** (adjacent).

**DEFER the CONFIRM-or-split + rename decision to the overdue audit (do NOT pre-decide it here — the v158 "don't generalize-the-definition-to-fit-the-2nd-instance" discipline / gap A from verification).** cmux's working label "Native-Desktop Terminal Multiplexer" encodes *three* specifics AoE does not share (native-desktop; libghostty; terminal-only). The honest options are (i) generalize the name to a delivery-agnostic "session-hosting multiplexer for AI coding agents," with **two candidate sub-flavors** — native-desktop (cmux) / cross-platform-TUI+web (AoE) — or (ii) keep them as distinct sub-flavors of one confirmed species. That is the audit's call, not the ship's. **0 new standalones; tier-sub-archetype-layer note (filed to 02b).**

**Why Paseo stays N=1 (and AoE is its *inverse*):** Paseo's load-bearing mint is the `/paseo-*` **skill suite that teaches one agent to orchestrate the OTHERS** (agent-of-agents recursion). AoE has **no** such recursion — a *human* is the orchestrator. Sharper than "AoE lacks the skill suite": AoE's CLI + HTTP API explicitly let it **be driven by external orchestrators** ("integrates with tools like OpenClaw") — AoE is a thing **to be orchestrated**; Paseo is the thing that **orchestrates**. Inverse roles → different species. (The very logic the v150 registry used to fence Paseo off from cmux fences AoE off from Paseo too.)

**Why AoE ≠ v161 CodePilot's "Desktop GUI Client" standalone:** CodePilot is a single-agent **visual front-end** that *replaces the terminal* (you chat/code/file-browse/git in a GUI instead of a terminal). AoE **multiplexes a fleet** of normal terminal sessions (the agents still run in their usual UX inside tmux); its web dashboard is a **fleet-management** surface, not a single-agent client. Different species — AoE strengthens cmux, CodePilot is its own §C standalone.

## SECONDARY — observations (NOT minted)

- **ACP (Agent Client Protocol) — OBSERVATION, future home = Pattern #18 sub-mechanism B, NOT a §C standalone.** AoE makes ACP architecturally first-class (a top-level `acp-worker/` dir + an ACP-driven "structured view": plan panels / tool-call cards / swipe-to-approve, mobile-first, dual-rendered with a raw-tmux fallback). But (1) ACP is a **consumed external open standard** (Zed-ecosystem, Apache 2.0, crates.io `agent-client-protocol`, 5-language SDKs), not AoE's invention; (2) ACP is **NOT corpus-first** — it recurs incidentally across ≥3 prior corpus subjects (**v60 free-claude-code** "JetBrains ACP" client-surface; **goclaw** + **get-shit-done** project folders reference it). AoE is merely the **first to make it architecturally first-class.** A mint demands corpus-first → fails the gate. The corpus's principled home for agent protocols is **Pattern #18 sub-mechanism B / protocol-variants** (B1 MCP, B2 CDP, B3 reserved — promoted at the v72 audit so the next protocol slots in as a variant). When MCP — a genuinely structural protocol — appeared, it became Pattern #18 B1, *not* a Library-vocab mint. **ACP, if ever warranted, is a B3/B4 protocol-variant CANDIDATE within Pattern #18 B; recorded now as an OBSERVATION (first architecturally-first-class instance), promotion-eligible to a tracked B-variant at a genuine 2nd ACP-first-class subject.**
- **Pattern #66 supply-chain** — `curl -fsSL …/install.sh | bash` install **AND** the `R`-to-expose-the-dashboard-over-HTTPS feature puts your dev dashboard on the **public internet** via Tailscale Funnel / Cloudflare Tunnel (QR + passphrase). A genuine attack-surface note (more substantive than v117/v134's curl|bash) **and the key pilot-fence.** Observation + pilot caveat, not a mint.
- **Pattern #84 cross-tool tolerance** — **13 auto-detected agents** (Claude Code / OpenCode / Mistral Vibe / Codex / Gemini / Antigravity / Cursor / Copilot / Pi.dev / Factory Droid / Hermes / Kiro / Qwen) = an 84c N+1 instance, among the broadest agent-coverage in corpus (ties v156 clawd-on-desk / v160 ping-island). *Caveat:* the 13-agent breadth is via **tmux auto-detect**; the rich **ACP structured view is a narrower per-ACP-capable-agent subset.*
- **OpenClaw + Hermes corpus-recursive** — the HTTP API "integrates with tools like OpenClaw" (OpenClaw recurs v149 Scrapling / v156 clawd-on-desk / v160 ping-island); Hermes is one of the 13 agents (v78 ECC Hermes lineage / v112). #84 / corpus-recursive observations.
- **v122 internal-dev-exhaust thread → N=5 (and the FIRST goal-aligned member).** Internal CLAUDE.md + AGENTS.md (which routes the team's *own* dev agents to named skills — office-hours/investigate/ship/qa/review/…) + a DESIGN.md = Mozilla.ai's team **dogfoods a skill-based Claude Code dev workflow to build AoE.** Per v122/v128/v142/v152, that is the build process, **not** the product → observation, not pattern-fuel, NOT a mint. Thread now v122+v128+v142+v152+v162 (N=5); v162 is the first **goal-aligned** member (prior 4 off-goal) — mildly notable, still not minted.
- **Single-source-of-truth config (`#[setting(...)]` Rust macro → TUI+web+server surfaces) + server-projected theming (Theme → ResolvedTheme).** Genuinely distinctive engineering (supports (c) STRONG); observation, not minted.
- **DESIGN.md is an aesthetic design-LANGUAGE doc** ("Industrial Warmth," warm amber/copper, an explicit anti-"Linear-style-template" stance, benchmarked against Warp/Zed/Ghostty) — unusual to ship a full brand/design-system doc for a terminal tool; the namesake "Empire" theme confirms the Age-of-Empires pun is intentional. Observation.
- **Nix flake packaging** recurs (v68 zero / v118 OpenHuman / v150 Paseo) — thin packaging observation, not a mint.
- **agent-deck (Go + Bubble Tea)** is credited in the README as inspiration — a corpus-adjacent prior-art node; a cited-to-subject-elevation watch (the v135 open-slide mechanism) if it later becomes a subject.
- **Boundary-map caveat (don't over-claim clean separation):** AoE *primarily* ORCHESTRATES/hosts sessions, but it has real OBSERVE surface (status-detection, diff-viewing, ACP plan-panels) and a mild CONTROL surface (file-editing). Resolved the way the OBSERVE/CONTROL/ORCHESTRATE map intends — **primary = orchestration/session-hosting; observe/edit are sub-features** — and said so, rather than asserting a clean single-layer claim.

## Counts after this ship
**46 patterns (UNCHANGED) / 10 Library-vocab CONFIRMED (UNCHANGED).** Session-multiplexer species **N=1 → N=2** (cmux + AoE; tier-layer, recorded in 02b; confirm/rename DEFERRED to the audit). **0 new §C standalones** (§28 cap used at 0; ACP downgraded to observation). Tracked PROVISIONAL surface ≈25 UNCHANGED (the cmux-species note is a tier-layer item, not a new §C row; the v161 CodePilot ship had taken it to ≈25). Streak **GA:25·OG:11 [7 ov]**; §35 CLEAR.

## The blunt note (carried to the response)
AoE is a clean GOAL-ALIGNED 3/4 and a genuinely additive ship (the 2nd session-multiplexer instance lets the held orchestration theme finally be decomposed at the audit). But it is the **10th consecutive "tool for operating AI coding agents" subject (v153→v162), ZERO piloted.** v161 CodePilot **already** broke out of the menu-bar-monitor sub-niche, so AoE does **not** "rescue" anything — it's a follow-on, and the standing lever is unchanged and now **louder**: AoE is the **single most pilotable** subject of the whole run (it's literally this vault's own workflow — run parallel Claude Code agents across branches, drive from the phone). The high-value next move is to **pilot it** (or another of the run), not catalogue a v163. **Pilot fence:** `curl | bash` install + the `R` remote-exposure tunnel → `install-snapshot` first, and do **not** enable remote exposure on a real repo without understanding the Tailscale/Cloudflare tunnel.
