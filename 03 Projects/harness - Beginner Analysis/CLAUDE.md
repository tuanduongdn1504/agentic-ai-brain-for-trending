# harness — Project Context

**Subject:** [`revfactory/harness`](https://github.com/revfactory/harness) — "Harness — The Team-Architecture Factory for Claude Code."
**Wiki version:** v131 (Routine **v2.6** — FIRST ship under v2.6 / §35 soft off-goal-rate ceiling).
**Status:** SHIPPED 2026-06-01. **GOAL-ALIGNED INCLUDE (3/4 STRONG)** — (b)(c)(d) STRONG + (a) WEAK/geographic. **Satisfies the breached §35 ceiling legitimately — no ceiling-override used.**

## One-line

Claude Code meta-skill / plugin that turns a one-sentence domain description into a coordinated **agent team** (`.claude/agents/`) + the **skills** they use (`.claude/skills/`) + an orchestrator, picked from **6 team-architecture patterns** (Pipeline / Fan-out-Fan-in / Expert-Pool / Producer-Reviewer / Supervisor / Hierarchical-Delegation). Single `skills/harness/SKILL.md` (Korean-primary) running an 8-phase workflow (Audit→Domain→Architecture→Agents→Skills→Orchestration→Validation→Evolution) + 6 reference guides. Requires the **experimental Agent Teams** flag. Apache-2.0, HTML(=GH-Pages-site, content is Markdown), v1.2.0. By **Minho Hwang** (`revfactory`, South Korea, @kakao). **4,851★ / ~67d / ~72★/d SUSTAINED-MODERATE-HIGH.** Dogfoods itself (`_workspace/` = a harness-generated team that audited+planned harness's own launch). Sisters: `harness-100` (100 generated harnesses) + `claude-code-harness` (A/B study, +60% quality, n=15 honest-disclosed).

## Pattern Library impact

**PRIMARY — NEW T1 sub-archetype "Agent-Team-Architecture Factory" (PROVISIONAL N=1, CORPUS-FIRST):** a meta-skill whose output is a *bespoke multi-agent team* (agents + their skills + orchestrator) synthesised per-domain via named team patterns. Distinct from skill-creator v95 (single skills), compound-engineering v126 (ships a fixed team), Understand-Anything v94 (runs a fixed pipeline). Promotion-eligible at N=2 (README names non-corpus neighbors Archon/meta-harness/harness-init/OpenRig in the same "L3 Meta-Factory" layer). Tier-sub-archetype layer, NOT a Library-vocab standalone.

**SECONDARY (cluster strengthening / observations — §28 disciplined):** **LV-C3 "Self-Referential-Methodology-Demonstration" → N=3** (v87 + v126 + v131 `_workspace/` self-audit; promotion-eligible at next audit; honest caveat: artifacts not explicitly stamped "generated by harness" — structural evidence only); agentskills.io SKILL.md chain another-implementer (administrative; exact count flagged for audit per v115); Pattern #82 quantitative-marketing + Pattern #83 honest-deficiency-disclosure (sister-repo "+60% / n=15, replications pending"); Pattern #84 NEGATIVE-evidence (deliberately single-harness — "deep over multi-runtime"); Pattern #57 corpus-recursive (cites ECC v1/v78); Library-vocab #12 routing artifacts N+1.

**§28 filing — DONE not claimed:** LV-C3 N=2→N=3 strengthening actually written into `_patterns/06-library-vocab-registry.md` this session. **ZERO new Library-vocab standalones; the new sub-archetype is a tier-layer item; NO Pattern Library state change** (46 confirmed / 8 Library-vocab CONFIRMED unchanged). **Honest non-claims:** (b) STRONG not STRONGEST (experimental flag + Korean skill); LV-C3 3rd instance is a *soft* N=3 (no explicit generator stamp); NOT identical to v107 claude-code-harness (same name, different repo/author); sub-archetype is N=1.

## §35 ceiling status (v2.6)

**Ceiling was BREACHED** entering this ship (v127/v128/v129 = 3 off-goal in last 3). **v131 = GOAL-ALIGNED INCLUDE → satisfies the §35.2 mandate** (the next ship while breached MUST be goal-aligned) **legitimately, no `[ceiling-override]` used.** The rolling-3-ship window after v131 = {v128 OG, v129 OG, **v131 GA**} = 2 OG → **still technically breached**; it clears once one more goal-aligned ship scrolls v128 out (i.e. a goal-aligned v132 → window {v129 OG, v131 GA, v132 GA} = 1 OG ≤ 1 → CLEAR). No override-frequency trigger trip (v131 is not an override).

## Streak (v2.5 §32 forward / v2.6)

Historical **"49+3\*" frozen @v125**. Forward: **`GA:1 · OG:3 [1 ov]` → `GA:2 · OG:3 [1 ov]`** (v131 = 2nd goal-aligned PASS under v2.5/v2.6). Override subset `[1 ov]` UNCHANGED.

## Cadence

v130 audit done → "v131+ = wikis OK (but §35 requires v131 goal-aligned)." v131 is goal-aligned → compliant. Next natural audit ~v134–v135. 1 ship since the v130 audit.

## Files

- `02 Wiki/index.md` — wiki page.
- `01 Analysis/(C) Phase-0-and-0.9-INCLUDE-verdict.md` — GOAL-ALIGNED INCLUDE gate + §35 ceiling analysis.
- `01 Analysis/(C) Pattern-Library-Phase-4b-T1-Agent-Team-Architecture-Factory-N1.md` — NEW sub-archetype N=1 + LV-C3 N=3 + secondaries.

## Pilot note

**Goal-aligned but CONDITIONAL/fenced** — requires the experimental Agent Teams flag + Korean-primary skill. If trying: `install-snapshot` → `/plugin marketplace add revfactory/harness` → `/plugin install harness@harness-marketplace` → set `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` → on a scratch project run "build a harness for code review" + inspect generated agents/orchestrator. Reversible. **Highest-value even without installing:** read `references/agent-design-patterns.md` + `team-examples.md` (a compact multi-agent-orchestration reference + a model for authoring your own team skills). The un-piloted **v126 compound-engineering** is still the more turnkey goal-#1 pilot (no flag, English); harness is its generator/architecture-study complement.
