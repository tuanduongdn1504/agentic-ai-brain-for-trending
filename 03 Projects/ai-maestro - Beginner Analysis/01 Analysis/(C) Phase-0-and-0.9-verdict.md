# (C) ai-maestro — Phase 0 scope gate + Phase 0.9 four-axis verdict

**Subject:** `23blocks-OS/ai-maestro` — "AI Maestro." **v163 wiki ship, 2026-06-15.** **Routine:** v2.6 (§31 / §35 / §37).
**Requested:** "build LLM wiki for https://github.com/23blocks-OS/ai-maestro."

> **Numbering:** git is authoritative and current at v162 (main HEAD `1b8be88` = the v162 mini-audit; `acbdd00` = the v162 agent-of-empires ship). The shim head was brought current at v162. So this is a clean **v163** — no collision, no drift this time.

## Phase 0 — scope gate
**In scope.** AI Maestro is a **multi-vendor orchestration platform for AI coding agents** — it manages Claude Code / Codex / Aider / Cursor / OpenClaw / Hermes / Droid (or any terminal agent) from one dashboard, gives them persistent memory + code-graph queries + **agent-to-agent messaging**, and moves agents across machines. Squarely goal #1 ("master Claude and autonomous agents for software development"), and — like v162 AoE — close to **Storm Bear's own workflow** (the README's origin story is the exact pain: *"I was running 35 AI agents across multiple terminals and became the human mailman between them"*). It belongs to the **agent-orchestration layer** of the corpus's "tools to operate coding agents" map (see Phase 4b) — specifically the **orchestration-PLATFORM** bucket (the v150 Paseo bucket), not the human-driven session-multiplexer species (v99 cmux / v162 AoE).

## Phase 0.9 — STRICT four-axis verdict: **GOAL-ALIGNED INCLUDE 3/4**

**(a) Cultural-peer / vendor-direct — FAIL.**
Author = **Juan Pelaez** (`@jkpelaez` / `jpelaez-23blocks`), **CTO & Founder of 3Metas and 23blocks** (LinkedIn: juancarlospelaez); locations Boulder (USA) + Roma (Italy); owner org `23blocks-OS`. A named **US/Italy founder** — not a VN/Asian-cluster cultural-peer, and **not** (a)-7 Foundational-Vendor-Direct-Source (23blocks is not a substrate the vault runs on — Anthropic-tier — so no (a)-7). Notable-founder status mints **no axis** (the v139 / v159-steipete discipline). **(a) FAILS; no axis minted.** (b) carries the include.

**(b) Goal-relevance — STRONG (not STRONGEST).**
A platform that **orchestrates multiple heterogeneous coding agents** as units — one dashboard, agent-to-agent messaging (AMP), persistent memory + code-graph, multi-machine mesh, a Claude Code plugin (5 skills + 32 CLI scripts), and an HTTP/gateway surface — is dead-center "operate autonomous agents for software development." It is **the platform analog of v162 AoE** (which scored (b) STRONG) and a **structural twin of v150 Paseo** (which scored (b) STRONG, near-STRONGEST). **STRONG-not-STRONGEST** because the **STRONGEST bar is reserved for the agent *substrate*** (Claude/Anthropic — the thing you master) **or a *methodology*** (v126 compound-engineering). AI Maestro is a third-party **control-plane around** the agents — the same layer as Paseo, cmux, AoE, ai-switcher. STRONG is the calibrated rating; STRONGEST would break calibration with Paseo (its nearest neighbor, held *explicitly* below STRONGEST); MODERATE/OFF-GOAL is indefensible (it orchestrates coding agents; it is not a finance app / video editor / English guide).

**(c) Engineering quality — STRONG.**
TypeScript 87.8% (+ Shell 6.6% / JS 4.4% / HCL 0.5% / CSS / Dockerfile); **995 commits / latest v0.35.54** (Jun 5 2026). Next.js + xterm.js + Tailwind dashboard; Node.js/TypeScript backend; **CozoDB** (semantic memory) + **ts-morph** code graph with **delta indexing**; **tmux + Docker + AWS EC2 (ARM64 + Nginx + SSL) + AWS ECS Fargate** = four deployment modes; **Terraform** (HCL) infra-as-code; Vitest. Three home-grown open protocols — **AMP** (Agent Messaging Protocol: email-like agent-to-agent messaging with cryptographic signatures + cross-machine push), **AID** (Agent Identity: portable cryptographic identity), **AAP** (Agent Actions Protocol: standardized tool/API/workflow execution). A **multi-machine peer mesh** ("every machine is equal," auto-discovery, no central server). Security-forward for the niche: cryptographic message signatures + a content-security gateway that screens **34 prompt-injection patterns** before agents see input.

**(d) Corpus connectivity — STRONG.**
The genuine **2nd "Multi-Vendor Coding-Agent Orchestration Platform"** → that standalone (minted at v150 Paseo) reaches **N=2** (v150 Paseo + v163 ai-maestro); this **completes the second bucket** of the v162 orchestration-theme decomposition (see Phase 4b). Distinct from the v99 cmux / v162 AoE **session-multiplexer species** (human-driven session hosting — ai-maestro adds agent-to-agent messaging + memory + mesh on top, a platform not a multiplexer). Pattern #84 cross-tool (7+ named agents — Claude Code/Codex/Aider/Cursor/OpenClaw/Hermes/Droid). **OpenClaw + Hermes corpus-recursive** (supported agents; v149/v156/v160/v162 + v78/v112). **Corpus-recursive capability absorption** — its persistent memory (CozoDB) echoes **v66 agentmemory** (Pattern #85 agent-memory-system) + v132 supermemory, and its code graph (ts-morph + delta indexing) echoes **v70 codegraph**; the platform *absorbs* what the corpus previously catalogued as standalone subjects. T4c Operator-Notification-Bridge N+1 (Slack/Discord/Email/WhatsApp gateways; v88 teleport). Pattern #57 agentskills.io-adjacent (the bundled Claude Code plugin = 5 skills, modest). Pattern #66 supply-chain (curl|bash install + crypto-identity + injection-screening). LolaBot framework (separate `23blocks-OS/lolabot` repo — cited-to-subject-elevation watch).

## §35 — STAYS CLEAR
Rolling-3 window {v161 GA, v162 GA, **v163 GA**} = 0 OG (**11 consecutive goal-aligned ships v153→v163**). NOT an override → override-frequency UNCHANGED (lifetime 10). Streak GA:25·OG:11 [7 ov] → **GA:26·OG:11 [7 ov]** (26th GOAL-ALIGNED PASS).

## §37 — provenance (page-stated, NOT API-verified)
≈**713★** / 94 forks / 13 watchers / **12 open issues** / **995 commits** / latest **v0.35.54** (Jun 5 2026) / MIT ("free for any purpose, including commercial") / **TypeScript 87.8%** (+ Shell 6.6% / JS 4.4% / HCL 0.5% / CSS / Dockerfile / Smarty).
- Page-stated as of 2026-06 via WebFetch of the rendered repo + raw README + WebSearch corroboration (GitHub profile, two Medium posts by Juan Pelaez, the docs QUICKSTART, ai-maestro.23blocks.com) — **NOT independently API-verified (§37.4)**; this environment mocks the GitHub API.
- **Velocity not claimed:** creation date not surfaced; 713★ / 995 commits / v0.35.54 with frequent releases is "mature-but-young," but with no reliable `created_at` this is **NOT a Pattern #52 claim.**
- Owner = `23blocks-OS` org; author **Juan Pelaez** (`@jkpelaez` / `jpelaez-23blocks`), CTO & Founder of 3Metas + 23blocks (Boulder USA / Roma Italy).

## Honest non-claims
- **(a) genuinely FAILS** — US/Italy founder; not a cultural-peer, not (a)-7 (23blocks is not a vault-substrate dependency); notable-founder ≠ (a) PASS (v139). No axis minted.
- **(b) STRONG, not STRONGEST** — STRONGEST is reserved for the substrate (Claude/Anthropic) or a methodology (v126); ai-maestro is a third-party orchestration control-plane (the Paseo/cmux/AoE/ai-switcher calibration).
- **0 new §C standalones** — the PRIMARY is the v150 Paseo orchestration-PLATFORM standalone reaching N=2 (a strengthening), **not** a mint. The distinctive **AMP agent-to-agent peer messaging** is held as a candidate **sub-flavor** distinguishing ai-maestro from Paseo, and a *possible future standalone*, but is **NOT minted** — deferred to the overdue audit (the v162-ACP / v158 "don't generalize / don't mint-for-one-feature" discipline). §28 cap used at 0.
- **Memory + code-graph are absorbed sub-features, NOT separate standalones** — counting ai-maestro's bundled CozoDB memory as a v66-agentmemory instance, or its ts-morph code-graph as a v70-codegraph instance, would double-count: the *product* is the orchestrator; memory/code-graph are components. Recorded as corpus-recursive **observations**, not pattern-fuel.
- **NOT Pattern #18 #8** — ai-maestro orchestrates **agents** (and routes messages between them); it does not aggregate LLM **providers** (the PROVIDER-vs-AGENT-vs-MCP-server layer line, v151 audit). **NOT** the v158 observability sub-archetype — it **orchestrates / messages / runs** agents (dashboard status is a sub-feature, not the product). **NOT** LV-C7 #22 (it's a Next.js *web* orchestration platform, not a Tauri desktop provider/account-config management-GUI). **NOT** the cmux/AoE session-multiplexer species (it adds agent-to-agent messaging + memory + multi-machine mesh — a platform, not a human-driven session host).
- **The Claude Code plugin (5 skills + 32 CLI scripts) is PRODUCT, not internal dev-exhaust** — unlike the v122/v152/v162 internal-CLAUDE.md thread (build process, not product), ai-maestro *ships* a Claude Code plugin as part of its product surface (Medium: "My AI Agents have their own inbox … A Claude Code Skill Story"). Pattern #57-adjacent (an installed plugin, **NOT** a `npx skills add` 57k implementer claim).
- **NOT a #52 claim** (no reliable creation date + NOT API-verified §37.4).
