# (C) Pattern Library — Phase 4b — developer-portfolios (v127): the 4th-override observation (deliberately thin)

**Routine:** v2.5. **Subject:** `emmabostian/developer-portfolios` (curated portfolio link list). **Phase 0.9:** 0/4 SKIP → **operator OVERRIDE #4**. **Pattern Library state change: NONE.**

This document is intentionally thin — thinner than the v122 dograh equivalent, because this subject is *more* out-of-scope (dograh at least had internal Claude-Code agent-skill tooling to note; this has none). An out-of-scope subject contributes nothing to the agent/skill Pattern Library; manufacturing promotions here would be the exact verdict-laundering the routine forbids. The substantive contribution is META (the override event + the trigger re-fire + the v2.5 outlier-track-usage finding), not a pattern.

---

## A. The substantive registration — META: 4th override + 2-in-20 re-trip + first-ever 3-in-30 trip

**The 4th operator-elected Phase 0.9 OVERRIDE** (v84 image-blaster → v116 Sana → v122 dograh → **v127 developer-portfolios**), and the **first time the 3-in-30 override-frequency trigger fires in corpus history**:

- **2-in-20 RE-TRIPS** — v122 + v127 (and v116) within the last 20 wiki-ships. The v125 audit discharged this trigger earlier today (2026-05-30); v127 re-fires it the very next ship after the v126 GOAL-ALIGNED pass.
- **3-in-30 TRIPS for the first time** — v116 + v122 + v127 within the last ~10 ships = 3 overrides in ≤30. v84 (~40 ships back) is outside the 30-window.
- Streak: historical **"49+3\*" FROZEN @v125** (forward-only). Forward v2.5 **GA:1 · OG:0 → GA:1 · OG:1 [1 ov]**; consecutive GA streak broken by v127.

**Common-cause (confirmed by v125, re-confirmed here):** all four overrides are *notable / high-signal repos that FAIL (a) cultural-peer + (b) goal-relevance* — the override valve is being used to **capture interesting off-scope subjects**, not to rescue STRICT misses. v125 already concluded this is an *intake-channel* question (not a criteria defect) and adopted the **corpus-knowledge-outlier track** (v2.5 §31–§34) as the remedy.

**The new finding v127 surfaces:** v127 is the **first off-goal capture *after* the outlier-track adoption** — and it was still routed through the **override** door, so it spent override budget and re-tripped both triggers. The outlier track (no streak/override accounting) was designed precisely so this wouldn't happen. So the next audit's live question is a **usage/default** question, not a criteria one: *why did an off-goal capture still go through override rather than the outlier track?* (operator preference vs. a missing default-path/invocation cue). Recommend the next audit decide the **outlier-track invocation default** so off-goal captures stop consuming override budget.

---

## B. Domain-agnostic / incidental observations (NO state change)

| Observation | Pattern | Strength | Note |
|---|---|---|---|
| No LICENSE file / `license: null` | **#29 Absent-LICENSE** | N+1 (incidental) | Real instance, but incidental at an out-of-scope subject. No state change. |
| Default branch is `master` (non-`main`) | (metadata) | thin | Cf. v100 Easydict `dev`; a metadata curiosity, not a pattern. |
| ~6.7-year lifespan (created 2019-09-13) | (metadata) | thin | **Corpus-record longevity** (exceeds v100 Easydict ~3.5y). A curiosity, not a pattern. |
| ~0.20 fork_ratio | (negative evidence) | thin | **NOT** deployment intent — it's the fork-to-PR contribution model (forks ≈ contributors). Recorded as negative evidence against reading fork_ratio as deployment. |
| Curated-list shape | (archetype, nominal) | thin | Shares the shape of awesome-* corpus subjects (v50, awesome-mcp-servers) but lists portfolio *websites*, not AI/agent tooling — shape-only, no domain link. |

**§28 discipline honored:** ZERO new Library-vocab standalones minted (cap = 2; an out-of-scope subject should mint none). None of the above is a new registration; all are observations-only, and the registry `06-library-vocab-registry.md` is **not** moved (per the §28 rule-5 reminder added at the v120 audit: "filing is an act, not a claim" — these are deliberately *not* filed, because an out-of-scope override should not move the registry).

---

## C. Honest non-claims

- **NOT** an agent-for-software-development tool; **NOT** a Claude-Code skill/plugin; **NOT** an agentskills.io implementer; **NOT** an MCP server; **NOT** an LLM application; **NOT** a substantial codebase.
- **NOT** a Pattern #52 velocity instance (~9.6/d sustained-low).
- **NO** new top-level Pattern; **NO** sub-archetype; **NO** Library-vocab promotion; **NO** Pattern Library state change.
- Unlike v122 dograh, there is **no** genuine corpus thread to carry forward — no internal Claude-Code tooling, no agent surface. The honest takeaway is entirely META: the 4th override and the first 3-in-30 trip.

---

## D. The one thing actually worth carrying forward

Not a pattern — a process signal. **The override channel and the outlier track now overlap, and v127 chose override.** If the corpus-knowledge-outlier track is to do its job (absorbing notable-but-off-goal captures without spending override budget or re-tripping frequency triggers), the next audit needs to make outlier-track tagging the **default** for off-goal captures, with override reserved for genuine STRICT-miss rescues. Until that default exists, every "build this interesting off-goal repo" request will keep firing the override triggers — as v127 just demonstrated, one ship after they were discharged.
