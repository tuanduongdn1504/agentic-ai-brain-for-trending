# (C) Phase 0 + Phase 0.9 verdict — developer-portfolios (v127) — **OPERATOR OVERRIDE**

**Routine:** v2.5. **Fetched:** 2026-06-01 (GitHub API + README). **Verdict:** STRICT **0/4 → SKIP**, force-built by **operator-elected OVERRIDE** (**4th in corpus history** after v84 image-blaster + v116 Sana + v122 dograh). Streak base = historical "49+3\*" FROZEN @v125; forward v2.5 base = GA:1 · OG:0 (v126).

---

## Phase 0 — scope gate

| Gate | Result |
|---|---|
| Repo reachable + readable | ✅ `emmabostian/developer-portfolios` |
| License present | ⚠️ **None specified** (GitHub API `license: null`; no LICENSE file) — mechanical soft-fail, immaterial to an out-of-scope override |
| Active / recently pushed | ✅ pushed 2026-05-31; created **2019-09-13** (~2,453 days ≈ **6.7 years** — longest-lived subject in corpus) |
| Scale floor (≥1★) | ✅ **23,531★** / 4,625 forks (fork_ratio ~0.20, **inflated by the fork-to-PR contribution model** — forks ≈ contributors, not deployments) |
| Tier classification | **Corpus-outlier** — not in the T1–T5 taxonomy. Forced-nearest = a curated-/awesome-list shape, but those corpus list-subjects curate *agent/AI tooling*; this curates *portfolio websites*. |

**Phase 0 = clears the scale/activity floors, but FLAGGED severely out-of-domain.** Every prior corpus subject sits in the Claude-Code / agent-skills / autonomous-coding / LLM-tooling ecosystem. This is a curated list of developer-portfolio websites with no AI/agent content of any kind — the most out-of-domain subject the corpus has gated.

---

## Phase 0.9 — STRICT 4-criterion INCLUDE filter

### (a) Author cultural-peer to Storm Bear — **FAIL**

Author = **Emma Bostian** (`@emmabostian`), a prominent **US/Europe-based developer-educator** (software engineer; ex-Spotify, Stockholm; Ladybug Podcast co-host; technical-career author). None of the corpus's (a) sub-axes apply:

- Not a VN/Asian *indie* cultural-peer ((a)-1…(a)-6): she is a well-established Western developer-educator at large-company / public-figure scale, not a solo/indie builder at Storm Bear's scale-and-context, and not in any VN/China-Mainland/Asian cluster.
- Not **(a)-7 Foundational-Vendor-Direct-Source** — the vault depends on Anthropic/Claude, not on a portfolio-list maintainer.
- No (a)-rescue is even available — there is no cultural-peer angle to rescue on (contrast v80/v123, where an inferred-VN/China-Mainland author carried an off-goal subject through (a)).

**(a) FAIL — clean, no rescue.**

### (b) Goal-relevance / vault-utility — **FAIL (hard)**

Documented #1 goal: *"Master Claude and autonomous agents for software development."* developer-portfolios is **a list of links to developers' personal portfolio websites, for inspiration.**

- Not a vault tool, not a Scrum tool, not a coding assistant, not a dev-workflow utility, not an agent, not an LLM application.
- Cost-of-trial is trivially LOW (you scroll a README), but directness to goal #1 is **exactly zero** — there is nothing to *use* toward autonomous-agent/software-dev mastery.
- This is the weakest (b) of any override: even v122 dograh built *autonomous agents* (just for telephony); this builds nothing — it indexes websites.
- Faint tangent: "developers' professional presentation" brushes Storm Bear's people/coaching side, but that is not goal #1 and a link list is not a coaching instrument.

**(b) FAIL.**

### (c) Instructive / exemplary — **WEAK → effectively FAIL**

- **The subject** is a Markdown list + a `feed.json` + a `CONTRIBUTING.md` PR workflow. As *community curation at scale* (1,742 entries, ~6.7 years, ~5,977 commits, a programmatic feed) it is a competent, long-lived example of running an open contribution list — but that teaches nothing transferable to the corpus's agent/skill/distribution/governance methodology.
- There is no architecture, no agent design, no skill authoring, no harness, no protocol — nothing the corpus exists to study.

**(c) WEAK** (charitably; effectively FAIL for corpus purposes). The lone instructive sliver — "how to run a big community-curated list" — is real but off-corpus.

### (d) Corpus connectivity — **FAIL**

- No agentskills.io chain, no SKILL.md, no Karpathy lineage, no Claude/MCP adjacency, no Pattern-Library lineage.
- The only connection is **nominal**: it shares the *shape* of corpus "awesome-list" subjects (v50 awesome-claude-skills, awesome-mcp-servers) — but those list agent/AI tooling, and this lists portfolio websites, so the shape-link carries no domain content.

**(d) FAIL.**

---

## Verdict

| Criterion | Result |
|---|---|
| (a) cultural-peer / (a)-7 | **FAIL** (no rescue available) |
| (b) goal-relevance / vault-utility | **FAIL (hard)** |
| (c) instructive | **WEAK → effectively FAIL** (off-corpus curation only) |
| (d) corpus connectivity | **FAIL** (nominal list-shape only) |

**STRICT = 0 clear PASS → SKIP.** Honest placement on the override scale: **the most out-of-scope override to date.** v84 image-blaster was an in-domain Claude-Code skill bundle; v116 Sana was a real instructive ML codebase; v122 dograh at least built autonomous agents and had internal Claude-Code tooling. developer-portfolios is a curated link list with no AI/agent surface whatsoever — it is further from corpus scope than all three.

**Operator decision (2026-06-01): OVERRIDE → INCLUDE.** Recorded honestly as the **4th operator-elected Phase 0.9 OVERRIDE** in corpus history (v84 → v116 → v122 → v127). Under routine **v2.5 §31** this is an **OFF-GOAL CAPTURE** ((b) FAIL) entering via the **override** channel (since (a) also FAILs, no (a)-rescue). Per **v2.5 §32** (FORWARD-ONLY streak-split; override triggers reconciled-not-replaced):

- Historical **"49+3\*" remains FROZEN @v125** (not recomputed, not re-tagged).
- Forward v2.5 streak: **GA:1 · OG:0 → GA:1 · OG:1 [1 ov]** — v127 increments the OFF-GOAL CAPTURE tally; `[1 ov]` marks it as the 1st override under v2.5 (4th lifetime). The consecutive **GA streak is broken** by v127 (current consecutive goal-aligned PASS run = 0; lifetime-under-v2.5 GA = 1).
- **⚠️ Override-frequency triggers — BOTH fire:**
  - **2-in-20 RE-TRIPS** — v122 + v127 (and v116) within the last 20 wiki-ships. The v125 audit discharged this trigger earlier today; v127 re-fires it.
  - **3-in-30 NOW TRIPS for the FIRST TIME** — v116 + v122 + v127 are all within the last ~10 ships (3 overrides in ≤30). v84 (~40 ships back) is outside the 30-window, so the firing three are v116/v122/v127.

**Mandated next-audit task:** the v125 audit already diagnosed the override pattern as an **intake-channel** issue (notable-but-off-goal repos the operator wants captured that FAIL (a)+(b)) and adopted the **corpus-knowledge-outlier track** (v2.5 §31–§34) as the fix. v127 is the first off-goal capture *after* that adoption — and it was routed through the **override** door anyway, spending override budget and re-tripping both triggers. The live question for the next audit is therefore **not** a criteria redesign but a **usage** question: should off-goal captures like this be tagged as pure corpus-knowledge-outliers (no override/streak accounting), and if so, why did v127 still go through override — operator preference, or a default-path/tooling gap? Recommend deciding the outlier-track *invocation default* before the next off-goal capture.

**Honest framing carried to every artifact:** developer-portfolios is corpus-knowledge-of-an-outlier. It is NOT a pilot candidate, NOT goal-aligned, and contributes NO change to Pattern Library state.
