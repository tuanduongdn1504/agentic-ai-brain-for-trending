# (C) Pattern Library mini-audit — v162 (2026-06-15)

**Type:** operator-requested audit immediately after the v162 agent-of-empires ship. Owes (operator's words): "the session-multiplexer-species N=2 decomposition on top of the standing LV-C7/observability calls and a fuller shim reconcile."
**Window:** v159 → v162 (post-v158-audit accumulation: v159 CodexBar, v160 ping-island, v161 CodePilot, v162 agent-of-empires).
**Routine:** v2.6. **Branch:** main (post-v162-merge `acbdd00`).
**Fact-verification: LIGHT** — this environment mocks the GitHub API (§37.4), so provenance is page-stated-not-API-verified; this is a **reasoning / bookkeeping audit**. The clean-fact-verification streak is **HELD, not advanced** (git ship-log v159→v162 verified to match state).

---

## AUTHORITATIVE STATE AFTER THIS AUDIT

- **Top-level confirmed patterns: 46 (UNCHANGED).**
- **CONFIRMED Library-vocab: 10 (UNCHANGED)** (#12, #14–#22).
- **CONFIRMED tier sub-archetypes: 2 (UNCHANGED count)** — "AI-Agent Observability / Status-Surface Tool" (CONFIRMED v158, now **N=9**: metering (a) N=5 + ambient-pet (b) N=3 + attention-routing (c) N=1) + T1 Domain-Vertical-Skill-Collection (CONFIRMED v101) … etc.
- **NEW PROVISIONAL tier sub-archetype REGISTERED + RENAMED: "Session-Hosting Multiplexer for AI Coding Agents (human-driven)" — N=2** (v99 cmux + v162 AoE). Renamed delivery-agnostic from cmux's working label "Native-Desktop Terminal Multiplexer." **PROMOTION-ELIGIBLE at N=3** (NOT confirmed at N=2 — the corpus bar is N=3). This **decomposes** the v151/v158 "AI-agent orchestration theme HELD heterogeneous" finding.
- **ACP (Agent Client Protocol) registered as a Pattern #18 sub-mechanism-B protocol-variant CANDIDATE (B3-candidate), PROVISIONAL N=1** (v162 AoE, first architecturally-first-class instance). Pattern-layer; NOT a §C standalone.
- **Tracked PROVISIONAL Library-vocab surface ≈ 25 (UNCHANGED)** — no new standalones, no retires.
- **Streak UNCHANGED (audit ≠ ship): GA:25 · OG:11 [7 ov]. §35 CLEAR** (window {v160 GA, v161 GA, v162 GA} = 0 OG; 10 consecutive goal-aligned ships v153→v162).
- **Cadence reset: next natural audit ~v167–v168.**

---

## DECISION 1 (headline) — Session-multiplexer species N=2: DECOMPOSE the held orchestration theme + REGISTER + RENAME

**The deferred call (from the v162 ship):** AoE is the genuine 2nd instance of cmux's session-hosting-multiplexer species → species N=2; confirm-or-split + rename was DEFERRED here.

**Decision:**
1. **DECOMPOSE the v151/v158 "AI-agent orchestration theme = HETEROGENEOUS → HELD, not minted" finding.** With AoE as a clean 2nd session-multiplexer instance, the theme is demonstrably *less* heterogeneous than it looked at v151/v158 (when cmux was a lone N=1 parenthetical). It decomposes into three distinct things:
   - **Session-Hosting Multiplexer for AI Coding Agents (human-driven)** — hosts N parallel coding-agent sessions, one per branch/worktree, terminal-multiplexer underneath, attention-signaling UI; a HUMAN drives. **N=2: v99 cmux (native-desktop, libghostty/Swift) + v162 AoE (cross-platform TUI+web, Rust+tmux).**
   - **Cross-Device Multi-Vendor Coding-Agent Orchestration PLATFORM (agent-of-agents)** — Library-vocab §C standalone, **N=1: v150 Paseo.** Its load-bearing distinction = the `/paseo-*` skill suite that makes one agent orchestrate others. **Distinct from the multiplexer species:** AoE is the *inverse* — a thing TO BE orchestrated via its HTTP API ("integrates with OpenClaw"), not an orchestrator. Paseo STAYS N=1 (AoE is NOT a 2nd Paseo).
   - **Agent-Team-Architecture Generator** — v131 harness (adjacent; generates a team's architecture, doesn't run sessions).
2. **REGISTER** the session-multiplexer as a tracked PROVISIONAL tier sub-archetype at **N=2, PROMOTION-ELIGIBLE at N=3.** (NOT CONFIRMED at N=2 — the corpus confirms sub-archetypes at N=3: observability N=7, Domain-Vertical N=3, #18 #8 N=3.)
3. **RENAME** cmux's working label **"Native-Desktop Terminal Multiplexer for AI Coding Agents" → "Session-Hosting Multiplexer for AI Coding Agents (human-driven)"** (delivery-agnostic). **Why the rename is legitimate, not generalize-to-fit inflation:** "native-desktop" was an N=1-instance descriptor; AoE is a genuine 2nd instance with a *different delivery* (cross-platform TUI+web), which proves the species is not desktop-bound. Broadening a name at the point a 2nd instance proves the generalization is standard corpus practice (the v158 audit broadened "Observability" the same way). The two deliveries are recorded as **two sub-flavors INSIDE the sub-archetype** (native-desktop / cross-platform-TUI+web), not split — the 8a/8b-inside-#8 precedent.
4. The v162 ship correctly **deferred** this rather than pre-deciding it (the v158 "don't generalize-the-definition-to-fit-the-2nd-instance" discipline) — the audit is the right place, and the call is register-and-rename-but-hold-PROVISIONAL, not confirm.

**Recorded in `02b` (tier-sub-archetype layer). 0 new §C standalones; top-level count UNCHANGED at 46.**

---

## DECISION 2 — Standing post-v158 calls (reconcile; mostly HOLD)

| Call | Status at audit | Decision |
|---|---|---|
| **Observability sub-archetype** (CONFIRMED v158) | N=7 → **N=9** (v159 metering N=5, v160 attention-routing (c) N=1) | No change needed; CONFIRMED. Sub-flavor **(c) attention-routing (v160 ping-island) HELD N=1, promotion-eligible at N=2** (no 2nd in-corpus instance; the external "Dynamic Island for agents" genre is NOT counted). |
| **CodePilot "Desktop GUI Client / Visual Front-End for a CLI Coding Agent"** (§C standalone, v161) | N=1 | **HOLD, promotion-eligible at N=2.** **AoE is NOT a 2nd instance** — different species (CodePilot = single-agent visual front-end replacing the terminal; AoE = fleet multiplexer of normal terminal sessions). |
| **#18 #8 "GUI-client aggregator" sub-variant (8c?)** (flagged v161) | 1 instance (CodePilot) | **DECLINE the mint.** One instance; anti-inflation. #18 #8 stays CONFIRMED N=3 (8a config-switcher v73/v117 + 8b live-routing-proxy v112); CodePilot's 17-provider aggregation recorded as a #8 N+1 instance only. Re-mint 8c only at a 2nd GUI-client aggregator. |
| **ACP (Agent Client Protocol)** (v162 flag) | first architecturally-first-class instance (AoE); incidental priors v60/goclaw/get-shit-done | **REGISTER as a Pattern #18 sub-mechanism-B protocol-variant CANDIDATE (B3-candidate), PROVISIONAL N=1.** The principled home (B1 MCP / B2 CDP precedent — B2 was registered at N=1 at v70 codegraph, so B3-at-N=1 is consistent). NOT a §C standalone (it's a protocol, and the corpus routes protocols to #18 B). Promotion to a *formalized* variant at a 2nd architecturally-first-class ACP subject. Editor↔agent protocol (Zed-ecosystem, Apache 2.0, crates.io `agent-client-protocol`), distinct from B1 MCP (agent↔tool/data) + B2 CDP (browser control). |
| **MCP-server / B1 dense-line** | N=7 (reconciled v151, mint declined v151+v158) | **CLOSED, stays N=7.** AoE is NOT a new B1 server (it's a session manager, not an MCP server). |
| **Social/Competitive Leaderboard for AI-Coding Usage** (§C standalone, v158) | N=1 | **HOLD, promotion-eligible at N=2.** |
| **LV-C7 #22 "Tauri-Desktop Management-GUI for a Coding Agent"** (CONFIRMED v158) | N=3 | No new instance; no change. (AoE is NOT #22 — not Tauri, and manages session/execution not provider/account config.) |
| **§28.3 time-aware auto-retire** | — | **NO retires.** Every live N=1 §C standalone was filed during the v126–v162 June burst (2026-06-01 → 2026-06-15) → none has crossed BOTH ≥15 wikis AND ≥30 days (the 30-day floor protects them — exactly the burst-cadence calibration §28.3 was adopted for at v151). |

**Override-frequency / off-goal-intake review (running):** v153→v162 = **10 consecutive GOAL-ALIGNED ships, ZERO overrides** — the override door has not fired this window. Common-cause RE-CONFIRMED (6th time after v120/v125/v134-135/v151/v158) = operator off-goal cataloguing via the override door is an *intake-channel* matter, **NOT a Phase-0.9 STRICT criteria defect**; the 7-in-20 / 10-lifetime figures are trailing (the last override was v152). **NO criteria redesign; NO 4th governor.** v151's two recommendations remain **PENDING operator sign-off** (re-surfaced below). Positive signal: when subject-selection is goal-aligned, no governor fires.

**⚠️ The real standing lever (re-stated, 6th audit running):** v153→v162 = **10 consecutive "tool for operating AI coding agents" ships, ZERO piloted.** v161 CodePilot broke out of the menu-bar-monitor sub-niche; AoE is the most pilotable of the run (it's literally this vault's own workflow). The lever is **pilot one / vary the domain**, not more accounting.

---

## DECISION 3 — Fuller shim reconcile

The root shim (`CLAUDE.md`) had drifted well beyond the v162 head (the v162 ship brought the *head* current + flagged it). This audit reconciles the **live misleading pointers** and **flags the stale append-log sections** rather than rewriting ~77 versions of historical narrative (that would be high-risk + low-value; the authoritative current content is the chapter file + `04 Reviews/`).

**Fixed (live, misleading):**
1. **`_state/03c-projects-v61-v151.md` → `_state/03c-projects-v61-v162.md`** (4 occurrences — the "full per-wiki detail lives in" / "verdict history in" / "Where the detail lives" pointers in the PL-header, streak line, and Weekly-Update sections). These are live pointers that became stale as the chapter was renamed at each ship without them being updated; they now point to the actual current chapter. **Verified safe:** all 4 are "detail lives in" pointers, none is a historical "renamed to v151" statement (v151 was an audit, no rename).
2. **Projects-index lead pointer** (`v61-v100 (most recent): 03c-projects-v61-v100.md`) — corrected to name the current chapter `03c-projects-v61-v162.md`.

**Flagged (stale append-log; NOT rewritten — documented as deliberate):**
3. **"## My Current Projects & Overviews"** is a stale append-log frozen at the ~v85–v100 era ("Latest active projects … Most recent 24 (v61-v84)"; per-version range entries with v100-era counts/pointers). A **currency banner** was prepended: the authoritative current chapter is `03c-projects-v61-v162.md` (entries through v162); the per-version entries below are historical and NOT maintained per-ship → see the chapter + `04 Reviews/`. Rewriting it is out of scope for an audit (it's append-log history; rewriting risks fabricating per-version detail).
4. **"My Goals & Current Progress / Current state:"** is a v78-era snapshot ("78-wiki corpus shipped + routine v2.2 …"). A **currency note** was prepended: current = ~162 wikis as of 2026-06-15, routine v2.6, streak GA:25 (10 consecutive GA v153→v162); the block below is a v78-era snapshot, not maintained per-ship.

**Honest scope statement:** "fuller reconcile" here = make the live pointers correct + flag the frozen snapshots so no agent is misled, NOT rewrite the historical append-log. A full shim rebuild (regenerating the Goals/Projects sections from current state) is a separate, larger task — recommended as a dedicated housekeeping session if the operator wants the shim fully current, not folded into a mini-audit.

---

## RECOMMENDATIONS (re-surfaced; PENDING operator sign-off — NOT auto-adopted)

Carried unchanged from v151 (still pending):
- **(i) off-goal-intake lever** — re-baseline the override trigger OR make the corpus-knowledge-outlier track the *default* for off-goal subjects (recommended). [The v153→v162 10-GA run means this hasn't bitten recently, but the door is still open.]
- **(ii) (a)-rescue tightening** — require a *solid* (a) signal (declared/located, or registered (a)-7), NOT a name-heritage inference, at the (a)-rescue gate.

New (this audit):
- **(iii) dedicated shim-rebuild housekeeping session** — the Goals + Projects-index sections are ~77 versions stale; a one-time rebuild from current state (not a per-ship or per-audit task) would retire the drift. Flagged, not scheduled.
- **(iv) the standing pilot lever** — 10 consecutive operating-agents ships, zero piloted; AoE is the most pilotable. Pilot one or vary the domain.

---

## Counts (post-audit)
**46 confirmed patterns / 10 CONFIRMED Library-vocab / 2 CONFIRMED tier sub-archetypes + 1 NEW PROVISIONAL tier sub-archetype (session-multiplexer N=2) + ACP B3-candidate PROVISIONAL N=1 / tracked PROVISIONAL Library-vocab surface ≈25 (UNCHANGED) / streak GA:25 · OG:11 [7 ov] UNCHANGED (audit ≠ ship) / §35 CLEAR / next natural audit ~v167–v168.**

Storm Bear's blunt take: the headline decision is genuinely additive and honest — AoE is a real 2nd session-multiplexer, so the orchestration theme the last two audits held "heterogeneous" finally decomposes into three clean things (multiplexer-species N=2 / Paseo-platform N=1 / harness-generator adjacent), and the rename is the legitimate generalization a 2nd instance earns — held at PROVISIONAL N=2, not over-promoted. Everything else HOLDS (no manufactured promotions): CodePilot's GUI-client and the social-leaderboard stay N=1, ACP gets its principled home as a #18-B3 candidate (not a fake standalone), the 8c mint is declined, B1 stays N=7. The shim reconcile is bounded and honest: I fixed the live pointers that actively mislead and flagged the ~v85-era append-log rather than rewrite history I can't reconstruct. The override door hasn't fired in 10 ships — but the corpus has still piloted nothing, and that's the lever, not the accounting.
