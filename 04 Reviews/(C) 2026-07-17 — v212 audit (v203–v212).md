# (C) 2026-07-17 — v212 audit (window v203 → v212)

> The natural audit the v203 audit (2026-07-16) scheduled at "~v212." Nine ships landed since with no audit (v204–v212; v203 was the prior audit). Operator-run, operator-signed on the headline decision.
> **Routine:** LLM Wiki Routine v2.7. **Verification:** hand — the ~205K CLAUDE.md shim overflows every subagent's 200K context (the v200→v212 self-throttle), so no workflow; the §F running log (v190→v212), the §C rows, and the v203 audit doc were hand-read this session.

---

## 0. Headline

**Counts UNCHANGED: 46 top-level patterns / 11 CONFIRMED Library-vocab.** 0 promotions, 2 §C mints (OfficeCLI v206, CLIProxyAPI v207), 0 hard retires. §C live standalones **40 → 42**; tracked PROVISIONAL surface **≈47 → ≈49**. Override review **DISCHARGED, 10th consecutive** (v153→v212 = 0 overrides). §35 CLEAR. Streak **GA:72 / 58 consecutive GA v153→v212** (audit ≠ ship — unchanged by this audit).

**The one count-changing decision — the v192 §C standalone promotion — was put to the operator and DECIDED: HOLD at N=2, promotion to CONFIRMED DEFERRED to a genuinely-independent non-port N=3.** Details in §2.

---

## 1. Window ship outcomes (v204–v212)

| ship | outcome | §C |
|---|---|---|
| v204 hallmark | NO MINT — Pattern #88 88c instance-strengthening (anti-slop design skill) | — |
| v205 system_prompts_leaks (asgeirtj) | NO MINT — Pattern #38 38a + #21 N=1→N=2 (cross-author 2nd prompt-archive; recorded not self-incremented) | — |
| v206 OfficeCLI | **+1 §C standalone N=1** — "Agent-First Office-Document Creation+Manipulation Capability Layer" | C41 |
| v207 CLIProxyAPI | **+1 §C standalone N=1** — "Self-Hosted Multi-Provider API Gateway (re-exposes CLI-tool OAuth subscriptions)" | C42 |
| v208 OmniRoute | **N=2 of the v207 standalone** — but a TypeScript **PORT** of v207 → promotion DEFERRED (see §2) | — |
| v209 gpt-5.6-instruct | NO MINT — offensive-jailbreak threat-intel data-point + DEFERRED watch axis | — |
| v210 AIRI | NO MINT — AI-companion/VTuber DOMAIN data-point + DEFERRED watch axis | — |
| v211 PixelRAG | NO MINT — visual/screenshot-RAG technique data-point + DEFERRED watch axis; model/research-substrate flavor | — |
| v212 tabularis | **N=2 of the v192 standalone** — non-port, cross-domain, cross-author (see §2) | — |

Net §C: +2 rows (C41, C42); the two N=2 events add no rows. **§C live standalones = 42.**

---

## 2. The two N=2-of-a-standalone events (the promotion questions)

### 2a. v207 "Multi-Provider Subscription-Gateway" → N=2 via OmniRoute v208 — HOLD (a PORT)
OmniRoute v208 is, by its own README, a TypeScript **port** of CLIProxyAPI v207 (+ a fork of 9router). A port is not a genuinely independent recurrence — it's the same thing reimplemented. → **N=2 recorded, promotion to CONFIRMED DEFERRED pending a fully-independent non-port 3rd instance** (kRouter / one-api / new-api / 9router are the landscape candidates). *Clear call, unchanged from the v208 ship note.*

### 2b. v192 "Product-First Native Application Retrofitted with a First-Party MCP Server" → N=2 via tabularis v212 — HOLD at N=2, defer to N=3 (OPERATOR-DECIDED)
- **N=1:** palmier-pro v192 (native macOS video editor + first-party MCP server, 51 tools).
- **N=2:** tabularis v212 (cross-platform desktop database GUI + first-party MCP server, 4 tools).
- This is the **strongest form of N=2**: fully-independent, cross-author, cross-domain, **non-port** — decisively stronger than OmniRoute's port-N=2 in 2a. And the v192 row's *own* definition predicted "the same move now spreading across Figma/Notion/Linear/Blender-class tools" — tabularis is that prediction beginning to play out.

**Decision (operator-signed): HOLD at N=2, promotion to CONFIRMED DEFERRED to a genuinely-independent non-port N=3.** Rationale:
1. **The corpus has never promoted a §C standalone to CONFIRMED below N=4** — the only §C→CONFIRMED promotion in corpus history (#23 code-knowledge-graph, at the v182 audit) was at N=4 (graphify v16 / GitNexus v33 / codegraph v70 / codebase-memory-mcp v172).
2. **The v203 audit explicitly held its N=2 buckets "eligible@N=3"** (session-multiplexer, orchestration-platform). Promoting the v192 standalone at N=2 would be a bar-lowering precedent.
3. **Anti-inflation (§28):** N=2 is the minimum recurrence; N=3 demonstrates a genuine class rather than a coincidence of two.
4. **The "non-coding domain" clause-stretch** (below) means the two instances aren't yet a perfectly identical class.

**But recorded:** this N=2 is materially stronger than any the corpus has deferred, and the class is very likely real — a clean non-port 3rd instance (the Figma/Notion/Linear/Blender-class move) → **promote at the next audit.** The row's status is upgraded to "PROMOTION-ELIGIBLE — strengthened N=2 (fully-independent, cross-author, cross-domain, non-port); a non-port N=3 confirms."

**Clause cleanup (decided, applied to the row):** the v192 standalone's definition said "an independently complete … GUI application **in a non-coding domain**." tabularis is developer/data-tooling — *coding-adjacent*, not the video-clean "non-coding" of palmier-pro. The clause exists to exclude agent-tool-first servers (the MCP surface IS the product), not to exclude developer tools whose primary function is not being-an-agent-tool. → **Generalized to "any domain where the product's primary function is not being an agent tool."** tabularis is decisively product-first (a full human SQL GUI + a bolt-on MCP server), so it fits the generalized clause cleanly.

---

## 3. Deferred watch axes — status in the window

| axis | v203 status | v204–v212 | v212 status |
|---|---|---|---|
| Persona-council orchestration (ai-berkshire v187 + marketingskills v202) | HOLD N=2, eligible@N=3 | no 3rd in window | **HOLD N=2** |
| Model / Inference-Substrate tier (GLM-5 v176 / DeepSpec v186 / TimesFM v193) | descriptive N=3 | PixelRAG v211 (research-retrieval / VLM-substrate flavor) | **descriptive N=4** (bookkeeping only; counts unchanged) |
| shared-context-foundation-skill (marketingskills `product-marketing`) | HOLD N=1 | no 2nd | **HOLD N=1** |
| Orchestration-platform (Paseo v150 + ai-maestro v163) | HOLD N=2 | no clean 3rd | **HOLD N=2** |
| Session-multiplexer (cmux v99 + AoE v162) | HOLD N=2 | no clean 3rd | **HOLD N=2** |
| Domain-Vertical-Skill-Collection | CONFIRMED, ≥6 verticals | no new vertical | **unchanged (≥6)** |
| §C#4 Cross-Functional Skill Collection | HOLD N=2 | — | **HOLD N=2** |
| Disclosed-individual / notable-author (a)-axis | KEEP HELD, NOT registered | El Mghari v204 / Johnson v205 / Debernardi v212 — all FAIL-(a), GA via (b) | **KEEP HELD, NOT registered** (rec-ii — registering re-opens the off-goal (a)-rescue door) |

**New DEFERRED watch axes minted in-window** (recorded, NOT §C standalones):
- v209 "weaponized offensive LLM-jailbreak / safety-bypass prompt pack" (threat-intel).
- v210 "self-hosted real-time embodied multimodal AI-companion / VTuber runtime."
- v211 "visual / pixel-space multimodal RAG (retrieval over rendered document screenshots) + pixel-surface agent perception (screenshot-not-HTML)."
- v212 "database-GUI / SQL-client domain" (corpus-first-DOMAIN data-point — first of its kind; NOT a §C mint, domain-not-capability).

---

## 4. Bookkeeping

- **#18 sub-archetype B / B1-MCP → ≈N=13** (v203 ≈N=11 + OfficeCLI v206 built-in MCP server + tabularis v212 built-in MCP server; exact tally = audit bookkeeping). Capability-vs-distribution → **no double-count** with the OfficeCLI §C standalone or the v192 §C standalone (the v140/v192 precedent: the standalones are about the capability/relationship, #18 B1 is the one-server-many-clients distribution shape).
- **#57 genuine forward-citations in-window:** OmniRoute v208 → CLIProxyAPI v207 (its port-parent) + headroom v144 (its compression engine) = **#57 ×2** (doubly corpus-recursive; distinct from v181 cortex-hub's *silent* bundling of v207 = NOT #57). No other #57 in-window (v212 tabularis: Kilo Code v177 *sponsors* it = corpus-recursive cross-ref, sponsorship ≠ influence-citation = NOT #57).
- **Provider-agnostic LLM-seam thread** (#18 #8 + #84 84c): strengthened by meetily v196 `generate_summary()` / OmniRoute v208 / AIRI v210 `xsAI` / tabularis v212 text-to-SQL. NO N-bumps (84c is not the ponytail generator mechanism); recorded as a live cluster.
- **LV-C7 Tauri-desktop cluster** → +tabularis v212 (cc-switch v73 / CodexPlusPlus v117 / OpenHuman v118 / PilotDeck v175 / meetily v196 / tabularis v212). Bookkeeping, no promotion.
- **Cited-to-Subject Elevation** (C10) — HOLD N=2 (Kilo Code v177 was a weak watched 3rd at v182/v203; no change in-window).

---

## 5. Override review · §35 · retires

- **Override review DISCHARGED, 10th consecutive** — v153→v212 = **0 operator overrides**; lifetime 10 (3 logged `[ceiling-override]` v146/v148/v152). The 2-in-20 / 3-in-30 frequency triggers are not firing.
- **§35 CLEAR** — rolling-3-ship window {v210 GA, v211 GA, v212 GA} = 0 OFF-GOAL. (Under the strict OFF-GOAL alternative reading of v209/v210 — both admitted GA via §40 operator-direction — the window is still ≤1 OG → clear.)
- **§28/§39 retires: 0 hard retires.** The in-window N=1 mints (OfficeCLI v206, CLIProxyAPI v207 — both 2026-07-16) are <30 days AND <15 wikis → under both floors, protected. The v203-flagged pre-v187 stale N=1 rows **C22–C27** remain past both floors → **a dedicated retire pass is STILL DEFERRED** (the v203 "avoid destructive edits on unverified one-offs; a batch retire pass warrants its own focused session" stance carries forward). Flag for a dedicated housekeeping session.

---

## 6. Standing items / next audit

- **The v192-standalone promotion is the live headline for the *next* audit** — a fully-independent non-port N=3 confirms; the Figma/Notion/Linear/Blender-class move the v192 row predicted is the thing to watch.
- **The v207-standalone promotion** likewise waits on a fully-independent non-port 3rd instance.
- **The C22–C27 retire pass** should get a focused housekeeping session.
- 🔴 **SHIM-SIZE remains the #1 infrastructure item + a load-bearing workflow blocker** — the ~205K shim overflows every subagent > 200K → the deep-dive workflows have failed `prompt-too-long` on every ship v200→v212 (all hand-built). The accreted CURRENT/PRIOR head bodies + Weekly-Update PRIOR blocks (v195→v211, full detail already in `_state/03c`) should be compacted to one-line pointers to restore workflow capability. The v212 ship wrote its head COMPACT + demoted v211, but the backlog stands.
- **Next natural audit ~v221** (≈9 ships, matching the v182→v203→v212 ≈20/≈9-wiki cadence).

**State after this audit:** 46 top-level patterns / 11 CONFIRMED Library-vocab (UNCHANGED); §C live standalones 42; tracked PROVISIONAL surface ≈49; streak GA:72 / 58 consecutive GA v153→v212.

Shipped on `wiki/audit-v212` off the v212 tip (`7c3df89`); the chain v204 → … → v212 → audit-v212 merges in order. Not auto-merged — operator reviews + merges.
